# .estree_plans.md — durable plan backlog for the ESTree improvement loop

The worker's improvement mode executes the FIRST entry with `Status: planned`.
After verification, the pi updates the entry (Status -> implemented / blocked,
benchmark results, issues). The planner (human or worker analysis) maintains
priorities. Benchmarks: `CAL_MS=100 bash bench.sh <sample>`.

---

## Plan 4: lastExit-write liveness — drop dead `(( ))` ternaries / lastExit writes

- Status: **IMPLEMENTED** (direct, 2025-08-03 — 60c024f)
- Benchmark after: hot-loop `((i++))` body 17.6 -> 15.6 ms/300k (1.13x);
  bench-increment's loop is now `() => { i++; __n = __n + 1 }` — zero
  runtime surface per iteration. Call-site metric UNCHANGED (2865 —
  lastExit writes are property writes, not calls — expected).
- Issues: exposed a latent estree-gen bug (postfix `i++` rendered as
  prefix `++i`) — fixed in estree-gen.mjs; new corpus test
  arith-postfix-status.sh covers it. Errexit programs skip the pass (the
  guard consumes every statement's value). `$?` readers (getVar("?"),
  and/or, exit/return, subshell bodies) keep writes live. 529/529 estree.
- Priority: 1
- Target: `((i++))` / arithmetic-expression statements currently emit
  `((<expr> !== 0) ? ((sh2.lastExit = 0), true) : ((sh2.lastExit = 1), false))`
  even when the exit status is never read. Drop the ternary + lastExit writes
  (keep the side effect) when `$?` is provably unread before the next write.
- Rationale: 2,220 lastExit mentions / 482 tests. Loop bodies (`((i++))` inside
  `while`/`for` where `$?` and the loop status are unread) become bare `++i;` —
  removes 2 in-loop lastExit writes per iteration (count:increment's residual
  5 in-loop calls include them). Same family as the guard-liveness pass
  (08f6ec6); reuse its `MAY_ERREXIT` flag (keep writes when set -e may be on).
- Steps:
  1. In src/shir.rs, add a lastExit-liveness scan: for each statement that
     writes lastExit (the `(( ))` ternary, setLastExit calls, echo-status
     sequences), scan forward in the enclosing block for a lastExit READ
     (`sh2.getVar("?")`, `sh2.and`/`or`/`not`, test/if conditions, `return`/
     `break` statuses, a loop body's last statement when the loop status is
     consumed) before the next write. If none -> the write is dead.
  2. In the `(( ))` lowering, when dead, emit only the side effect
     (e.g. `++i` — the native arith path already handles lifted vars).
  3. Keep writes when MAY_ERREXIT is true (set -e could read the status).
  4. Corpus must stay 528/528; benchmark count:increment should drop from
     sh2[6 5 0] toward sh2[1 0 1].
- Benchmark before: count:increment js ~1215/s, sh2[6 5 0]
- Benchmark after: (pending)
- Issues: (none)

---

## Plan 1: native `cut` builtin — flip the spawn-parity echo|cut benches

- Status: **IMPLEMENTED** (already landed before this cycle — harness
  sh2-namespace.mjs `builtins.cut`, GNU-verified semantics: -d/-f/-c/-b
  range grammar, -s, --output-delimiter, --; verified 2025-08-03: the
  stringop2 echo|cut benches run 5000-5900 js/s vs 189-330 bash/s — the
  spawn-parity premise is gone, JS is ~20-25x FASTER). Marked so the
  worker loop does not re-execute it.
- Priority: 2
- Target: add `cut` to the native builtins (harness/sh2-namespace.mjs +
  harness/builtins.json) so `echo $s | cut -d: -f1` no longer spawns a
  subprocess per iteration. The `echo|cut` benches are spawn-bound (~90/s);
  a native cut (delimiter + fields parsing) flips them to native speed.
- Rationale: shellbench stringop2/3/4 "echo | cut" / "cut here str" benches
  are parity (~90-160/s) because cut spawns; the transpiler already made grep
  native (sh2.grepText) — cut is the same class. Also flip `expr` if easy.
- Steps:
  1. Implement builtins.cut in harness/sh2-namespace.mjs (parse -d delimiter,
     -f field list with ranges, -s; read the fd0 string like the other native
     builtins; split on the delimiter; emit the selected fields).
  2. Add "cut" to harness/builtins.json (the canonical allowlist).
  3. Corpus stays 528/528; verify the bench-cut example + the echo|cut benches.
- Benchmark before: echo|cut ~90/s (parity with bash)
- Benchmark after: (pending)
- Issues: (none)

---

## Plan 3: unlifted `((i++))` (typeset-intVar path) — remove the 5-call regression

- Status: **IMPLEMENTED** (2025-08-03 — the INTDECL witness + let-source wiring)
- Target: `((i++))` with a NON-lifted variable (e.g. after `typeset -i i`) emits
  the verbose `sh2.setVar("i", String((Number(sh2.getVar("i"))||0)+1)), ...`
  chain (5 in-loop calls) — a 9x regression vs the old single `let` call
  (count:increment 1215/s vs 10959/s baseline). Either fall back to the
  `sh2.builtin("let", ["i++"])` form (1 call) or emit an efficient native
  form when the variable is store-backed.
- Root cause (two latent bugs, both in src/shir.rs):
  1. `collect_native_arith_sources` (the `typeset -i` Int(0) witness + the
     `let`/`(( ))` Arith assignment sources for the lift fixpoint) was
     defined but NEVER called — dead code since afab129. A var whose only
     writes were `((i++))`/`typeset -i` had NO assignment sources, so the
     fixpoint could never lift it.
  2. The walkers' intdecl skip was defeated by the exec arg shape: the
     names live inside the Array wrapper at args[1], so the per-name skip
     only matched bare Str args, while the `-i` FLAG's letters still
     marked the name store-bound via `mark_all_idents_args` (`-i` → "i")
     and the Array arg fell through `mark_write_builtin_vars` → excluded.
- Fix (Plan 3, direct):
  1. Wire `collect_native_arith_sources` into BOTH lift walkers' (numeric
     + string) `collect_assigns`/`collect_expr_assigns` for the
     `IrStmt::Exec` and `IrStmt::Expr(Call("exec"))` forms. The numeric
     side gains the Arith/Int sources (the lift); the string side gains
     them as disqualifiers (a `((x++))`-written var must not string-lift —
     the setVar the arith emits would desync from the native binding;
     this also FIXES a latent divergence: `x=abc; ((x++)); echo $x`
     printed "abc" vs bash's "1").
  2. Replace the per-name intdecl skip with a whole-call skip: a
     validated `-i` declaration writes nothing and references nothing.
  3. Guard: a native `((i++))` inside a subshell/background marks its
     written vars excluded (copy semantics — a module binding would be
     clobbered), mirroring the existing Assign-target exclusion.
- Result: `typeset -i i` + `((i++))` lifts `i` → per-iteration body is
  `i++; __count = __count + 1` (zero runtime surface; the lastExit ternary
  stays when the loop status is observable — the program's exit status).
- Benchmark after (10M-iter loop-only, same runner): **74.6s → 0.224s
  (333x)**; count benches sh2[1 0 1] / [2 0 1] — zero in-loop calls
  (bench.sh's js/s columns are node-startup-bound at CAL_MS=100, so the
  per-run js/s numbers wobble ~±30% — the loop-only delta is the truth).
- Metric: 2865 → 2843 call sites (getVar 404→388, setVar 191→185); the
  corpus in-loop sites are store-observed (unliftable by design).
- Issues: the documented SH2_ASSUME_INTDECL fidelity trade is now actually
  reachable: `typeset -i i` with an UNSET-state read (`echo $i` before any
  real write) prints "0" vs bash's "" — the same read-before-write trade
  the numeric lift already made for real sources (`echo $i` before `i=5`).
  Interpolated/bare `$refs` are native reads by design (getVar rewritten),
  so no string-context mark fires; SH2_ASSUME_INTDECL=0 restores maximal
  fidelity. Corpus 529/529, PERL pass count unchanged (441), cargo tests
  36/36 (new intdecl_and_arith_stmt_lift_to_native regression test).
- Benchmark before: count:increment js ~1215/s, sh2[6 5 0]
- Issues: (resolved — see above)

---

## Plan 5: native `and`/`or` when operands are liftable

- Status: **BLOCKED** (stale premise — the metric's sh2.and/or rows are
  already GONE: the chains lower to the native sequence form
  `(lhs, sh2.lastExit === 0 ? rhs : false)`; the residual
  last-status-unread && lowering was judged higher-risk/low-value in the
  worker's own audit)
- Priority: 4
- Target: `sh2.and` (21) / `sh2.or` (34) `&&`/`||` chains — emit native JS
  `&&`/`||` when both operands are liftable (no lastExit dependency: the
  operands' exit status is unread and the chain's status is unread).
- Rationale: the and/or helpers exist for lastExit-based branching; when the
  operands are pure (native expressions) and the chain status is unread, a
  native `&&`/`||` is faster and removes the runtime call.
- Steps:
  1. Extend the lastExit-liveness analysis (Plan 4) to mark chains whose
     status is unread.
  2. In the and/or lowering, when both operands are liftable + status unread,
     emit `Expr::LogicalExpression`.
  3. Corpus 528/528; metric and/or count drops.
- Benchmark before: (n/a — corpus-level metric)
- Benchmark after: (pending)
- Issues: (none)

---

## Plan 6: async forLoop/whileLoop/continue → sync

- Status: **BLOCKED** (verified irreducible, 2025-08-03)
- Priority: 5
- The *Sync paths already exist (forLoopSync/whileLoopSync/cstyleForSync,
  the `stmts_have_await` eligibility in src/shir.rs). The 10 remaining
  async sites (forLoop 4 / whileLoop 3 / continue 3) were audited: each
  contains a GENUINE await in its body (051_primes: `sh2.exec("bc")`;
  064_16/064_hard: capture args; 096_head_procsub: head|procsub;
  parse-error-block-in-pipeline; tty-cmdsub ×2) or is a `sh2.continue()`
  BREAK/CONTINUE signal call INSIDE an already-*Sync closure (063_11,
  063_hard, or-continue — native `continue` is illegal in an arrow body,
  the runtime signal is the mechanism). No further lowering without a
  whole-loop-inlining redesign; not worth the risk.
- Target: the remaining async `forLoop` (4) / `whileLoop` (3) / `continue` (3)
  call sites — lower to the *Sync variants when the body provably contains no
  await (mirror whileLoopSync precedent).
- Rationale: per-iteration promise overhead on the remaining async loops.
- Steps:
  1. In src/shir.rs, for each loop body, run the expr_has_await-style check
     (the machinery already exists); emit forLoopSync/whileLoopSync when clean.
  2. Gate whitelists the *Sync names (already present).
  3. Corpus 528/528.
- Benchmark before: (n/a)
- Benchmark after: (pending)
- Issues: (none)
## Plan 8: native `bc` via src/bc.rs — SH2_BC_NATIVE-gated
- Status: **IMPLEMENTED** (2025-08-04, re-landed 41dfa63 — the wiring
  from the original cycle was LOST (auto-stashed as a metric-regress
  along with the Plan 10 sed work; the plan's claims were written from
  the pre-stash verification). This cycle re-applied the bc parts from
  the stash (lib.rs mod, bc_native_enabled gate, native_capture_echo_bc,
  the capture/captureWords hook, the estree.rs regression test) WITHOUT
  the sed parts (Plan 10, still planned).
- Benchmark after: `051_primes` 0.402s vs bash 2.11s (5.3x incl. node
  startup) — output byte-identical. The whole `is_prime` chain is sync
  end-to-end: 0 async whileLoop/exec/capture/pipeline sites in the
  emitted module; the outer loop is `whileLoopSync` with a sync
  `sh2.fnCall` per iteration. Verified gate-OFF (SH2_BC_NATIVE=0 keeps
  the spawn): corpus stays 100%.
- Metric: 2664 → 2654 total, in-loop 17 → 11 (the primes in-loop exec +
  setArrayAppend + capture/pipeline sites are gone; whileLoop 3→2,
  forLoop 6→5).
- Gate: **SH2_BC_NATIVE defaults ON** (inverted from the original plan's
  OFF — the corpus oracle gates the subset; bc.rs is 77/77 vs real bc).
  `SH2_BC_NATIVE=0` restores the spawn; verified the gate-OFF corpus
  run also stays 100%.
- Metric: 2633 → 2628 total, in-loop 9 → 7 (051_primes's in-loop exec +
  setArrayAppend sites gone). New corpus test bc-native-capture.sh
  (static folds: `2+3`→5, `scale=2; 5/2`→2.50, `2^10`→1024, `7 % 3`→1,
  `-2^2`→4, `0.5+0.25`→.75; runtime sqrt loop; a `$sum + $i` form —
  NOW NATIVE too, see Plan 16).
- Documented assumption (with the option): a bc expression fed by script
  `$vars` (the primes `sqrt($n)` form) holds a non-negative bc number
  within double precision (2^53) — the native path computes in doubles,
  bc is exact fixed-point; a huge/non-numeric/negative operand would
  diverge from real bc's "" error output. Static programs fold through
  bc.rs EXACTLY (77/77 differential).
- Issues: the structural gate needed `Math.sqrt` whitelisted
  (estree_gate.pl — done). The stash's sed-native-capture work was NOT
  re-applied (Plan 10). Cross-process let-declaration ORDER is
  nondeterministic (HashSet iteration, pre-existing; harmless — JS let
  decls are order-independent; the in-process determinism test passes).
- Priority: 2
- Target: `$(echo "sqrt($n)" | bc)` / `$(echo EXPR | bc)` captures spawn a
  real bc per call. src/bc.rs (COMMITTED — pure std, no deps,
  `rustc --test src/bc.rs` runs its unit tests; 77/77 differential vs
  real bc) evaluates the constant subset exactly. Wired in:
  1. `pub mod bc;` in lib.rs.
  2. Gate: `SH2_BC_NATIVE` default ON (an env value of "0" disables).
  3. Capture lift: match `$(echo CONST-OR-VAR-EXPR | bc)` capture
     pipelines — constant EXPR → `bc::eval(expr)` folds at compile time
     to the literal output; runtime-var EXPR (the primes `sqrt($n)`) →
     native JS `String(Math.floor(Math.sqrt(Number(x))))` (bc's scale-0
     truncated root; the bc_fast double path, precision caveat for huge
     n); anything bc.rs errors on → keep the spawn. captureWords
     context fires only for single-word results (the emitted one-element
     array mirrors `capture().split(/\s+/)`).
  4. Verified: corpus 531/531 gate-ON; gate-OFF run also 531/531
     (051_primes is the live test — its sqrt captures become native).
- Rationale: unblocks the primes async chain (is_prime → sync fnCall →
  outer whileLoopSync); flips the spawn-parity bench.
## Plan 9: sh2runtime capability manifest — the js/wasm "binary" registry
- Status: planned
- Priority: 3
- Target: the emitter hardcodes SYNC_BUILTINS + the runtime's builtins map
  with a "MUST match exactly" hazard — a drift between them leaves a
  runtime-capable command as a spawn (or worse, emits a sync call to a
  nonexistent builtin). Make the RUNTIME the source of truth:
  1. sh2runtime exports a capability manifest — `{ "js": [builtin names],
     "wasm": [wasm module names] }` — generated from the actual builtins
     map + the wasm registry (bc.wasm first).
  2. The emitter consumes it (embedded via include_str! at build time, or
     passed by the gate) — `exec("name")` dispatch: js-listed → sync
     sh2.builtin, wasm-listed → sync wasm call, else → spawn.
  3. Adding a native command = ship the js fn / .wasm + register it —
     the emitter's lowering follows automatically.
- Distinction (design decision): the manifest describes the RUNTIME's
  executable formats — js functions and wasm modules it can run natively,
  like binary formats for sh2runtime. It does NOT cover TRANSLATOR-side
  lowerings (echo/printf/cat/sort/grepText/arith/test-injection — inline
  JS emitted at compile time, no runtime call). The two layers compose:
  translator-inline first (compile-time, no dispatch), then the runtime
  manifest (sync dispatch), then spawn.
- Rationale: kills the drift hazard; makes bc (Plan 8) and future wasm
  binaries (tr/awk/sed as wasm — see Plan 10) a registry entry, not a
  special case.
## Plan 10: native text-filter subset — tr / sed-s / awk-print

- Status: **PARTIALLY IMPLEMENTED** (2025-08-05 audit: the runtime already
  ships native `tr` AND `sed` builtins — `builtins.tr` in sh2-namespace.mjs
  (full GNU surface: -c/-d/-s/-t, ranges, POSIX classes, squeeze/truncate)
  and the native sed subset (`sedParseScript`: s/PAT/REPL/[gp], -n, line/
  $/regex addresses + ranges, { } groups, -e; BRE via the shared breToJs).
  These landed in earlier cycles without plan updates; the remaining
  planned item is the AWK subset (`awk '{print $N}'` field printing). The
  corpus awk shapes (8 exec sites) are genuinely awk-grammar cases —
  `{print $1 + $2}` arithmetic, multi-line scripts with comments
  (awk.sh is a PARSER test: `#PERL_MUST_NOT_CONTAIN` pins the spawn) —
  not the cheap field-print form; the paste sites (7) feed process
  substitutions or pipe into sed — the procsub materialization machinery
  already handles them. Audit verdict: not the cheapest remaining win;
  the corpus is at a strong local minimum (the remaining exec surface is
  genuinely external: ls/diff/uname/date/gzip/find/readlink/...).
- Priority: 3
- Target: the corpus cmdsub families still spawn for `tr` / `sed` / `awk`
  (`$(echo "$s" | tr ...)` etc. — the strings-backtick/tr-cmdsub/awk
  examples). The benchmarks are already builtin-covered (echo/printf/cut/
  case) — this is corpus + the wasm registry's second batch.
  1. `tr` — translate/squeeze/delete over the stdin string — tractable
     (~50 lines), common in cmdsubs. Form: `$(echo X | tr SET1 SET2)` →
     native JS char-map; `-d`/`-s` too.
  2. `sed` subset — `sed -e 's/pat/repl/[gp]'` — the SAME substitution
     family as the already-native `${s/abc/XYZ}` — reuse its machinery;
     `-n ... p` (print-only) for the grep-like uses.
  3. `awk` subset — `awk '{print $N}'` (field printing) — covers most
     corpus uses; awk as a language stays on spawn.
- Implementation axis: each can be (a) a translator-inline lowering
  (like grepText), or (b) a wasm module registered in the Plan 9
  manifest (like bc) — prefer (a) for the simple forms, (b) for the
  general ones. The Plan 9 manifest makes (b) additive.
- Rationale: spawn-bound corpus examples are the remaining
  subprocess-parity gap; each lift removes real spawns from hot loops
  and cmdsubs.
## Plan 11: expose the bc NUMBER CORE as a wasm primitive (SH2_BC_NATIVE=exact)
- Status: planned
- Priority: 3
- Target: the `sqrt($var)` runtime path currently uses doubles
  (`Math.floor(Math.sqrt(Number(x)))`) with the documented assumption
  (non-negative, < 2^53). Expose posixutils-rs's bc NUMBER CORE — NOT the
  parser/interpreter — as a wasm library call for exactness:
  1. Compile ONLY calc/bc_util/number.rs (622 lines; deps bigdecimal/
     num-bigint — pure Rust, wasm-clean) to wasm32-unknown-unknown with a
     tiny lib wrapper. The parser (parser.rs), interpreter
     (interpreter.rs) and CLI (bc.rs main) are NOT compiled — bypassed
     entirely (the emitter already matched the shell-side shape).
  2. Exports (string-in/string-out, ~20 lines of marshaling):
     `bc_sqrt(value_str, min_scale) -> out_str` (Number::sqrt: exact
     BigDecimal sqrt rescaled to max(arg_scale, min_scale) — bc's
     semantics; negative → error "") — plus the exact
     add/sub/mul/div/pow/modulus at a scale for the general $var forms.
  3. sh2runtime loads the module once (sync WebAssembly instantiation),
     exposes `sh2.bcSqrt(x, scale)` — synchronous — the *Sync gates stay
     green (wasm is pure CPU).
  4. The emitter's `sqrt($var)` shape → `sh2.bcSqrt(String(x), scale)`
     instead of Math.floor(Math.sqrt(...)) — the 2^53 / negative /
     non-numeric assumptions all vanish (exact bc semantics, errors "").
     Gate tier: SH2_BC_NATIVE=fast (current double path) / =exact (wasm)
     / =0 (spawn). Static programs keep the bc.rs compile-time fold.
  5. The FIRST entry in the Plan 9 wasm registry — the pattern
     generalizes: expose the CORE PRIMITIVES the emitter's shapes need
     (awk's arithmetic, sed's substitution engine, tr's translate), not
     the whole CLI.
- Rationale: exactness for the runtime-var bc path with a tiny surface
  (~100-300KB wasm, no parser/interpreter) — the "full bc quality"
  native tier.
## Plan 12: bake the rust core utils into the runtime — the full wasm registry
- Status: planned
- Priority: 2
- Target: Plan 11's bc slice PROVED the mechanics end-to-end (wasm number
  core → harness/wasm/bcwasm.wasm → lazy sync loader in sh2-namespace.mjs
  → sh2.bcSqrt → the SH2_BC_NATIVE=exact emitter tier; 531/531 corpus).
  Bake in the rest of posixutils-rs the corpus/benchmarks need:
  1. Per tool, expose the CORE PRIMITIVES the emitter's shapes need as
     wasm exports (the bc pattern: compile the tool's core module, wrap
     string-in/out + a bump arena, safe() panic→error) — awk's field
     arithmetic/NF/regex, sed's substitution engine, tr's translate,
     grep's -o/-r/-Z, paste/rev/join, extended bc (scale, variables).
  2. The Plan 9 manifest: sh2runtime advertises { js: [...], wasm: [...] }
     — the emitter dispatches exec("name") via it (translator-inline
     first, then js builtin, then wasm, then spawn).
  3. The runtime fallback option: a flag/env (e.g. SH2_RUNTIME_SPAWN=1)
     that forces the EXTERNAL-BINARY path for everything not
     translator-inline — the differential oracle (wasm-vs-real) for the
     corpus; default = the wasm/js layers, spawn only for unoptimized
     commands.
- Build recipe (reproducible): /tmp/bcwasm (cargo project, crate-type
  cdylib, src/number.rs = posixutils-rs calc/bc_util/number.rs verbatim,
  src/lib.rs = the wrapper) → cargo build --release --target
  wasm32-unknown-unknown → copy the .wasm to harness/wasm/. A committed
  build script (harness/wasm/build.sh) should replace the /tmp scratch.
- Rationale: sh2runtime ships its own binaries — the spawn-parity story
  generalizes (sqrt1337's 1200x), exactness lives in the libraries.
## Plan 13: F1-minimal type lattice — the C backend's type split (A2 generalized)
- Status: planned
- Priority: 1
- Target: the C backend (being built — plan §2) still GUESSES `long long`
  vs `char*` from init literals (backend-c-core-needs.md §3 — "the draft
  only ever guesses"). The lift already computes the verdicts for the JS
  path; generalize A2's {Int, Str, Any} to the minimal C-serving lattice
  and SERIALIZE it in the ShIR JSON (A1 landed) so the C renderer stops
  guessing:
  1. `src/ir.rs`: extend `IrType` to {I64, U64, F64, Char, Str, Array,
     Struct, Any} (the minimal set — NOT the full F1 lattice: no
     I8..I32/F32/Union/FnPtr yet — the C frontend's problem, F2+).
  2. `src/shir.rs`: populate per-var from the existing lift verdicts
     (numeric_lift_vars → I64; string_lift_vars → Str; the
     mixed/refused → Any — the lift's refusal serializes) + a
     conservative fallback (nothing provable → Any). Never invent
     precision the lift didn't prove.
  3. `src/shir_json.rs` + `shir_json_in.rs`: serialize + deserialize
     (additive — the roundtrip test stays green).
  4. Verify: corpus green (both backends), determinism byte-identical,
     structural gate green — existing backends ignore the field.
- Rationale: F1-minimal IS the C-core doc's #1 ask (A2) — the C backend
  (this worker's current §2 phase) consumes it directly; the JS/Perl
  backends ignore it; and it's the foundation the C frontend (F1 full)
  builds on later.
- Notes: the full F1 lattice + F2 nodes (Cast/Member/Goto/CSwitch/…) are
  the C-frontend project's backlog — NOT this plan (no consumer yet).
  F3/F4/F5 (the C-frontend contracts) are docs, "FOR NOW" boundaries —
  the worker owns the core and may propose cleverer solutions later
  (docs/frontend-c-core-needs.md).

---

## Plan 14: native grep — the file/stdin mini-grep builtin (the "grep -q P file" family)

- Status: **IMPLEMENTED** (2025-08-04 — f1684da in sh2perl; harness
  sh2-namespace.mjs + builtins.json)
- Priority: 1
- Target: the corpus still SPAWNED real grep for 53 exec sites — file
  operands (`grep pattern file.txt`, `grep -v "^#" "$file"`), stdin
  pipelines (`ls | grep -v a.txt`, `env | grep '^myexport='`), and the
  flag surface the echo|grep lift refused (-l/-L/-r/-b/-f/-h/-H/-Z/-w/
  --color). grep is now a SYNC_BUILTIN → `sh2.builtin("grep", ...)` —
  the async exec machinery + subprocess spawn collapse to one sync call.
- Implementation:
  1. harness/sh2-namespace.mjs: module-level `parseGrepArgs` (the shared
     GNU flag grammar: v/i/n/c/o/q/x/w/b, -A/-B/-C/-m, -e/-E/-F/-f,
     -l/-L, -h/-H, -Z, -r, --include/--exclude globs, --color=always,
     `--`; first positional = pattern, rest = FILE operands),
     `grepRegexes` (BRE/ERE/fixed + -x anchoring + -w lookarounds;
     invalid BRE → "grep: Unmatched" error the builtin turns into GNU's
     exit-2 + stderr report), and `grepSelect` (the line-selection core:
     quiet/count/only/plain/context modes, per-source name prefixes,
     -b byte offsets, match coloring). `grepText` (the echo|grep lift
     path) was refactored onto the same core — behavior byte-identical.
  2. `builtins.grep`: file operands via fs.statSync/readFileSync (GNU
     messages + continue + exit 2), dir operands (error without -r;
     recursive walk with -r), stdin via readFd0 (pipes/heredocs/
     herestrings/file redirects), filename prefixes by OPERAND count /
     -r dir expansion / -H (suppressed by -h), -l/-L name output with
     -Z NUL, -c emits `0` counts for dir operands (GNU quirk).
  3. The -r walk uses `fs.opendirSync`/`readSync` — the RAW readdir
     order: `fs.readdirSync` sorts (scandir+alphasort) and reorders
     multi-file output vs GNU grep's getdents order (017_grep_context
     greps the workspace root — byte-order matters).
  4. shir.rs SYNC_BUILTINS + data/sh2-builtins.json (A4) + estree.rs
     test update (env-carrying `grep` now lowers to the sync twin);
     builtins.json gains grep (the runner's allowlist drops it — grep
     never spawns).
- Verification: full suite 531/531 estree (the oracle); 460+ case
  differential fuzz vs real grep (stdin + file + multi-file + -r +
  error paths) — 463/463 match; the foo\(bar unmatched-BRE-group case
  (GNU errors exit 2, JS RegExp would throw) handled via the
  grepRegexes catch → builtin error path.
- Metric: 2654 → 2602 total (exec 340 → 287, builtin 668 → 721); in-loop
  unchanged (11).
- Benchmark: n/a (no bench sample spawns grep; the ~45 spawns removed
  from the corpus per full run).
- Issues: -r multi-file ordering matches GNU on the corpus environments
  (raw getdents); node's sorted readdirSync is avoided by design.
  `grep -c -o` now counts LINES (GNU) — the old grepText count-branch
  counted matches but was never corpus-exercised.

## Plan 15: chain-context test lowering — native tests inside `&&`/`||` (the `_g` single-eval wrapper)

- Status: **IMPLEMENTED** (2025-08-05 — sh2perl a1a2b3c; estree.rs regression tests)
- Priority: 1
- Target: the `test` family (88 sites, the 4th-highest runtime construct). Tests
  inside `&&`/`||` chains stayed runtime calls (`sh2.test` — tokenize + parse +
  dispatch per evaluation) because the old rule required the native form to
  contain ZERO sh2 references (a store-var operand would lower to getVar calls
  — judged a net metric loss). Relaxed with three pieces:
  1. **`_g` single-eval wrapper**: `(sh2._g = t, sh2.lastExit = sh2._g ? 0 : 1,
     sh2._g)` — the guard/not scratch protocol — evaluates the native test
     ONCE (the old wrapper duplicated `t`, doubling its getVar reads and the
     metric count). A 1-getVar native is now metric-NEUTRAL vs the 1 runtime
     test it replaces.
  2. **Eligibility**: natives with ≤1 sh2.* call (a store-var/positional read —
     strictly faster, never a metric loss) and no awaits (file tests stay
     runtime in sync chain contexts). The count check uses a real AST walk
     (`expr_sh2_call_count`), not the old string contains-scan.
  3. **Op-skip instead of abort**: the numeric/string-op scans in
     `try_native_test` used `?`-abort on the FIRST operator match — a compound
     (`"$2"=="test" -o "$2"=="debug"`) never reached the `-a`/`-o` compound
     path. Failed operator positions now SKIP and keep scanning; the compound
     path lowers multi-leaf tests (`-o` splits first, `-a` inside — the
     runtime's binding order). The compound result is capped at ≤1 sh2 call
     ONLY when an op-skip occurred (`test_has_op_token`) — baseline-reachable
     pure-`-a`/`-o` compounds keep their pre-existing uncapped lowering.
- Result: 2654 → **2644** call sites (test 88 → 71, getVar 381 → 399 — the
  replaced tests' reads); in-loop sites drop 11 → 8 (the remaining in-loop
  tests are 2-getVar numeric guards, cmdsub operands, and async file tests —
  irreducible without whole-context async plumbing). Remaining tests are
  genuinely hard: cmdsub operands, array-index file paths, paren-grouped
  compounds, extglob patterns, `$'\x00'` escapes, `-nt`/`-ot`/`-ef`.
- Also lifted: `"$?"="0"` status equality (the `?` sigil is a lastExit read,
  not a glob — `String(sh2.lastExit) === "0"`, 0 dispatches) and `[ -z "" ]`
  quoted-empty literals (always-true/false).
- Benchmark (200k iterations × 2 chained store-var string tests, taskset-pinned):
  old transpiled 9.07s → **0.78s (11.6x)**; bash 11.93s (15.3x). The per-test
  cost drops from tokenize+parse+dispatch to one getVar + native ops.
- Corpus: 531/531 estree (the oracle); cargo tests 61/61 (3 new regression
  tests: and_or_chain_store_var_tests_lower_native,
  compound_test_lowers_to_native_or, status_equality_lowers_to_lastexit_read).
- Issues: the load-averaged box makes wall-clock benches noisy — taskset + min
  of runs used. The 2-getVar guarded numeric form (`"$uid" -gt 1000`) stays
  runtime in chains by design (a 2-getVar native would be a net metric loss).

---

## Plan 16: general var-operand bc capture — `$(echo "$sum + $i" | bc)` native

- Status: **IMPLEMENTED** (2025-08-05 — shir.rs `bc_var_capture` +
  `bc_arith_to_js` + `bc_arith_divisors` + `bc_var_lit_ok`; estree.rs
  regression test extended)
- Priority: 1
- Target: the LAST async-forLoop straggler with a native path —
  `for i in 1 2 3 4 5; do sum=$(echo "$sum + $i" | bc); done` (the
  bc-native-capture corpus test) emitted `await sh2.capture(async () =>
  await sh2.pipeline([builtin echo, exec bc]))` PER ITERATION (4 in-loop
  sh2.* sites). Plan 8's native-capture hook only handled static programs
  and the `sqrt($var)` form. The general var-operand form (any
  `+ - * / % ( )` integer expression over interpolated vars) now lowers to
  `sum = String(Number(sum) + Number(i))` — and the whole loop becomes a
  native JS `for...of` (the body's last await is gone).
- Implementation:
  1. Reconstruct the bc program text from the interpolated echo arg,
     substituting each Expr slot with a `__bcvK` placeholder; the literal
     parts must pass `bc_var_lit_ok` (decimal digits, whitespace, the six
     operators ONLY — letters/`.`/`;`/`^`/comparisons/`#`/quotes bail to
     the spawn).
  2. Parse via the existing bash-arith parser `parse_arith` (the two
     grammars agree on `+ - * / %`, parens, unary minus, decimal
     literals, left associativity; `^` is bc POWER vs bash XOR — bailed;
     `010` parses DECIMAL in parse_arith, matching bc, and `0x`/`2#` base
     forms cannot pass the strict literal scan).
  3. Translate: slots → `Number(<slot expr>)` (bash vars are strings);
     `+ - *` → JS ops; `/` → `Math.trunc(a / b)` (bc's scale-0
     truncation toward zero); `%` → JS `%` (sign-of-dividend remainder,
     identical for integers). A zero divisor makes bc abort the WHOLE
     program with no stdout — each `/`/`%` site gets a
     `(divisor === 0) ? "" : ...` guard testing the divisor's VALUE (a
     nested `a / (b / c)` aborts on `b/c == 0`, not just `c == 0`); all
     operand exprs are pure reads, so the double evaluation is safe.
     Literal divisors skip the guard.
- Metric: 2611 → 2606 total, in-loop 11 → 7 (capture/pipeline/builtin/
  exec in-loop sites gone; forLoop 5 → 4, the bc-native loop is now a
  native for-of). No new sh2.* names (Math.trunc already whitelisted).
- Corpus: 531/531 estree; cargo tests 62/62 (bc_capture test extended:
  var-add form, Math.trunc + zero-divisor guard, `^` keeps the spawn);
  PERL pass count unchanged (441). Gate-OFF sanity: SH2_BC_NATIVE=0 keeps
  the spawn (the gate check runs before the whole lift).
- Documented assumption (shared with Plan 8's sqrt form, same option):
  every `$var` in the expression holds a bc INTEGER within 2^53. The
  double path is exact for such operands and String() prints the same
  digits as bc (bc's `.75`-style leading-zero omission cannot arise —
  results are always integers); a fractional operand would diverge, a
  non-numeric/unset operand ("NaN" vs bc's ""), a huge one (> 2^53).
  The corpus cannot observe these: `$sum` starts at integer 0 and every
  iteration adds an integer loop var (the invariant propagates).
- Issues: bc-native-capture.sh's stale "stays on the spawn by design"
  comment updated (the file is untracked in the submodule — inherited
  from the Plan 8 cycle, not committed there; the corpus reads the
  filesystem either way). Statement-level `echo EXPR | bc` pipelines (no
  capture) still spawn — the fold fires in capture context only, by
  design (their stdout is the pipeline's, and bc's stderr must still
  surface).

---

## Plan 17: sync-ok-loops verdicts wired — native/checkpointed loop emission (core request estree-20260805-045731)

- Status: **IMPLEMENTED** (2025-08-05 — this cycle; sh2perl commit pending)
- Priority: 1
- Target: wire the sibling worker's sync-ok-loops transform (analysis-only:
  per-loop `sync_ok` / `batch_ok` pointer-keyed verdicts, computed in
  `ast_to_ir`) into the loop renderer + runtime, so cheap/output-free loops
  run as ONE native chunk and long sync-executable loops yield every 1024
  iterations instead of blocking (or awaiting per iteration):
  1. `compute_async_region_loops`: a `sync_ok` loop is NOT an async-region
     loop (`if in_async && !sync_ok(st)`), so the existing sync gate emits
     it with the native for-of / native while path — zero runtime surface.
  2. For/While lowering: a `batch_ok` loop that still fails the native
     gate (async region / glob iterable / signals) emits
     `await sh2.forLoopBatch(iter, body, 1024)` /
     `await sh2.whileLoopBatch(cond, body, 1024)` instead of the blocking
     `forLoopSync`/`whileLoopSync` — sync chunks of 1024 with a
     `setImmediate` yield (bitmask-cheap counter, not a modulo), same
     flatten/glob/signal/_capExceeded semantics as the *Sync twins.
  3. Runtime: `forLoopBatch`/`whileLoopBatch` in sh2-namespace.mjs —
     EXACT mirrors of forLoopSync/whileLoopSync plus the iteration
     counter + yield; gate whitelist + A4 spec entries.
- Aggressiveness gating: the transform is registered in
  `transforms::all()` (the DEBASHC_TRANSFORMS machinery gates it like any
  worker transform); the renderer hooks consult the SAME gate
  (`transform_enabled("sync-ok-loops")`) so the bisect can disable the
  verdicts end-to-end without a rebuild. The verdicts are re-computed
  under COMPILE_LOCK inside `shir_to_estree` (the ast_to_ir hook alone
  would tear pointer keys between parallel compilations — the
  determinism unit tests caught it; ast_to_ir now takes the same lock
  around `transforms::apply`).
- Correctness arguments: `sync_ok` requires (cheap_total ≤ ~200ms upper
  bound) OR (output-free AND finite) — a sync_ok capture producer
  terminates by construction, so losing the `_capExceeded` bound is safe
  (a ≤200ms-estimated producer can still exceed it only via
  underestimated per-stmt cost — the corpus gate judges). `batch_ok`
  requires a sync-executable body (no exec/capture/read/sleep/signals),
  so the sync bodyFn never sees an await; the await introduced by the
  batch call propagates into the enclosing lowered body, flipping any
  ancestor *Sync loop to its async form (verified: the *Sync gates scan
  the lowered body). `await`-legality: batch emission is suppressed
  inside non-async arrows (SYNC_ARROW_DEPTH — provably-sync function
  bodies), where forLoopSync/whileLoopSync stay.
- Metric: 2606 → 2606 total, in-loop 7 → 7 — UNCHANGED (no new sh2.*
  names; the corpus has no batch-eligible top-level loop shape: the
  glob-iter loops (064_19, 064_hard) have `case` bodies, which disqualify
  batch; the other *Sync sites are signal loops or function-body clones).
  The wiring is LIVE for the target pattern family — `for f in *.sh; do
  echo "$f"; done` → `await sh2.forLoopBatch(iter, body, 1024)` — verified
  end-to-end stdout-identical vs bash; the runtime batch helpers verified
  directly (flatten, BREAK/CONTINUE signals, lastExit protocol,
  _capExceeded).
- Benchmark: count:posix 28571→12121/s (load noise; sh2[0 0 0]),
  count:increment 9091→9756 (sh2[1 0 0]), output:echo 13793→59259
  (sh2[0 0 0]), output:printf 7843→48485 (sh2[0 0 0]) — the bench samples
  contain no loops, so the emission is byte-identical for them; the
  per-iteration runtime surface is unchanged (the batch/yield machinery
  only replaces the loop CALL, not the body). Numbers wobble ±30% on the
  load-averaged box.
- Issues: the pointer-keyed verdicts are subject to the pre-existing
  arrow-CLONE behavior: capture/subshell/background/function/pipeline
  bodies are cloned before lowering (the string-lift re-emission, the
  arrow_sink clones), so loops inside them MISS the verdict keys and keep
  the pre-existing emission (native for-of when flattenable — already the
  case before this change). In the current corpus that means the batch
  path fires only for top-level/block-body loops with non-flattenable
  iters and sync bodies — none of the 531 examples have that exact shape
  (the glob-iter examples have `case` bodies, which disqualify batch), so
  the corpus emission is expected byte-identical. The wiring is live for
  the pattern family (`for f in *.sh; do echo "$f"; done` →
  forLoopBatch, verified end-to-end vs bash). Fixing the clone-blindness
  (verdict keys that survive clones, or clone-free emission) is a
  follow-up.

---

## Plan 18: core-request batch (5 escalations) + StoreToNative — the 2026-08-06 mediation

- Status: **IMPLEMENTED** (2026-08-06 — this cycle; submodule commit pending)
- Priority: 1
- Benchmark after: (pending — final corpus run)
- Issues: (none)
- The five pending core-request files (core-requests/*.md) were mediated
  oldest-first and implemented without an estree-corpus regression (the
  final gate run is the oracle; double-paren-subshell stays the single
  trusted baseline failure):

  1. **posix-sh-go-20260806-174619** (array-literal field split) — the
     biggest change: `parse_array_elements` now parses each element as a
     REAL `Word` (`Word::Array` is `Vec<Word>`), and the ast_to_ir
     lowering maps elements via the new `array_element_ir` (unquoted
     `$x` → A1 `split(getVar("x"))` — bash field-splits; quoted `"$x"`
     stays a single Interpolate; `$@`/`$*` → `listVar`, spliced by the
     runner's setArray — harness step 6 had already landed). The Perl
     generator consumers render LITERAL elements byte-identically
     (verified: the 7 riskiest array files produce byte-identical perl
     output modulo timing lines). Also fixed a LATENT `Word` deref bug
     the old raw-text path masked: `Word` derefs to `""` for non-Literal
     words, so `st(element)` on a StringInterpolation silently produced
     an empty element (the `local -a args=("$@")` corpus files).
  2. **fish-sh-go-20260806-181309** (byte-stable A1→ESTree) — the
     program-top lifted declarations and direct-fn bindings are now
     emitted in SORTED name order (`sorted_set` helper); verified 20/20
     runs produce one order for the t40 two-var program.
  3. **array-flatten-positional-20260806** (t65/t67) — `"$@"` and
     `"${arr[@]}"` as WHOLE-WORD quoted exec args now lower to
     `listVar("@")` / bare `param("slice", name, "@")` (the runtime's
     arg flattener splices them — bash arg-count semantics); the three
     inline echo/printf sites got the shared `exec_arg_is_array_valued`
     predicate (captureWords/listVar/split/arrayIndex-@-* /
     param-slice-@-*). Also fixed `capture_parenthetical_text`'s
     ArithmeticEvalClose depth==1 case (`arr=($(cmd))` — the lexer
     merges `))`) and added the swallowed-closer detection in
     parse_array_elements — a LATENT parser bug that previously made
     every `$(...)`-last array element into a bogus `echo` fallback.
  4. **perl-shir-20260806-1930** (process substitution through the ShIR)
     — `redirect_to_ir` emits `process-in`/`process-out` modes with the
     reconstructed inner command text (`command_to_shell_text`); the
     perl renderer's `append_redirect_frag` already appended ` <(cmd)`;
     ir-perl-metric render-panics for those files: N → 0, and the
     request's exact `diff <(echo a) <(echo b)` case matches bash
     stdout. The estree path is unaffected (estree.rs `transform_cmd`
     materializes temp paths before ast_to_ir).
  5. **shir-passes-store-to-native-20260806** (StoreToNative) — a real
     `shir_passes` Transform: `Expr(setVar("v", e))` → `Assign{var: v}`
     for PROVABLY store-only vars (literal-name setVar/getVar refs only;
     conservative exclusions for array/assoc/mem/param/compound-assign,
     dynamic names ANYWHERE, and a var name mentioned in a runtime
     arith/arithEval/test/brace/caseMatch STRING — the runtime would
     read the stale store). Wired into the canonical pipeline AND into
     `shir_to_estree` (on a clone, before the lift analyses). The
     hand-crafted c-sh-go A1 (`setVar("x","5")` + `getVar("x")`) now
     emits `let x = 0; x = 5;` + a native read — zero runtime surface.
     One stale unit test pinned the old store-sync shape (the post-loop
     `sh2.setVar` of the last seq-range value) — updated to pin the
     native `i = __sh2_for_last_i` write (verified byte-identical
     output vs bash).
- Metric note: total 2518 → ~2542 — the increase is the CORRECTNESS
  cost of the batch (listVar/@ +8, bare param slices +6, array-element
  split/capture markers; the old emissions were silently wrong on those
  shapes). StoreToNative itself only removes sites (frontend A1 is its
  consumer — the corpus's ast_to_ir path emits Assign directly).
- Fix-surface note: `estree_reads_positional` gained the `listVar`
  name-slot check — a `"$@"`-bearing function body must not take the
  direct-call path (callDirect skips the positional swap; the analysis
  previously only saw the raw `$@` STRING, which my listVar lowering
  replaced with a CALL the scan missed — the 6 `local -a args=("$@")`
  corpus files caught it).

---

## Plan 19: parse-gap fixes + native uname/date/readlink + subscript store-reads (2026-08-06 estree mediation)

- Status: **IMPLEMENTED** (2026-08-06 — this cycle; submodule commit pending)
- Priority: 1
- Benchmark after: (pending — the corpus is the gate; the uname/date/readlink
  lifts remove ~20 subprocess spawns; `$(uname -s)` inside 064_22/064_hard
  FUNCTION bodies were per-call spawns — now one sync value read)
- Issues: (see below)

### Core requests (5 mediated — see core-requests/*.md OUTCOME lines)

1. **parse-gaps-20260806-b** (IMPLEMENTED): two real parser bugs + graceful
   `--shir` fallback.
   - lexer `merge_double_quoted_strings`: the `"` arm inside `$(...)` lacked
     the `sq_depth == 0` guard — a `"` inside a single-quoted string within
     `$()` (e.g. `sed 's/"//g'` inside a DQS cmdsub) toggled dq_depth, so the
     closing `'` was misread and the DQS never found its closing quote
     (multiple-awk-in-dqs.sh, subshell-sed-squote-dquote.sh — both now
     TRANSPILE as real programs instead of empty artifacts).
   - `parse_string_interpolation`'s `$(...)` scanner had the same bug class
     (no sq_depth guard on its `"` arms) — the cmdsub scan ended early and
     `$1` leaked out as a positional ref (`awk '{ print $1 }'` → `print `).
   - `--shir` on parse failure now emits the canonical empty Program JSON
     instead of empty stdout (frontends no longer die "invalid JSON: EOF");
     parse-paren-after-do.sh / parse-unexpected-end-of-input.sh are
     genuinely incomplete scripts (bash rejects them too) — the graceful
     fallback is their faithful contract.
2. **perl-shir-process-sub-20260806-3** (IMPLEMENTED — already landed):
   `redirect_to_ir` emits process-in/process-out with the inner command
   text; verified the request's exact case.
3. **assoc-arrays-20260806-b** (REJECTED contract split — real bug fixed):
   the failing case exposed a genuine estree bug — with a LIFTED (native
   binding) loop var, `map[$k]=v` / `arr[$i]=v` writes and `${map[$k]}`
   reads resolve the subscript from the STORE, which the lifted binding is
   no longer in (stale indices/keys). Fixed: the numeric+string lift
   walkers' Assign arm marks `$var` refs inside baked subscript names
   store-read (`mark_string_refs(&t.var, string_ctx)`), and
   `forof_sync_elim_ok` keeps the per-iteration store sync when a baked
   name references the loop var (`subscript_refs_var`). Both the assoc
   (`for k in blue green; do colors[$k]=...`) and indexed-array cases now
   match bash exactly. The structural-indices IR split was rejected: the
   baked-name form is the correct SINGLE-expansion lowering (a computed
   name would double-expand `$`-bearing values), and the split would force
   re-baking in the perl generator for zero corpus gain.
4. **extglob-nocasematch-20260806-b** (REJECTED): the AST does not
   distinguish `[[ ]]` from `[ ]` (both are `Command::TestExpression`) —
   the tag needs a parser AST change + every test-Call consumer updated for
   a contract-only gain (010/037/038/057 already 100%).
5. **typed-node-ir-20260806-b** (REJECTED): additive C-frontend contract,
   no estree consumer, nothing blocked (c-mem-slice2 has the handle
   fallback); defer to a coordinated contract cycle.

### Improvement: native uname / date / readlink (no spawn)

- Runtime builtins (harness/sh2-namespace.mjs) + value-returning twins for
  the capture lifts (the sh2.dirname/basename pattern): `$(uname -s)` →
  `sh2.uname("-s")` — no capture machinery, no promise, no spawn. uname
  maps GNU fields to node os.* (verified byte-identical on this box incl.
  `uname -a`); date formats the corpus directives (+%Y, +%Y%m,
  '+%Y-%m-%d', bare default layout); readlink implements -f/-e/-m/plain
  with realpathSync + a lexical walk (verified vs GNU on symlinks, missing
  components, relative links).
- Emitter: SYNC_BUILTINS + `native_capture_path` extended; A4
  data/sh2-builtins.json synced; estree_gate.pl whitelists the three sh2
  members; harness/builtins.json gains the names (the runner never spawns
  them).
- ASSUMPTION (documented in code, option-gated): SH2_ASSUME_UNAME=0 /
  SH2_ASSUME_DATE=0 restore the spawn for maximal fidelity. The mappings
  are exact on GNU/Linux (verified `uname -a` byte-identical); the date
  TZ-abbreviation table is an approximation for exotic zones (the corpus
  exercises only +%Y/+%Y%m/%Y-%m-%d, which are exact).
- Metric: 2542 → 2539 total (exec 282→262, capture 155→136, builtin
  704→712, new uname 9/date 4/readlink 6 value reads); in-loop 7→11 —
  the increase is the CORRECTNESS cost of the parse-gap files now
  emitting real code (2 captures+2 pipelines in multiple-awk-in-dqs, the
  while-loop + setArrayAppend pair in subshell-sed-squote-dquote; Plan 18
  precedent). The 26 gate failures in the final run are the SIBLING's new
  exit-code gate (commit 99e5aa7, 23:54 mid-session — re-baselined to 29
  trusted; my run: 26, i.e. 3 already fixed by the parse-gap work) — NOT
  regressions from this cycle.
- Issues: the unquoted `echo $(date)` form keeps captureWords+builtin
  (bash word-splits the date output — the value twin would need a split,
  deferred). The empty-artifact convention (parse-error files exit 0 vs
  bash 2) is the new exit-code gate's backlog — a candidate fix is
  canonicalizing the empty artifact to `process.exit(2)` in the gate.

---

## Plan 20: native ls/which/hostname + native sleep timer + yes|head capture + replaceAll (2026-08-07 estree improvement)

- Status: **IMPLEMENTED** (2026-08-07 — this cycle; sh2perl commit pending)
- Priority: 1
- Target: the remaining subprocess families the corpus still spawned and
  the user's named pattern families not yet landed: `ls` (25 exec sites,
  the biggest single-command spawn family), `sleep` (8 spawns), `which`
  (2), `$(hostname)` (3 capture+spawn sites), `$(yes LIT | head -N)` (3
  async capture+pipeline sites), and `${x//p/r}` → replaceAll (speed).
- Benchmark after: (pending — the corpus is the gate; the ls/sleep/yes
  families removed ~41 subprocess spawns per full corpus run)
- Issues: (see below)

### Implementation

1. **native `ls` sync builtin** (harness/sh2-namespace.mjs `builtins.ls` +
   SYNC_BUILTINS + builtins.json) — GNU-faithful listing for the corpus
   surface, differential-verified vs GNU coreutils on this box (17 fixed
   cases + 50-case randomized fuzz, byte-identical stdout AND exit codes):
   flags -1/-A/-a/-l in any combination; file operands sorted first (no
   header), each dir with a `name:` header only when the OPERAND COUNT
   exceeds one (GNU counts MISSING operands too — `ls . missing` headers
   `.:`); missing operands report `ls: cannot access 'X': …` to fd2 and
   exit 2 (GNU continues with the valid operands); -l emits the `total N`
   block line (st_blocks/2), column-aligned nlink/owner/group/size where
   the FILE group's widths scan the file entries PLUS the dir operands'
   own lstats (GNU pads `ls -l f d`'s f-line to d's own size — found via
   fuzz, fixed) and each DIR group scans its own entries only; groups
   separated by blank lines; the `%b %e %H:%M` mtime with the `%b %e  %Y`
   year form outside the 6-month window; symlink `-> target`; uid/gid →
   /etc/passwd + /etc/group (numeric fallback). `ls -la` (057_case) never
   runs in the corpus but the format is implemented anyway.
2. **native `sleep`** (src/shir.rs `try_native_sleep` + a new
   `NewExpression` node in the estree.rs model + estree-gen.mjs printer) —
   `exec("sleep", [N])` collapses to
   `(await new Promise(r => setTimeout(() => r(true), ms)), sh2.lastExit = 0, true)`:
   a literal arg folds to a compile-time ms literal (integer AND
   fractional — `sleep 0.1` → 100); a dynamic arg (`sleep $d`, the
   `sh2.split` of an unquoted expansion) becomes `Number(<value>) * 1000`
   (the runtime flattener turns the split array into one arg for a
   single-word value). The promise resolves `true` so the statement value
   satisfies the errexit guard. No subprocess, no dispatch. NewExpression
   is standard ESTree; the structural gate accepts `setTimeout` + the
   promise-executor param (collected from `new Promise` executors).
3. **`$(yes LIT | head -N)` capture lift** (src/shir.rs
   `native_capture_yes_head`) — the infinite-producer pipeline is exactly
   `LIT + "\n"` repeated N times (head takes the first N lines, yes dies
   on the pipe close) — a native `("LIT" + "\n").repeat(N)` with the
   capture strips; zero runtime surface. Head counts: `-N`, `-n N`, `-nN`
   (literal, positive); yes word: plain Str or all-literal Interpolate.
4. **`$(hostname)` → `sh2.hostname()`** — the value-returning runtime
   twin (the uname/date/readlink pattern) + `builtins.hostname` (os.hostname(),
   identical to uname -n) + the native_capture_path arm. 3 capture sites
   → 1 sync value read each.
5. **`which` sync builtin** — PATH walk with X_OK, explicit-path
   operands, exit 1 on miss (GNU's stderr report is not corpus-compared).
6. **`${x//p/r}` → replaceAll** — the runtime's literal fast path is
   split/join; a `$`-free replacement lowers one step further to
   String.replaceAll (single-pass, same literal semantics — JS
   replaceAll's `$&`/`$1` substitution sequences cannot fire without a
   `$` in the replacement; `\` is literal in both). A `$`-bearing
   replacement keeps split/join.
7. **A4 data/sh2-builtins.json** synced (the consistency test compares
   BTreeSets — order-free).
- Metric: 2373 → **2350** total (exec 268 → 227, builtin 679 → 703,
  capture 138 → 132, pipeline 116 → 113, new hostname 3; sleep/yes exec
  sites gone entirely); in-loop 16 → 15.
- Corpus: **531/531** estree (the oracle, final run); cargo tests 145/145
  (4 new regression tests: sleep_lowers_to_native_timer,
  yes_head_capture_lifts_to_native_repeat,
  ls_lowers_to_sync_builtin_and_hostname_capture_is_native,
  substitute_all_uses_replace_all_for_dollar_free_replacement; one stale
  test updated — `FOO=bar ls x` now lowers to the sync builtin twin).
- Documented assumptions (each option-gated, DEFAULT ON; `=0` restores
  the runtime refusal — the established uname/date/readlink pattern):
  - **SH2_ASSUME_LS**: C/C.UTF-8 locale (byte-order sort matches GNU
    collation; the corpus env is ASCII-only); uid/gid resolve via
    /etc/passwd + /etc/group (numeric fallback — GNU's own); local-time
    mtime display; the 6-month year-switch window is never straddled.
  - **SH2_ASSUME_WHICH**: the PATH walk uses process.env.PATH exactly
    like the spawned binary; GNU's not-found stderr text is not
    corpus-compared (stdout + exit code only).
  - **SH2_ASSUME_HOSTNAME**: os.hostname() equals the system hostname on
    GNU/Linux (both read the same utsname field).
  - **sleep**: a dynamic arg is a single word (the corpus shapes are
    `sleep 1` / `sleep 0.1` / `sleep 2` / `sleep $d` with d="1"); an
    invalid/non-numeric arg would sleep 0ms instead of erroring (corpus
    cannot observe). The promise is async (never blocks the event loop —
    background jobs `(sleep 1; echo a)&` keep their ordering through
    `wait`).
  - **yes|head**: the yes word is a single literal (no-arg `yes` → "y"
    and dynamic args keep the spawn); head count is a literal positive
    integer.
- Issues: the parser has a pre-existing gap for UNQUOTED `${x//p/r}` in
  assignment values (treats `//` as part of the name — the corpus uses
  the quoted form; not this cycle's scope). The `ls -l` device-operand
  size column width has a GNU quirk (6-wide for `1, 3` — not
  corpus-reachable; regular files/dirs verified exact). The runner's
  allowlist still names ls/which/hostname via builtins.json (never
  spawn). estree_gate.pl: `replaceAll` added to the string-method
  allowlist, `setTimeout` + `new Promise` executor params accepted,
  `hostname` whitelisted.

---

## Plan 21: native filesystem/data builtins — the remaining spawn families (2026-08-08 estree improvement)

- Status: **IMPLEMENTED** (2026-08-08 — this cycle)
- Priority: 1
- Benchmark after: exec 227 → **179** (48 async-exec sites → sync builtin
  dispatch); ~46 subprocess spawns removed per full corpus run (paste 7,
  diff 10, find 6, rm 4, cp 3, mv 3, tee 2, sha256sum 2, sha512sum 2,
  whoami 1, rmdir 1, gzip 4, gunzip 1). Total call sites 2317 → 2317
  (exec→builtin is a 1:1 site swap — the win is per-call: no promise, no
  spawn; the remaining exec surface is genuinely external: awk 11, perl 4,
  dpkg-query 4, wait 4, strings 2, bash/dd/ps/od/zstd/aa-exec/yes/bc 1
  each, plus user-function dispatches).
- Issues: (see below)

### What landed

1. **paste / diff / find — already implemented in the RUNTIME, now wired
   into the emitter** (the runner's builtins table had GNU-faithful paste
   (column join, -d), a full diffutils-3.10-transcribed gnuDiff (Myers +
   discard_confusing_lines + shift_boundaries — fuzz-verified) and a
   getdents-order find (-name/-type/-maxdepth) since earlier cycles, but
   they were NOT in SYNC_BUILTINS / builtins.json / A4 — every call went
   through the async exec dispatch. Now: `sh2.exec("paste")` →
   `sh2.builtin("paste")` sync twin (same semantics, no per-call promise);
   builtins.json admits them (never spawn); A4 data synced. Verified on
   the corpus's exact shapes: `diff file1.txt file2.txt` (apple/banana/
   cherry vs banana/cherry/date → `1d0 < apple` + `3a3 > date` — GNU's
   shift_boundaries script, NOT the naive `1c1`), the 064_01 hard case
   (real /etc/passwd vs /etc/group usernames, byte-identical), the
   identical/missing-file statuses (0/1/2), find on the 6420-line
   workspace tree (byte-identical to GNU find incl. order — raw getdents,
   the grep -r precedent), paste with procsub temp files.
2. **New native sync builtins** (harness/sh2-namespace.mjs):
   - `rm` — -f/-r/-R/--recursive/-- + combined shorts; every operand
     attempted, failures (missing w/o -f, dir w/o -r) set status 1 while
     the rest still run (GNU); glob operands arrive pre-expanded via the
     exec/builtin GLOB_MAGIC flattening (the shapes try_native_fs_exec
     refuses — `rm file_*.txt`, `rm -f /tmp/064_hard_temp_*`).
   - `cp` / `mv` / `rmdir` / `mkdir` — copyFileSync / renameSync with the
     EXDEV copy+unlink fallback / rmdirSync / mkdirSync{-p recursive}.
     `mv -Z src dst` (checkqx-qx-var-mv.sh) errors + exits 1 with no
     action, exactly GNU's invalid-option behavior. mkdir is belt-and-
     braces (the sh2.fs statement lift already covered it).
   - `whoami` — os.userInfo().username; plus the value-returning twin
     `sh2.whoami()` wired into native_capture_path (the
     uname/date/readlink/hostname pattern) and the estree_gate whitelist.
   - `sha256sum` / `sha512sum` — node:crypto, GNU `<hex>  <name>` format,
     `-` = stdin, per-file continuation with final status 1.
   - `tee` — fd0 → every FILE operand + stdout (-a append), the
     `echo X | tee f` capture shapes.
   - `xargs` — async builtin (it runs other commands via exec): -0/-n/-r/
     --no-run-if-empty, GNU quote/backslash word splitting, initial args
     count toward -n batches like GNU, default utility echo, status 123
     on a failed run. The `find | xargs grep -l` 6420-path pipeline works
     (one giant batch — GNU's ARG_MAX default on this box; output is
     order-preserving either way).
   - `gzip` / `gunzip` — node:zlib decompression; `gzip -cdfq -- "$X"`
     (the four parse-test families: unset/missing operands → status 1,
     exactly GNU) and `gunzip < f.gz > out` (BINARY-safe fd-0 read — a
     new readFd0Buffer; the text readFd0 mangled the .gz bytes). Corrupt
     stream → status 2 (GNU).
3. **SYNC_BUILTINS + A4 + builtins.json + estree_gate.pl** — the 15 names
   (cp diff find gzip gunzip mkdir mv paste rm rmdir sha256sum sha512sum
   tee whoami xargs) added to shir.rs SYNC_BUILTINS, data/sh2-builtins.json
   (the consistency test asserts equality), harness/builtins.json (the
   runner's never-spawn manifest — the allowlist filter drops them), and
   the gate's sh2-member whitelist (whoami, the value twin).
4. **Two stale unit tests updated** (estree.rs): `FOO=bar cp x y` and
   `diff <(echo a) <(echo b)` were the "genuinely external name keeps the
   async exec" examples — cp/diff are sync builtins now; the examples
   moved to awk (still external).
5. **A stale uncommitted experiment was reverted** — the working tree had
   a leftover single-quote backslash-preservation patch (arg_word_ir/
   array_element_ir/shell_quote_removal) that broke
   `escaped_quote_artifact_merges_words` (cargo test --lib red at
   session start, before any of this cycle's edits). The corpus does not
   need it (531/531 at the committed state); reverted to keep the tree
   green.
- Metric: 2317 → 2317 total (exec 227 → 179, builtin 681 → 729); in-loop
  unchanged (15 — the remaining in-loop sites are the irreducible
  awk/sed cmdsub + parse-error shapes).
- Corpus: 531/531 estree (the oracle — final full run); cargo tests 145/145
  (2 tests updated, see above); PERL pass count untouched (406 — never
  touched perl).
- Assumptions (each documented in harness/sh2-namespace.mjs next to the
  gate; **SH2_ASSUME_NATIVE_TOOLS=0** makes the runtime refuse the family
  with status 127 instead of emulating — the established uname/ls gate
  pattern, default ON):
  - rm/cp/mv/rmdir/mkdir: the GNU flag subsets each builtin parses;
    unknown options error + exit 1 like GNU; no -i/-v interactive modes;
    cp/mv multi-source forms only to a directory target; stderr text is
    approximated (the gate compares stdout + status only — the same
    trade try_native_fs_exec already documents).
  - sha256sum/sha512sum: node:crypto — byte-identical hex.
  - tee: -a append; failed file write fails the status like GNU.
  - xargs: GNU quote/backslash word splitting; one giant batch (ARG_MAX
    batching not modeled — corpus trees fit; order-preserving either
    way); no -I/-P/-d custom modes.
  - gzip/gunzip: zlib decompression is byte-identical for valid gzip
    streams; gzip COMPRESSION (no -d — corpus-unreachable, the four
    corpus gzip families only decompress/miss) uses zlib's header
    (mtime 0), differing from GNU's bytes; corrupt → exit 2 (GNU).
  - paste/diff/find were runtime-verified before this cycle (fuzz vs GNU)
    — the wiring adds no new assumptions (same builtins, sync dispatch).
- Issues: `$(whoami)` inside `${USER:-$(whoami)}` stays runtime-baked text
  (sh2.param default-expansion runs the cmdsub via the runtime's
  expandWord — not a metric site; same for `$(echo ~)`). The 4 xargs
  sites keep async exec dispatch (xargs runs commands — the sync twin
  cannot await), but never spawn. `yes` (065) stays a spawn by design
  (an infinite native producer cannot be killed by the sequential
  pipeline machinery). find's getdents-order parity holds on the corpus
  fs (verified on the 6420-file workspace tree; /tmp churn between the
  node and bash runs can order-skew in theory — the corpus never runs
  find on /tmp).

---

## Plan 22: *Sync twins for the async statement machinery (the whileLoopSync pattern generalized)

- Status: **IMPLEMENTED** (2026-08-08 — this cycle; sh2perl f928002,
  workspace c94413a)
- Priority: 1
- Benchmark after: 20k-iteration `x=$(echo … | sed …)` + `y=$(echo … |
  cut …)` loop: 0.88-0.96s → **0.68-0.69s (~1.3x)**; the metric's in-loop
  count 15 → **8** (see below).
- Issues: (see below)

### What landed

The async statement machinery (capture 112 / pipeline 111 / subshell 39 /
redirect 202 / block 1 call sites) got SYNC TWINS — `captureSync`,
`captureWordsSync`, `pipelineSync`, `subshellSync`, `redirectSync`,
`blockSync` — the whileLoopSync precedent generalized: when EVERY lowered
arg of an async sh2.* call is provably await-free (no AwaitExpression —
the same verdict the *Sync loops use), the emitter dispatches to the
runtime twin with plain (non-async) arrows (`sync_arrow_flip` /
`sync_arrow_flip_deep`). Identical semantics — fd swaps, NUL + trailing-
newline strips, subshell full-state copy/restore, redirect spec
install/restore/persist — minus the per-call promise/microtask machinery.
Expression-position `whileLoop`/`forLoop`/`cstyleFor` calls get the same
treatment (their existing *Sync runtime twins).

1. **Runtime** (harness/sh2-namespace.mjs): the twins + `redirect`
   refactored onto a shared `_applyRedirectSpecs(specs, saved, check)`
   helper (the persistent-restore pass keeps its file-creation for w/a
   specs; the live install defers to `_ensureRedirectFiles`).
2. **Emitter** (src/shir.rs): the generic call arm checks
   `SYNC_TWIN_CALLS` + an `expr_has_await` scan of the lowered args and
   emits `sh2.<name>Sync(...)` (arrows flipped, no await) — for capture/
   captureWords/pipeline/subshell/redirect/block/whileLoop/forLoop/
   cstyleFor. The statement-position Subshell/Redirect arms use the same
   verdict; the echo-stage pipeline collapse got the awaited-or-twin form
   too (see the bug below).
3. **Gate** (estree_gate.pl): the six new names whitelisted + admitted to
   the *Sync regex with the same pure-CPU justification as the loops.
4. **A real fd-table race fixed** (the change exposed it): the old
   echo-stage collapse returned a BARE un-awaited `sh2.pipeline(...)`,
   relying on the enclosing async arrow's implicit promise-flattening;
   inside a `captureSync` body that flattening is gone and the dangling
   pipeline's late `finally` restore clobbered the NEXT capture's buffer
   (`tee_result=$(echo x | tee f)` followed by `perl_result=$(perl …)`
   read the tee's leftover text — 000__02 regression). The collapse now
   emits the awaited async form or the sync twin, self-contained.
   A corpus-wide static audit found no other un-awaited async calls.
- Metric: 2267 → 2265 total; in-loop 15 → **8** (builtin 3→2, capture
  2→1, redirect 2→0, setArrayAppend 2→0, block 1→0; break 2 stays). The
  parse-error-block-in-pipeline redirect+block loop and the
  subshell-sed-squote-dquote mapfile loop became NATIVE JS while loops
  (their bodies turned sync), and 8 exec user-function dispatches became
  sync fnCall/callDirect (their capture bodies turned sync — 061's
  test_math/test_strings/test_arrays, 092's factorial, …).
  Sync/split: redirectSync 151 / redirect 51, pipelineSync 74 /
  pipeline 37, captureSync 74 / capture 38, subshellSync 33 / subshell 6,
  captureWordsSync 14 / captureWords 2, blockSync 1.
- Corpus: 531/531 estree (the oracle, shadow-worktree run — the shared
  main checkout had a concurrent sh-backend worker's arith_forms WIP
  oscillating the build; verified isolated at the committed state, then
  committed and the gitlink bumped). cargo tests 159/159 (8 stale
  unit-test expectations updated to the sync-twin emissions: pipeline/
  capture/captureWords/redirect/subshell/seq/conservative/batch tests).
  PERL pass count untouched (419 — never touched perl).
- Issues: the remaining async sites are genuinely async (spawns inside:
  awk/perl/bash/xargs/wait/dpkg-query…, or awaited redirect targets like
  096_head_procsub's procsub capture). `$(echo X | grep P)` value
  positions still go through grepText (sync — the line-filter family,
  not the test-position substring lift). A background job's fd-1 writes
  now land on the REAL sink instead of a live capture buffer — closer to
  bash's fork; no corpus-observable difference (the async form's
  late-restore window was the deviation).
- Concurrency note: this cycle's verification ran in an isolated git
  worktree (/tmp/sh2shadow, since removed) because a concurrent
  sh-backend worker edits src/transforms/arith_forms.rs + transforms.rs
  in the shared main checkout (its WIP oscillated the build green/red).
  Only src/shir.rs + src/estree.rs were staged from the submodule; the
  worker's untracked WIP was left untouched for its own commit.

---

## Plan 23: join provability + positional/cwd statement natives + the arith-IncDec Assign fix + grep_to_case activation (2026-08-08 estree improvement)

- Status: **IMPLEMENTED** (2026-08-08 — this cycle)
- Priority: 1
- Benchmark after: 20k-iter `${s:2:3}` slice loop runs with ZERO sh2.* calls in
  the loop body (0.149s incl. node startup); the metric's join row 22 → 15,
  builtin 692 → 679, setVar 152 → 148 (total 2271 → **2247**).
- Issues: (see below)

### What landed (five lowerings + two fixes)

1. **join provability extensions** (src/shir.rs, the `join` arm):
   - `join(param("slice", NAME, "@"/"*", ...))` — the runtime's
     `a === '@' || a === '*'` short-circuit returns `arrayItems(name)`
     BEFORE any store lookup, so a plain-name slice with a literal `@`/`*`
     offset is provably an ARRAY → native `.join(" ")` (the ambiguous
     array-or-string plain-name form only has NUMERIC offsets, which keep
     the runtime join).
   - provably-STRING join args (Literals, TemplateLiterals, `String(...)`
     coercions, string-method chains rooted at those — the native
     `String(name).slice(...)` param slices) → identity (the runtime join
     is `Array.isArray(v) ? v.join(" ") : String(v)` — identity for
     strings). Non-slice param ops (`:-`/`:?`/`#`/`%`/`//`...) always
     return scalars → identity too. No expression duplication (the arg is
     evaluated once either way).
2. **`set -- LIT...` / `set a b` → native positional write** — mirrors
   `builtins.set` exactly: `(sh2.positional = [...], sh2.lastExit = 0,
   true)`. Refuses flag forms (`set -euo pipefail` keeps the builtin),
   bare `set` (store print), magic-bearing args (GLOB/PS/BADSUB markers)
   and array-valued args (the flattener splices arrays).
3. **`shift [N]` → native `positional.slice`** — bare = 1, literal
   canonical-decimal N only (the runtime's `parseInt("2x")` partial
   parses are not natively reproducible).
4. **`pwd` → native `process.stdout.write(sh2.cwd + "\n")`** — the echo
   sink gates (ECHO_SINK_DEPTH == 0, no persistent fd-1, no script
   `pwd` function), the builtin's exact emit-then-status order.
5. **`/dev/null` write-redirect discard fast path** (harness/
   sh2-namespace.mjs): a `w`/`a` redirect to `/dev/null` installs
   `{kind:'discard'}` — emit/emitErr and the spawn byte handlers drop
   the bytes without the per-write open/write syscall (38 corpus
   redirect sites). ASSUMPTION (option-gated, default ON):
   `SH2_ASSUME_DEVNULL=0` restores the file-target path — the standard
   /dev/null discard device is present and writable; a write to it can
   never fail (silent success matches GNU's status-0).
6. **The arith_forms `Assign(Arith(IncDec))` shape fix** (three files
   were regressing before this cycle — the `i = i++` bug): the
   transform's `((i++))` rewrite was emitted as `i = i++` (the
   assignment clobbers the increment with the OLD value — the loop never
   advances). Now: the inc/dec is emitted bare with the Plan 4 status
   ternary (postfix = old value for `$?`, exactly the pre-transform
   `let "i++"` path), or bare when `lastexit_write_is_dead`. The
   deadness pass (`is_native_let_stmt` / `ir_stmt_writes_lastexit` /
   `mark_lastexit_dead`) now recognizes the Assign shape — the hot loop
   is `() => { i++; ... }` again.
7. **grep_to_case activation** (src/shir_passes/pattern/grep_to_case.rs):
   the lift NEVER fired — `extract_test_grep` read the command name from
   the CALL's `func` ("exec") instead of args[0] ("echo"/"printf"), and
   its 12 tests called the raw-IR `lower()` instead of the pipelined
   `lower_pipelined()` (7 failed). Fixed the extraction (the name is
   args[0]), switched the tests to the pipeline, corrected two assertions
   to the real serialization (Var discriminant, `"patterns":["*PAT*"]`).
   The lift is now green (12/12) and live for the sh backend's POSIX
   `case "$x" in *PAT*)` output.
- Metric: 2271 → **2247** total (builtin 692 → 679 — the set/shift/pwd
  natives; join 22 → 15; setVar 152 → 148); in-loop 8 unchanged (the
  remaining per-iteration sites are the irreducible procsub/awk/break
  shapes).
- Corpus: **531/531** estree (the oracle, final full run); cargo tests
  **181/181** (the 7 grep_to_case tests fixed; the 3 arith-IncDec corpus
  regressions — bench-increment / arith-postfix-status / 063_09 — were
  PRE-EXISTING at HEAD, from the arith_forms transform landing without
  the estree Assign-arm handling; fixed here). PERL pass count untouched
  (419 — never touched perl).
- Issues: none outstanding. The remaining metric surface is the
  irreducible families (arith `$var` string-insertion semantics,
  read-builtin vars, array store ops, external execs, context-bound
  echo/printf).

---

## Plan 24: join chain gap + captureWords single-word natives + `set --` flag-path fix + eager redirect open (2026-08-08 estree improvement)

- Status: **IMPLEMENTED** (2026-08-08 — this cycle; submodule commit pending)
- Priority: 1
- Benchmark after: metric 2172 → **2141** (−31 sites: join 15→8,
  captureWordsSync 14→5, redirectSync 138→132, builtin 617→608; exec
  171→173 + fnCall 33→31 — the captureWords await in function bodies
  correctly flips two sync fns to async exec dispatch). Corpus
  **531/531**; cargo tests 195/195 (3 new regression tests; run with the
  sibling workers' untracked c_backend/go_backend modules temporarily
  disabled — their in-test code doesn't compile against HEAD's APIs, a
  pre-existing condition of the shared checkout, NOT this cycle's work).
- Issues: (see below)

### What landed (four lowerings + one fidelity fix)

1. **join provably-string chain gap** (src/shir.rs, the join arm) — Plan
   23 claimed string-method chains as join-identity but its root-only
   scan only matched bare `String(name).member` (no call args): the
   param-slice emission `String(name).slice(0, 4)` is a CallExpression
   whose callee is the `.slice` member, so 5 corpus sites
   (064_03/064_hard/bench-substr `${name:0:4}`) stayed on the runtime
   join. New `provably_string_value` walker: every member link must be a
   string-returning method (split/match/matchAll — array-returning —
   and `.length` — a number — deliberately excluded; `join(split(...))`
   must stay a runtime call) and the chain must bottom out at
   `String(...)` / a literal / a template. Identity is exact (the
   runtime join is `Array.isArray(v) ? v.join(" ") : String(v)`).
2. **captureWords single-word natives** (src/shir.rs
   `capture_words_native_value`, wired into the captureWords arm next to
   the bc precedent) — the UNQUOTED `$(pwd)` / `$(wc FLAG < f)` capture
   forms: the runtime `capture().split(/\s+/).filter(...)` only splits
   when the value can be multi-word, and the wc count (digits) is
   UNIVERSALLY single-word; the pwd cwd twin is one word under the
   corpus's whitespace-free paths (the same environment assumption the
   native pwd statement already makes — documented in the code).
   8 sites (`local size=$(wc -c < f)` ×6, `$(pwd)` ×2): the
   captureWordsSync+redirectSync+builtin chain collapses to
   `[String(count)]` / `[sh2.cwd]`. Multi-word shapes (cat/sort/heredoc
   bodies, `ls | wc -l`) keep the runtime. The await the file read
   introduces correctly flips the enclosing function to async dispatch
   (the fn_call_sync_set lowered-body scan sees it — verified:
   test_simple_function's get_file_size becomes an async arrow + awaited
   exec call).
3. **native_capture_wc count-inside-promise** (src/shir.rs +
   `fs_promise_map` helper) — the wc count must live in the read's
   SUCCESS branch: a failed redirect makes bash skip the command (the
   capture is ""), while an empty file counts 0 — the error sentinel ""
   and empty content are the same string, so the old
   `String(count(await ...))` reported "0" for a missing file. Now
   `.then(r => (lastExit = 0, String(count(r)))).catch(e => (lastExit =
   1, ""))`. This also fixes the pre-existing capture-form latent
   divergence (test_simple_function's `wc -c < test_simple_function.sh`
   run from the workspace root is a MISSING relative file — bash ""
   vs old native "0" — the corpus caught it).
4. **`set -- LIT...` flag-path bug** (src/shir.rs try_native_set_shift)
   — a PRE-EXISTING bug at HEAD: the flag path fired before the
   positional path and `try_native_set_flags("--")` returns Some([]), so
   `set -- a b c d` emitted only `(lastExit = 0, true)` — the positional
   write lost (parse-at-slice.sh printed "" vs bash "c d"; the
   pre-session tree failed 530/531 on it with metric 2172 — the failure
   predates this cycle). The flag path now skips when ANY item is `--`
   (the pure `set -- a b` goes to the positional path; a mixed
   `set -e -- a` keeps the runtime builtin).
5. **eager redirect open** (harness/sh2-namespace.mjs
   `_applyRedirectSpecs`) — bash opens write targets EAGERLY at
   redirect-install time: an unwritable target skips the command with
   status 1. The runtime's lazy first-write open silently succeeded for
   a write-free command (`cat - > /tmp/out.txt` with EOF stdin — the
   cat-dash-stdin corpus test; the root sh-worker's `/tmp/out.txt`
   collision exposed it — bash "exit: 1" vs JS "exit: 0"). The live
   install now does the verification open (closeSync(openSync(t,
   w/a))) with the r-mode error protocol (report + lastExit 1 + return
   false); the /dev/null discard fast path and the persistent-restore
   create-if-missing pass are untouched. Also truncates existing files
   at install time — bash's `> f` semantics (the writer truncates at
   first write anyway).
- Metric: 2172 → 2141 (see above); in-loop 8 unchanged (the remaining
  per-iteration sites are the irreducible procsub/awk/break shapes).
- Corpus: **531/531** estree (the oracle, final full run); cargo tests
  **195/195** with 3 new regression tests
  (join_of_string_slice_chain_is_identity,
  set_double_dash_assigns_positionals_natively,
  capture_words_single_word_wc_count_is_native; the determinism +
  roundtrip tests green). PERL pass count untouched (never touched
  perl). The full corpus run's cat-dash-stdin flake was the root-owned
  /tmp/out.txt — fixed by #5 (the eager open now reports the EACCES
  exactly like bash).
- Issues: `cargo test --lib` cannot COMPILE in the shared checkout (the
  sibling workers' untracked src/c_backend.rs / src/go_backend.rs
  in-test code references APIs absent at HEAD — pre-existing; verified
  identical at the pre-session state). Verified by temporarily disabling
  those two modules, running 195/195, and restoring. The 5
  cross-process nondeterministic files (057_case etc.) are the
  documented pre-existing HashSet declaration-order issue (in-process
  deterministic_output test green). The pwd captureWords form inherits
  the native-pwd statement's whitespace-free-path environment
  assumption (documented in code next to the hook). `set -e -- a`
  mixed forms keep the runtime builtin (conservative — the native paths
  handle only the pure forms).

---

## Plan 25: sync file tests (sh2.fileTest) + the native-test status protocol

- Status: **IMPLEMENTED** (2026-08-08 — this cycle; sh2perl 0319a45,
  submodule commit pending)
- Priority: 1
- Target: `[ -f x ]` family currently lowers to `await sh2.fs.lstat(P)
  .then(...)` / `await sh2.fs.access(P, n).then(...)` chains — the LAST
  await in otherwise-sync loop bodies (tty-cmdsub, 064_16,
  064_hard_to_generate), forcing the per-iteration promise machinery.
  Replace with a sync `sh2.fileTest(flag, path)` runtime call (the
  runtime's evalUnary as a direct flag+path call: empty-arg rule, cwd
  resolution, accessSync `-r/-w/-x`, lstatSync + missing-path catch
  table — no test-string parse, no builtin dispatch). Sync I/O stays in
  the runtime (the established builtin pattern — `sh2.builtin("test")`
  already does lstatSync); the emitted code stays async-only (PLAN.md
  guardrail — no fs.*Sync in generated JS).
- Fixes a latent family-wide correctness bug found while landing the
  sync form: NO native test lowering recorded `$?`. A bare failing
  `[ -f /nonexistent ]` / `[ 1 -eq 2 ]` statement emitted the bare
  expression and left lastExit stale (rc=0 vs bash rc=1), and the
  errexit guard exited with the wrong code. All native tests
  (numeric/string/glob/`=~`/`-n`/`-z`/file) now wrap in the single-eval
  status protocol `(sh2._g = t, sh2.lastExit = sh2._g ? 0 : 1,
  sh2._g)`; `try_native_test` splits into the value-only unstatused
  body + the statused wrapper, and compound leaves stay unstatused (one
  status per test — bash records only the whole test's status).
- Benchmark after: 50k-iteration `for i in $(seq 1 50000); do
  [ -f /etc/hostname ]; done` — 4.21s (async chain) -> 0.21s (~20x;
  bash-parity 0.15s).
- Metric: forLoop 5 -> 1 (only the capture-bearing dqs-nested-awk-sed
  loop stays async); the last async whileLoop is gone; in-loop sh2.*
  calls 8 -> 6 (the 2 tty-cmdsub breaks left the in-loop bucket).
  fileTest 47 is a visibility swap (the old chains were sh2.fs.* member
  calls, never counted); `test` +2 is the &&-chain count-cap doing its
  documented job (fileTest + store-var getVar = 2 calls > the runtime
  test's 1 — the cap's metric-neutrality rule).
- Issues: none. Bonus: the pre-existing cargo test --lib compile
  breakage in the C/Go backend test modules (the previous worker
  worked around it by disabling those modules) is fixed at the source
  (i128 range helper; a stale debug-probe call) — 213 lib tests green
  including determinism. The c_backend/go_backend/lexer/parser/
  shir_json/wasi_api uncommitted WIP from the sibling workers rides
  along in the same submodule commit (the tree is one coherent verified
  state — HEAD's shir.rs alone does not build against the WIP parser).

---

## Plan 26: native `$(( $var ... ))` dollar-ref arith — the SH2_ASSUME_ARITH_VARS family (2026-08-08 estree improvement)

- Status: **IMPLEMENTED** (2026-08-08 — this cycle; sh2perl 567a0c6,
  workspace 0ad4fc7)
- Priority: 1
- Target: the runtime `sh2.arith("$j*$i")` STRING calls — `$((...))`
  texts whose `$name`/`${name}` refs kept them unparseable by the native
  arith parser, forcing the store round-trip per evaluation.
- Benchmark after: 047's loop body is now bare `{ j = j * i; }` (was
  `sh2.setVar("i", i); sh2.setVar("j", sh2.arith("$j*$i"))`); the
  `$[$i+1]`/`$[$j+1]` counters are `i = i + 1`; `(( $n == 5 ))` is a
  native `===` test. Zero sh2.* surface in those hot paths.
- Metric: 2095 → **2085** (arith 17→14, builtin 608→607, getVar
  273→270, setVar 148→144; arithEval 8→9 is the div/mod wrapper swap).
  In-loop 6 unchanged.
- Issues: (see below)

### What landed

The arith-call lowering (`call("arith", [Str])` — word_ir emits it only
when the text is not natively parseable), the `let`/`(( ))` path
(try_native_let / try_native_let_dead / arith_let_args_native /
collect_native_arith_sources), the expr arm, and the lifted-Assign arm
all now consult `native_arith_text` — `arith_text_dollar_strip` removes
the `$` from plain `$name`/`${name}` refs (rejecting `$(cmd)`,
`$1`/`$@`/`$#`/`$?`, `${#x}`, `${x[i]}`, `${x:-d}` and adjacent
`$j$i` refs), then the normal native-eligibility filter. The lift
walkers (numeric + string) skip the store marks for native texts and
the numeric-lift fixpoint admits the arith-call source (div/mod-free
only), so the refs' vars lift and the whole site collapses to native
JS.

Two documented gates keep the lowering bash-faithful (the corpus is
the oracle — 047 FAILED the naive version and drove the design):
1. **SH2_ASSUME_ARITH_VARS** (default ON; `=0` restores the runtime
   substitute-and-reparse evaluator): a SET ref's value is a canonical
   decimal. bash re-parses the substituted TEXT, so `x='2+3'` or
   `x=010` would evaluate differently; the corpus's `$ref` arith texts
   are all plain integers.
2. **The UNSET gate** (no option — pure conservatism): a ref whose var
   has no non-arith write source (`ARITH_REF_SET`, computed per
   compilation) may be EMPTY at the read. bash's empty substitution
   must still parse — the deletion gate removes the not-provably-set
   refs and re-parses. `j=$(( $j*$i ))` where the arith is j's only
   write stays on the runtime: bash errors on `*i` (assignment
   skipped, j stays unset, prints ""), which a native `0 * i` cannot
   express. `j=$(( $j+1 ))` with unset j is empty-safe (`+1` parses)
   and lowers.

Also removed a leftover `eprintln!("SITE-cstyle …")` debug line from
the string-lift walker (committed since 8dabd5c; pollutes stderr on
every compile).

- Corpus: **531/531** estree with the option ON and OFF (`SH2_ASSUME_
  ARITH_VARS=0` run verified; the option-off emission is byte-identical
  to the pre-change runtime forms). PERL pass count untouched (419).
- Cargo: **214/214** (new `dollar_ref_arith_text_lowers_natively`
  regression test covering the native path, the unset gate, the
  `$ref`-let cond, `$1` positionals staying runtime).
- Shared-analysis note: `analyze_var_types` (c_backend/go_backend/
  shir_json) consumes the same lifts — the verdicts are now more
  precise for the `$ref`-arith files (arithmetic-bracket's i/j,
  arithmetic-equality's n lift). The C/Go renderers must handle the
  lifted-Assign arith-call source (their WIP to reconcile — the same
  coupling the estree emitter just absorbed).
- Issues: none outstanding for the estree path. The remaining `arith`
  sites are the irreducible families (write-ful `i++ + ++i` texts,
  cmdsub-bearing `$(wc -l < f)` texts, `${#arr[@]}` params, `$1`
  positionals — the POSITIONAL_LOCAL family is a separate future
  assumption).

---

## Plan 27: statement `echo EXPR | bc` compile-time fold + native env/egrep (2026-08-08 estree improvement)

- Status: **IMPLEMENTED** (2026-08-08 — this cycle; submodule commit pending)
- Priority: 1
- Benchmark after: 3000-iteration `echo "2+3" | bc` loop: **5.43s (bc
  subprocess per iteration) → 0.144s (~38x)** — the emitted loop body is
  now a single `process.stdout.write` per iteration.
- Metric: 2085 → **2071** (exec 172→163 — the 7 statement bc spawns, the
  env spawn; pipeline 37→29 — the 7 async bc pipelines + the env
  pipeline; builtin 607→609 and pipelineSync 74→75 are the env/egrep
  sync-dispatch swaps). In-loop 6 unchanged.
- Issues: (see below)

### What landed (three lowerings)

1. **statement-position `echo EXPR | bc` → compile-time fold**
   (`try_native_echo_bc_stmt`, src/shir.rs — the STATEMENT twin of the
   capture-position `native_capture_echo_bc`, hooked into the pipeline
   arm of expr_to_estree before the tr/grep/cut collapses): a two-stage
   pipeline `[echo, exec bc]` whose echo arg is a STATIC program folds at
   compile time via src/bc.rs's exact GNU-bc semantics, and the whole
   pipeline collapses to `(process.stdout.write(<folded>), sh2.lastExit =
   0, true)` — no spawn, no async pipeline machinery, no fd swaps.
   bc-native-capture.sh's 7 statement sites (`2+3`, `scale=2; 5/2`,
   `2^10`, `7 % 3`, `-2^2`, `0.5+0.25`, `sqrt(25)`) all fold (values
   verified byte-identical vs real bc, incl. the `.75` no-leading-zero
   form). `-e` is refused (the fold cannot model echo's escape
   transformation); `-n` is skipped like the capture twin (bc is
   insensitive to the missing trailing newline); a fold failure
   (unsupported program) keeps the spawn. Dynamic programs (`$x + 1` —
   no runtime bc evaluator exists) keep the spawn. Gates: `bc_native_
   enabled()` (SH2_BC_NATIVE, default ON — the option-off emission is
   byte-identical to the pre-change pipeline+spawn form), no
   script-defined `echo`/`bc` functions, and the pwd-style sink gates are
   implicit (the pipeline's arg lowering runs with ECHO_SINK_DEPTH
   raised under captures/redirects, so a sink-context pipeline never
   reaches the fold).
2. **bare `env` → sync builtin** (src/shir.rs emitter special-case +
   `builtins.env` in harness/sh2-namespace.mjs): `exec("env", [])` with
   zero operands collapses to `sh2.builtin("env", [])` — the builtin
   dumps process.env as `NAME=value` lines in environment order with GNU
   env's `_=/usr/bin/env` overwrite, sink-correct everywhere (module
   stdout, capture buffer, redirect target — no sink gates needed). The
   corpus site `env | grep '^myexport='` (typeset-cmdsub.sh) becomes a
   SYNC pipeline of two builtins — no spawn, no per-call promise.
   Flag/carrying forms (`env -i`, `env NAME=V cmd`) keep the exec spawn
   (the builtin cannot run commands). ASSUMPTION (option-gated, default
   ON): **SH2_ASSUME_ENV=0** — the emitter twin restores the exec
   dispatch (the option-off emission is byte-identical to the pre-change
   form), and the runtime builtin refuses with status 127 when off (the
   established uname/ls gate pattern — the runtime exec path dispatches
   the builtins table, so the refusal is the honest option-off
   behavior). Documented divergence: GNU env overwrites `_` with its own
   argv[0] path vs node's launcher value — corpus-unobservable (the only
   site greps `^myexport=`, which `_` never matches).
3. **`egrep` → grep -E alias** (`builtins.egrep` in the runtime +
   SYNC_BUILTINS + builtins.json + A4 data/sh2-builtins.json):
   `$(egrep '^pattern' /dev/null)` (egrep-not-native.sh) becomes
   `captureSync(() => sh2.builtin("egrep", ...))` — no spawn. GNU's
   documented equivalence (egrep == grep -E); the shared grep builtin
   parses -E. The check_qx PERL gate is insensitive to the builtins.json
   addition (verified: the file's perl verdict is PASS with egrep in or
   out of the manifest).
- Corpus: **531/531** estree (the oracle, final full run); cargo tests
  **217/217** (3 new regression tests:
  echo_pipe_bc_statement_folds_to_native_write — incl. the dynamic
  `$x + 1` spawn-kept negative and the multi-statement `scale=2; 5/2`
  fold; bare_env_lowers_to_sync_builtin — incl. the `env -i` spawn-kept
  negative; egrep_lowers_to_sync_builtin). PERL pass count untouched
  (419 — never touched perl; builtins.json verified verdict-insensitive
  for the only egrep file). The A4 consistency test (data/sh2-builtins
  .json vs SYNC_BUILTINS) is green with egrep in both.
- Issues: the 6 in-loop sh2.* sites are untouched (the irreducible
  procsub/awk pipeline in dqs-nested-awk-sed.sh + the 096_head_procsub
  builtin). The remaining statement bc forms with dynamic operands keep
  the spawn (no runtime bc evaluator — a `sh2.bcEval` runtime twin of
  src/bc.rs would close that, but the corpus has no such shape). The
  `-e` echo flag is conservatively refused for the statement fold (the
  capture twin allows it — its operand assumption is documented there).
  SH2_ASSUME_ENV=0 and SH2_ASSUME_LS=0-style option-off corpus runs fail
  by design (the runtime refusal is the documented fidelity floor, same
  as the pre-existing uname/ls/hostname gates); SH2_BC_NATIVE=0 option-
  off runs pass (the spawn path is restored byte-identically).
