#include <stdio.h>
#include <string.h>
#include <assert.h>

char* w = NULL;
const char y[17] = "alpha beta gamma";

int main(int _mac, char **_mav) {
    assert(strlen(y) <= 16);

    /* TODO(unsupported): for item type */
    return 0;
}
/* 1 construct(s) lowered to TODO markers */