# t72_len_append: Python list len() and append()
# diagnostics: program prints its result to stdout
l = [1, 2]
l.append(3)
print(len(l))

# DRIVER: frontend emit gap (red gate — the worker's work item).
