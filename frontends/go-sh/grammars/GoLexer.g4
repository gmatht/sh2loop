// go-sh: Go source -> shIR JSON (A1 contract), ANTLR4+Go. Initial
// upgrade of the previous hand-rolled go-sh.go (the worker / git
// history has the old version). The proper antlr4 generation from
// grammars/GoLexer.g4 + GoParser.g4 (from grammars-v4) is TODO for
// the worker; this stub handles the v1 shell-flavored Go subset.
package main
