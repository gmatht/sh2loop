# t13_return: the `return` keyword form of the flow_control_statement
# node — pinned REFUSE. The A1 Return is the function rung's value
# channel (the plan maps `return v` inside functions, and functions
# refuse in v1); a top-level return in pwsh halts the script — the
# semantics are unpinned (refuse > guess).
return
