# t12_continue: the `continue` keyword form of the flow_control_statement
# node — pinned REFUSE, the t11_break precedent exactly: the A1
# Continue is a loop signal and v1's only landed loop (the t06
# do_statement duplication) lowers the body OUTSIDE the loop — a
# continue there would miscompile (the while/for rungs host it).
continue
