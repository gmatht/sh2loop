# triage: c backend can't render what the zsh-sh-go frontend emits

## NEED
The c renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend zsh-sh-go, example t25_num_cmp.zsh (cross-product pair).

## EVIDENCE
estree matches native; c differs

## OUTCOME: rejected: FAIL-BACKEND — the estree reference already renders this A1 and matches native; the c renderer lives in the backends/c worktree owned by its backend worker (out of core scope).
