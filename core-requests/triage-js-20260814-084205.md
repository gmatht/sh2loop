# triage: js backend can't render what the cpp-sh-go frontend emits

## NEED
The js renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend cpp-sh-go, example t04_comment.cc (cross-product pair).

## EVIDENCE
render failed: ShIR JSON ingress: Program.var_types[0].type: expected {{kind: Float, width: N}}

## OUTCOME: rejected: already implemented in the shared core — typed-C-int var_types objects (07671d8) are contract-valid and the estree reference renders this A1 (re-verified this round: the real cpp-sh-go A1 for t04_comment.cc ingresses on main and estree matches native); the failing backend/js worktree core predates main (merge-base c2d5cf6) and lacks the fix — merge main to pick it up
