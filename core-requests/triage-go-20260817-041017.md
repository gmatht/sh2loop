# triage: go backend can't render what the go-sh frontend emits

## NEED
The go renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend go-sh, example t58_seq_range.go (cross-product pair).

## EVIDENCE
estree matches native; go differs
