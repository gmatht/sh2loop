# triage: js backend can't render what the cpp-sh-go frontend emits

## NEED
The js renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend cpp-sh-go, example t30_static_assert.cc (cross-product pair).

## EVIDENCE
estree matches native; js differs

## OUTCOME: implemented — t30_static_assert.cc PASS — printf %s/%d native arm (5e7e7ac)
