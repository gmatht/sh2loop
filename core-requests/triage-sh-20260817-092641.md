# triage: sh backend can't render what the bat-sh-go frontend emits

## NEED
The sh renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend bat-sh-go, example t36_redirect_var.bat (cross-product pair).

## EVIDENCE
estree matches native; sh differs

## SUPERSEDED: dead_store_elim (redirect-target reads)
## REASON: Root cause was a canonical core transform bug, not the sh renderer:
## dead_store_elim (src/optimize.rs) did not count redirect TARGET reads, so it
## dropped the `f=out.txt` store and folded the redirect target to '' — the
## estree REFERENCE was the broken side (output ""), while the sh renderer
## (no DSE yet in its worktree) correctly printed "hi\nmore"; the mismatch was
## mis-filed as FAIL-BACKEND. Core commit 1e123dd (2026-08-17 12:40, "DSE
## redirect-target reads + mirror renderer arms (triage t36 cluster)") fixed the
## read-set walk; the 16:58 sweep records sh PASS and a direct render+run
## matches the corrected oracle byte-for-byte.
