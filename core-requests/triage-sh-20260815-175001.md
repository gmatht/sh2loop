# triage: sh backend can't render what the go-sh frontend emits

## NEED
The sh renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend go-sh, example t68_case_glob.go (cross-product pair).

## EVIDENCE
estree matches native; sh differs

## OUTCOME: implemented — 2026-08-15: single-`=` tests now render the GLOB case-emulation (the estree reference treats `=` as a pattern match; `<=`/`>=` and bare-tilde guards keep the `[ ]` literal forms) in the sh renderer, backends/sh 524d556 + main b875c44. Triage re-verified: go-sh × sh t68_case_glob.go = PASS (native match).
