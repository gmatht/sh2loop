# triage: python backend can't render what the go-sh frontend emits

## NEED
The python renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend go-sh, example t89_to_lower.go (cross-product pair).

## EVIDENCE
estree matches native; python differs

## OUTCOME: rejected: backend/python worktree renderer gap (sh2.param/arith/exec/test stubs); the python worker owns the renderer — estree reference (shared core) already matches native; no shared-core change exists.
