# triage: python backend can't render what the posix-sh-go frontend emits

## NEED
The python renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend posix-sh-go, example t51_arith_loop.sh (cross-product pair).

## EVIDENCE
render failed: ShIR JSON ingress: Program.stmts[0].body[0].expr.args[1].elements[0].ast.lhs.type: unknown arith type "Ident"

## OUTCOME: rejected: the A1 arith `Ident` node is implemented in the shared core since ef84c6f (KNOWN_ARITH + Ident arm in src/shir_json_in.rs, locked by the arith_ident_ingress regression test — same t51_arith_loop.sh shape ingresses and round-trips); the `unknown arith type "Ident"` failure comes from the backend/<lang> WORKTREE's branch-local src/shir_json_in.rs copy, which lags main (branch-local file, not tracked on main — no core change can land there). Fix: merge main into backend/<lang> (or cherry-pick the arith section of main's src/shir_json_in.rs) and rebuild.
