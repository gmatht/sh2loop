# triage: java backend can't render what the perl-sh-go frontend emits

## NEED
The java renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend perl-sh-go, example t43_heredoc.pl (cross-product pair).

## EVIDENCE
render failed: render: printf with a non-literal format (v1)

## OUTCOME: rejected: backend-renderer scope: "printf with a non-literal format (v1)" is the java renderer's printf arm (java_backend.rs — both the main mirror and the backends/java worktree lack the non-literal-format arm; estree matches native, so the A1 contract is fine); renderer arms are owned by the backend/java worker.
