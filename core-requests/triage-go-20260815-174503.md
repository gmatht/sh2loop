# triage: go backend can't render what the fish-sh-go frontend emits

## NEED
The go renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend fish-sh-go, example t55_array_elem.fish (cross-product pair).

## EVIDENCE
estree matches native; go differs

## OUTCOME: rejected: backend-renderer scope — the go renderer lives in the backends/go worktree (backend/go branch, owned by that backend worker); the estree reference already renders this A1 and matches native, so no shared-core change is needed.
