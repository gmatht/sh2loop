# triage: perl backend can't render what the zsh-sh-go frontend emits

## NEED
The perl renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend zsh-sh-go, example t32_redirect.zsh (cross-product pair).

## EVIDENCE
estree matches native; perl differs

## OUTCOME: rejected: backend-owned renderer — the estree reference proves the A1 correct (renders and matches native); the perl redirect-target arm drops the file target (main fixed the sh→perl path in 970d3e9, the A1→perl path is still missing it) — perl_backend.rs lives in the backend/perl worktree; merge main and finish the renderer work in the perl worktree
