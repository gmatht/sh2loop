# triage: sh backend can't render what the fish-sh-go frontend emits

## NEED
The sh renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend fish-sh-go, example t59_until.fish (cross-product pair).

## EVIDENCE
estree matches native; sh differs

## OUTCOME: rejected: backend-renderer scope: the A1 is contract-valid (estree reference matches native); the sh renderer's behavior differs — the sh backend worker owns backends/sh (sync main into the branch, then port renderer fixes)
