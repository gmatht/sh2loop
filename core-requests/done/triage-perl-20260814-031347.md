# triage: perl backend can't render what the posix-sh-go frontend emits

## NEED
The perl renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend posix-sh-go, example t51_arith_loop.sh (cross-product pair).

## EVIDENCE
render failed: ShIR JSON ingress: Program.stmts[0].body[0].expr.args[1].elements[0].ast.lhs.type: unknown arith type "Ident"

## OUTCOME: implemented — verified PASS via `harness/triage.sh posix-sh-go  perl` (fresh backend/perl binary): the backend/perl branch's sync of the core arith-Ident support (main ef84c6f) + its renderer work resolved the pair; estree reference also ingresses the emission (--shir-in-estree exit 0). No shared-core change was needed this round.
