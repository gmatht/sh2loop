# triage: c backend can't render what the zsh-sh-go frontend emits

## NEED
The c renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend zsh-sh-go, example t51_arith_loop.zsh (cross-product pair).

## EVIDENCE
render failed: ShIR JSON ingress: Program.stmts[0].body[0].expr.args[1].elements[0].ast.lhs.type: unknown arith type "Ident"
## OUTCOME: implemented — the cited ingress gap (arith "Ident") is fixed in the shared core (KNOWN_ARITH "Ident" + ArithAst::Ident arm, pinned by the arith_ident_ingress test); the pair re-verified PASS via harness/triage.sh this round
