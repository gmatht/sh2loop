# triage: c backend can't render what the py-sh-go frontend emits

## NEED
The c renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend py-sh-go, example t83_array_index_read.py (cross-product pair).

## EVIDENCE
estree matches native; c differs

## OUTCOME: rejected: no shared-core defect — triage classifies this FAIL-BACKEND (estree reference already matches native); the c renderer is backend-owned and its worker is active
