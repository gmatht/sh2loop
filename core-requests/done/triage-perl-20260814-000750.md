# triage: perl backend can't render what the py-sh-go frontend emits

## NEED
The perl renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend py-sh-go, example t44_background.py (cross-product pair).

## EVIDENCE
estree matches native; perl differs

## OUTCOME: implemented — verified py-sh-go/t44_background.py renders end-to-end (frontend emit -> strict ingress -> render) with the current core; the ingress/contract root cause (typed-C var_types/Cast/Sizeof, Ident arith, ForInit, new-node arms) is fixed.
