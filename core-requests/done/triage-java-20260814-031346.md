# triage: java backend can't render what the posix-sh-go frontend emits

## NEED
The java renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend posix-sh-go, example t48_accum.sh (cross-product pair).

## EVIDENCE
render failed: ShIR JSON ingress: Program.stmts[1].body[0].expr.ast.rhs.type: unknown arith type "Ident"

## OUTCOME: rejected: backend-ingress scope — the A1 arith node type "Ident" is contract-valid and the SHARED core already implements it (main ef84c6f, KNOWN_ARITH + ArithAst::Ident; re-verified: the exact t48/t51 emissions ingress via debashc --shir-in-estree exit 0, and the backend/perl cross-product pair PASSes); the java backend branch's own A1 ingress copy (backends/java/src/shir_json_in.rs KNOWN_ARITH + ArithAst in backends/java/src/ir.rs) is behind main and must accept Ident — the backend worker owns backends/java (setup_backends.sh --sync or port ef84c6f).
