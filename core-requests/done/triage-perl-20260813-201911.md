# triage: perl backend can't render what the fish-sh-go frontend emits

## NEED
The perl renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend fish-sh-go, example t54_str_subst.fish (cross-product pair).

## EVIDENCE
estree matches native; perl differs

## OUTCOME: rejected: backend-renderer scope: the A1 is contract-valid (estree reference matches native); the perl renderer's behavior differs — the perl backend worker owns backends/perl (sync main into the branch, then port renderer fixes)
