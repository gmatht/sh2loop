# triage: c backend can't render what the go-sh frontend emits

## NEED
The c renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend go-sh, example t35_exit_code.go (cross-product pair).

## EVIDENCE
estree matches native; c differs

## OUTCOME: rejected: backend-renderer scope — the c renderer lives in the backends/c worktree (backend/c branch, owned by that backend worker); the estree reference already renders this A1 and matches native, so no shared-core change is needed.
