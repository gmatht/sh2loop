# triage: js backend can't render what the zig-sh-go frontend emits

## NEED
The js renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend zig-sh-go, example t01_print.zig (cross-product pair).

## EVIDENCE
estree matches native; js differs
## OUTCOME: rejected: verified: the current backends/js binary renders zig-sh-go t01_print.zig PASS vs native (fixed by the backend/js worker's merges / prior ingress fixes since filing); no shared-core change needed.
