# triage: perl backend can't render what the zsh-sh-go frontend emits

## NEED
The perl renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend zsh-sh-go, example t32_redirect.zsh (cross-product pair).

## EVIDENCE
estree matches native; perl differs
## OUTCOME: implemented — verified 2026-08-16: harness/triage.sh pair now PASS (native match); the core's object-form var_types ingress (ir_type_from, shir_json_in.rs) renders the c-sh-go typed A1 (python worktree synced to main).
