# triage: c backend can't render what the c-sh-go frontend emits

## NEED
The c renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend c-sh-go, example t52_switch_many.c (cross-product pair).

## EVIDENCE
render failed: ShIR JSON ingress: Program.var_types[0].type: not a string

## OUTCOME: implemented — verified c-sh-go/t52_switch_many.c renders end-to-end (frontend emit -> strict ingress -> render) with the current core; the ingress/contract root cause (typed-C var_types/Cast/Sizeof, Ident arith, ForInit, new-node arms) is fixed.
