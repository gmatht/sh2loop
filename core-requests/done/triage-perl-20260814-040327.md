# triage: perl backend can't render what the perl-sh-go frontend emits

## NEED
The perl renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend perl-sh-go, example t44_background.pl (cross-product pair).

## EVIDENCE
estree matches native; perl differs

## OUTCOME: rejected: already fixed in the shared core — the main-branch renderer matches native for this pair (verified end-to-end this round); the backend/perl worktree predates the fix — merge main (setup_backends.sh --sync) to pick it up
