# triage: java backend can't render what the posix-sh-go frontend emits

## NEED
The java renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend posix-sh-go, example t51_arith_loop.sh (cross-product pair).

## EVIDENCE
render failed: ShIR JSON ingress: Program.stmts[0].body[0].expr.args[1].elements[0].ast.lhs.type: unknown arith type "Ident"
## OUTCOME: rejected: the Ident arith ingress gap was fixed in the shared core (KNOWN_ARITH + ArithAst::Ident, pinned by the arith-Ident regression test); triage now records SKIP-REFUSE "word not in the v1 Java subset: Arith(...)" — a java_backend.rs renderer refusal, owned by the backend/java worktree worker.
