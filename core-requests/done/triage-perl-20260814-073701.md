# triage: perl backend can't render what the zsh-sh-go frontend emits

## NEED
The perl renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend zsh-sh-go, example t32_redirect.zsh (cross-product pair).

## EVIDENCE
estree matches native; perl differs

## OUTCOME: rejected: backend-owned renderer — the estree reference proves the A1 correct; perl_backend.rs lives in the backend/perl worktree (merge-base 2bbbffd, missing main's 970d3e9 redirect + 9340682 background/=~/split perl fixes) — merge main and finish the renderer work in the perl worktree
