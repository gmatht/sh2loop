# triage: perl backend can't render what the bat-sh-go frontend emits

## NEED
The perl renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend bat-sh-go, example t49_robocopy2.bat (cross-product pair).

## EVIDENCE
estree matches native; perl differs
## OUTCOME: rejected: phantom — the estree reference crashes (estree-runner source-allowlist refusal of the underlying command), so the pair is unverifiable; triage classifies it SKIP-ESTREE-REF — no perl defect evidenced
