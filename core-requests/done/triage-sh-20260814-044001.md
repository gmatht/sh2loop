# triage: sh backend can't render what the c-sh-go frontend emits

## NEED
The sh renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend c-sh-go, example t20_switch.c (cross-product pair).

## EVIDENCE
render failed: ShIR JSON ingress: Program.var_types[0].type: not a string
## OUTCOME: rejected: already implemented in the shared core — main ingresses+renders this A1 (verified this round: estree reference OK on all 24 distinct A1s of the batch); the failing sh worktree binary predates the typed-var_types/ForInit/Cast ingress (07671d8/0b1e575) — sh backend worker: setup_backends.sh --sync + rebuild
