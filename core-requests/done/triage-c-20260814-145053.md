# triage: c backend can't render what the zsh-sh-go frontend emits

## NEED
The c renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend zsh-sh-go, example t48_accum.zsh (cross-product pair).

## EVIDENCE
render failed: ShIR JSON ingress: Program.stmts[1].body[0].body[0].expr.ast.rhs.type: unknown arith type "Ident"

## OUTCOME: implemented — the cited ingress gap (arith "Ident") is fixed in the shared core (KNOWN_ARITH "Ident" + ArithAst::Ident arm; pinned by the arith-Ident ingress tests); the zsh-sh-go t48_accum.zsh pair verified PASS via triage this round
