# triage: perl backend can't render what the posix-sh-go frontend emits

## NEED
The perl renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend posix-sh-go, example t62_word_split.sh (cross-product pair).

## EVIDENCE
estree matches native; perl differs

## OUTCOME: rejected: verified: the current backends/perl binary renders posix-sh-go t62_word_split.sh OK (ingress was synced into the backend worktree since this request was filed); estree reference already matched native. No shared-core change needed.

## OUTCOME: rejected: verified: the current backends/perl binary renders posix-sh-go t62_word_split.sh OK (ingress was synced into the backend worktree since this request was filed); estree reference already matched native. No shared-core change needed.
