# triage: java backend can't render what the c-sh-go frontend emits

## NEED
The java renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend c-sh-go, example t46_factorial.c (cross-product pair).

## EVIDENCE
render failed: ShIR JSON ingress: Program.var_types[0].type: not a string

## OUTCOME: rejected: still fails in the java backend's OWN renderer (render: expr not in the v1 Java subset: Arith(Bin { op: "*", lhs: Var("f"), rhs: Var("n") })); that component lives in backends/java (backend/java branch), not the shared core — the java backend worker owns the fix.

## OUTCOME: rejected: still fails in the java backend's OWN renderer (render: expr not in the v1 Java subset: Arith(Bin { op: "*", lhs: Var("f"), rhs: Var("n") })); that component lives in backends/java (backend/java branch), not the shared core — the java backend worker owns the fix.
