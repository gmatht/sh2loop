# triage: java backend can't render what the c-sh-go frontend emits

## NEED
The java renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend c-sh-go, example t58_func_runtime.c (cross-product pair).

## EVIDENCE
render failed: ShIR JSON ingress: Program.var_types[0].type: not a string

## OUTCOME: rejected: still fails in the java backend's OWN renderer (render: statement not in the v1 subset: Function { name: "add", body: [Assign { targets: [AssignTarg); that component lives in backends/java (backend/java branch), not the shared core — the java backend worker owns the fix.

## OUTCOME: rejected: still fails in the java backend's OWN renderer (render: statement not in the v1 subset: Function { name: "add", body: [Assign { targets: [AssignTarg); that component lives in backends/java (backend/java branch), not the shared core — the java backend worker owns the fix.
