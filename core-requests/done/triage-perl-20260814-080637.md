# triage: perl backend can't render what the perl-sh-go frontend emits

## NEED
The perl renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend perl-sh-go, example t60_contains.pl (cross-product pair).

## EVIDENCE
estree matches native; perl differs

## OUTCOME: rejected: backend-owned renderer — the estree reference proves the A1 correct; the negated contains() arm renders the positive branch ("has2" vs estree "no2") — perl_backend.rs lives in the backend/perl worktree
