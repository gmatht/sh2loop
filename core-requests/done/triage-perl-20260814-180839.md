# triage: perl backend can't render what the c-sh-go frontend emits

## NEED
The perl renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend c-sh-go, example t21_stdlib_more.c (cross-product pair).

## EVIDENCE
estree matches native; perl differs

## OUTCOME: rejected: duplicate of triage-perl-20260814-165943 (finalized this round) — backend-renderer scope: estree matches native so the A1 is contract-valid; the perl renderer's sticky array-flag renders getVar("buf") as $buf[0] after the empty setArray decl; perl worker owns backends/perl
