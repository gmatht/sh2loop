# triage: c backend can't render what the bat-sh-go frontend emits

## NEED
The c renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend bat-sh-go, example t29_mixed2.bat (cross-product pair).

## EVIDENCE
render failed: ShIR JSON ingress: Program.stmts[9].type: unknown stmt type "Goto"
## OUTCOME: implemented — the cited ingress gap (stmt "Goto") is fixed in the shared core (KNOWN_STMTS Goto/Label + IrStmt::Goto ingress); the bat-sh-go t29_mixed2.bat pair re-verified PASS via harness/triage.sh this round
