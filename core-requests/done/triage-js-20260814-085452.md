# triage: js backend can't render what the posix-sh-go frontend emits

## NEED
The js renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend posix-sh-go, example t51_arith_loop.sh (cross-product pair).

## EVIDENCE
render failed: ShIR JSON ingress: Program.stmts[0].body[0].expr.args[1].elements[0].ast.lhs.type: unknown arith type "Ident"

## OUTCOME: rejected: already implemented in the shared core — the A1 arith Ident node (ef84c6f) is contract-valid and the estree reference renders this A1 (re-verified this round: the real posix-sh-go A1 for t51_arith_loop.sh ingresses on main and estree matches native); the failing backend/js worktree core predates main (merge-base c2d5cf6) and lacks the fix — merge main to pick it up
