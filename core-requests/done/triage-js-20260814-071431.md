# triage: js backend can't render what the cpp-sh-go frontend emits

## NEED
The js renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend cpp-sh-go, example t05_arith_loop.cc (cross-product pair).

## EVIDENCE
render failed: ShIR JSON ingress: Program.stmts[1].type: unknown stmt type "ForInit"

## OUTCOME: rejected: already implemented in the shared core (07671d8 typed-C-int var_types objects + Cast/Sizeof arith; ef84c6f arith Ident; ForInit stmt; all locked by cargo-test regression tests, re-verified this round: the real frontend A1 for this example ingresses on main, estree 521/521); the failing backend/js worktree core predates main (merge-base c2d5cf6 (08-12)) and lacks the core fixes — merge main to pick it up
