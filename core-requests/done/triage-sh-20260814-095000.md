# triage: sh backend can't render what the zsh-sh-go frontend emits

## NEED
The sh renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend zsh-sh-go, example t29_increment.zsh (cross-product pair).

## EVIDENCE
estree matches native; sh differs

## OUTCOME: rejected: backend-owned sh renderer staleness — main's sh renderer re-verified prints 2 (MATCHES native); backend/sh worktree (e240890) predates main's IncDec rendering fix — sync backend/sh and rebuild
