# triage: perl backend can't render what the cpp-sh-go frontend emits

## NEED
The perl renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend cpp-sh-go, example t05_arith_loop.cc (cross-product pair).

## EVIDENCE
render failed: ShIR JSON ingress: Program.stmts[1].type: unknown stmt type "ForInit"

## OUTCOME: rejected: already implemented in the shared core (07671d8 typed-C-integers; ingress handles object var_types / ForInit / Cast — estree+perl pass, verified this round); the failing backend worktree core predates the 2026-08-12 sync — merge main to pick it up (for_init_roundtrip regression test landed)
