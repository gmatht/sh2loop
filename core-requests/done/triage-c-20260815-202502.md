# triage: c backend can't render what the perl-sh-go frontend emits

## NEED
The c renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend perl-sh-go, example t21_array.pl (cross-product pair).

## EVIDENCE
estree matches native; c differs

## OUTCOME: rejected: out of scope — the c renderer is owned by the backend/c worktree worker (single-owner discipline); the shared-core estree reference already renders the cited A1 and matches native (re-verified this round), so no shared-core change exists to make.
