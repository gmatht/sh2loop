# triage: python backend can't render what the bat-sh-go frontend emits

## NEED
The python renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend bat-sh-go, example t53_renpattern.bat (cross-product pair).

## EVIDENCE
estree matches native; python differs

## OUTCOME: rejected: out of scope — the python renderer is owned by the backend/python worktree worker (single-owner discipline); the shared-core estree reference already renders and matches native, so no shared-core change exists to make.
