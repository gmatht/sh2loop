# triage: c backend can't render what the c-sh-go frontend emits

## NEED
The c renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend c-sh-go, example t49_prime.c (cross-product pair).

## EVIDENCE
estree matches native; c differs

## OUTCOME: rejected: backend-renderer scope: "estree matches native" confirms the A1 is contract-valid, so the shared core is exonerated; the c (backends/c) renderer diverges/stubs the lowering — out of the shared-core scope this round; the c (backends/c) backend worker owns the fix (same rationale as the previous round triage-go rejections).
