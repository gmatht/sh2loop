# triage: perl backend can't render what the perl-sh-go frontend emits

## NEED
The perl renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend perl-sh-go, example t60_contains.pl (cross-product pair).

## EVIDENCE
estree matches native; perl differs

## OUTCOME: rejected: backend-owned renderer — the estree reference proves the A1 correct (renders and matches native); the perl diff lives in perl_backend.rs in the backend/perl worktree — merge main and finish the renderer work in the perl worktree
