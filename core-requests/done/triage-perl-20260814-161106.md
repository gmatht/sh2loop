# triage: perl backend can't render what the bat-sh-go frontend emits

## NEED
The perl renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend bat-sh-go, example t24_forf2.bat (cross-product pair).

## EVIDENCE
estree matches native; perl differs

## OUTCOME: implemented — verified bat-sh-go/t24_forf2.bat renders end-to-end (frontend emit -> strict ingress -> render, latest triage sweep) with the current core + backend binaries; the shared-core ingress/contract root cause (typed-C var_types {kind,width} objects, arith Ident/Cast/Sizeof, ForInit, new-node arms) is fixed on main and synced into the backend branches.
