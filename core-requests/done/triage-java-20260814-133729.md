# triage: java backend can't render what the cpp-sh-go frontend emits

## NEED
The java renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend cpp-sh-go, example t04_comment.cc (cross-product pair).

## EVIDENCE
render failed: ShIR JSON ingress: Program.var_types[0].type: not a string

## OUTCOME: rejected: still fails in the java backend's OWN renderer (render: expr not in the v1 Java subset: Arith(Bin { op: "+", lhs: Var("x"), rhs: Num(2) })); that component lives in backends/java (backend/java branch), not the shared core — the java backend worker owns the fix.

## OUTCOME: rejected: still fails in the java backend's OWN renderer (render: expr not in the v1 Java subset: Arith(Bin { op: "+", lhs: Var("x"), rhs: Num(2) })); that component lives in backends/java (backend/java branch), not the shared core — the java backend worker owns the fix.
