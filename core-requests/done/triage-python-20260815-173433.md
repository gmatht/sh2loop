# triage: python backend can't render what the posix-sh-go frontend emits

## NEED
The python renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend posix-sh-go, example t18_elif_chain.sh (cross-product pair).

## EVIDENCE
estree matches native; python differs

## OUTCOME: rejected: backend-renderer scope — the python renderer lives in the backends/python worktree (backend/python branch, owned by that backend worker); the estree reference already renders this A1 and matches native, so no shared-core change is needed.
