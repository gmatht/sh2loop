# triage: rust backend can't render what the powershell-sh-go frontend emits

## NEED
The rust renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend powershell-sh-go, example t19_merging_redirection.ps1 (cross-product pair).

## EVIDENCE
estree matches native; rust differs
## OUTCOME: rejected: rust renderer bug in backend-owned rust_backend.rs (out of core scope) — estree ref matches native so the A1 is correct
