# triage: sh backend can't render what the zsh-sh-go frontend emits

## NEED
The sh renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend zsh-sh-go, example t29_increment.zsh (cross-product pair).

## EVIDENCE
estree matches native; sh differs

## OUTCOME: implemented: shared-core ingress already in main; backend/sh synced+rebuilt this round; sync-surfaced renderer gaps fixed (needs_num Exec/ForInit walk, arith ${name} read, _num gate); pair now PASSes on the sweep
