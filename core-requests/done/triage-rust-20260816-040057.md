# triage: rust backend can't render what the go-sh frontend emits

## NEED
The rust renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend go-sh, example t73_range.go (cross-product pair).

## EVIDENCE
estree matches native; rust differs
## OUTCOME: implemented — verified 2026-08-16: harness/triage.sh pair now PASS (native match).
