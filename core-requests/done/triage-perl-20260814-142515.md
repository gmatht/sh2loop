# triage: perl backend can't render what the c-sh-go frontend emits

## NEED
The perl renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend c-sh-go, example t06_for.c (cross-product pair).

## EVIDENCE
render failed: ShIR JSON ingress: Program.var_types[0].type: not a string

## OUTCOME: implemented — verified c-sh-go t06_for.c renders end-to-end (frontend emit -> strict ingress -> render -> execute) with the current core and matches native (triage.sh PASS this round); the ingress/contract root causes (arith Ident reads ef84c6f, typed var_types object ingress, ForInit strip, perl renderer arms) are fixed in the shared core.
