# triage: perl backend can't render what the py-sh-go frontend emits

## NEED
The perl renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend py-sh-go, example t80_comp_if.py (cross-product pair).

## EVIDENCE
estree matches native; perl differs

## OUTCOME: rejected: out of scope — the perl renderer is owned by the backend/perl worktree worker (single-owner discipline); the shared-core estree reference already renders the cited A1 and matches native (re-verified this round), so no shared-core change exists to make.
