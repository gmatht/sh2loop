# triage: perl backend can't render what the py-sh-go frontend emits

## NEED
The perl renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend py-sh-go, example t78_try_except.py (cross-product pair).

## EVIDENCE
render failed: ShIR JSON ingress: Program.stmts[0].type: unknown stmt type "Try"

## OUTCOME: rejected: the ingress crash in the EVIDENCE is fixed (Try now ingresses — the pair is SKIP-REFUSE under the perl renderer's documented REFUSE > GUESS); rendering try/except is a perl renderer feature the backend/perl worker owns (the estree reference renders it via the runtime — A1 contract-valid).
