# triage: java backend can't render what the cpp-sh-go frontend emits

## NEED
The java renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend cpp-sh-go, example t04_comment.cc (cross-product pair).

## EVIDENCE
render failed: ShIR JSON ingress: Program.var_types[0].type: not a string
## OUTCOME: rejected: the var_types object-IrType ingress gap was fixed in the shared core (ir_type_from object arms, pinned by for_init_roundtrip); triage now records SKIP-REFUSE "expr not in the v1 Java subset: Arith(...)" — a java_backend.rs renderer refusal, owned by the backend/java worktree worker.
