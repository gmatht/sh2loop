# triage: sh backend can't render what the c-sh-go frontend emits

## NEED
The sh renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend c-sh-go, example t41_gcd.c (cross-product pair).

## EVIDENCE
estree matches native; sh differs

## OUTCOME: rejected: backend-renderer scope: the estree reference matches native, so the A1 is contract-valid; the sh backend's rendering diverges (backend worker owns backends/sh)
