# triage: java backend can't render what the zsh-sh-go frontend emits

## NEED
The java renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend zsh-sh-go, example t51_arith_loop.zsh (cross-product pair).

## EVIDENCE
render failed: ShIR JSON ingress: Program.stmts[0].body[0].expr.args[1].elements[0].ast.lhs.type: unknown arith type "Ident"

## OUTCOME: rejected: already implemented in the shared core on main (07671d8 typed-C-int var_types + Cast/Sizeof ingress; ef84c6f arith Ident) — estree reference re-verified MATCHES native this round; backend/java worktree core predates main (never synced) — run setup_backends.sh --sync java and rebuild
