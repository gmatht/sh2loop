# triage: java backend can't render what the c-sh-go frontend emits

## NEED
The java renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend c-sh-go, example t28_dowhile.c (cross-product pair).

## EVIDENCE
render failed: ShIR JSON ingress: Program.var_types[0].type: not a string

## OUTCOME: rejected: pair now SKIP-REFUSE — the java renderer refuses this A1 on its documented v1-subset REFUSE > GUESS policy (renderer is backend-owner scope); the shared-core ingress root cause is fixed — the A1 ingresses and reaches the renderer
