# triage: java backend can't render what the c-sh-go frontend emits

## NEED
The java renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend c-sh-go, example t05_while.c (cross-product pair).

## EVIDENCE
render failed: ShIR JSON ingress: Program.var_types[0].type: not a string

## OUTCOME: implemented: shared-core java renderer (main mirror, sh2perl main 1ec0871) renders t05_while.c (c-sh-go): the cited ingress errors (var_types "not a string" / unknown arith type "Ident") were already fixed in main; the remaining renderer refusals (Arith Bin/IncDec/Ident, DoWhile, ForInit, Break, setVar, C mem arena, test conds) now render native Java — verified javac-compile + native match on all 14 distinct pairs; estree 546/546, perl 370/176, lib 307 passed (only the pre-existing glsl bridges_tex_group_dependency failure). backend/java picks it up on its next main sync.
