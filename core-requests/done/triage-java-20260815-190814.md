# triage: java backend can't render what the zig-sh-go frontend emits

## NEED
The java renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend zig-sh-go, example t03_if_else.zig (cross-product pair).

## EVIDENCE
estree matches native; java differs
## OUTCOME: rejected: the A1 now ingresses cleanly and the estree reference matches native (triage: FAIL-BACKEND); the residual "java differs" is the java renderer, owned by the backend/java worktree worker (its java_backend.rs diverges from main) — no shared-core change exists to make.
