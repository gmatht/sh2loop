# triage: perl backend can't render what the c-sh-go frontend emits

## NEED
The perl renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend c-sh-go, example t10_string_pointer.c (cross-product pair).

## EVIDENCE
estree matches native; perl differs

## OUTCOME: rejected: backend-renderer scope: the estree reference matches native, so the A1 is contract-valid; the perl backend's rendering diverges (backend worker owns backends/perl)
