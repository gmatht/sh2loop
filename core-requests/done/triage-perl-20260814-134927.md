# triage: perl backend can't render what the perl-sh-go frontend emits

## NEED
The perl renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend perl-sh-go, example t27_not_expr.pl (cross-product pair).

## EVIDENCE
estree matches native; perl differs

## OUTCOME: rejected: verified: the current backends/perl binary renders perl-sh-go t27_not_expr.pl OK (ingress was synced into the backend worktree since this request was filed); estree reference already matched native. No shared-core change needed.

## OUTCOME: rejected: verified: the current backends/perl binary renders perl-sh-go t27_not_expr.pl OK (ingress was synced into the backend worktree since this request was filed); estree reference already matched native. No shared-core change needed.
