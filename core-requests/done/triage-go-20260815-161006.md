# triage: go backend can't render what the bat-sh-go frontend emits

## NEED
The go renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend bat-sh-go, example t16_case.bat (cross-product pair).

## EVIDENCE
estree matches native; go differs

## OUTCOME: rejected: out of scope — the go renderer is owned by the backend/go worktree worker (single-owner discipline); the shared-core estree reference already renders and matches native, so no shared-core change exists to make.
