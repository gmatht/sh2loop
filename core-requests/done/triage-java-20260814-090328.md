# triage: java backend can't render what the zsh-sh-go frontend emits

## NEED
The java renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend zsh-sh-go, example t51_arith_loop.zsh (cross-product pair).

## EVIDENCE
render failed: ShIR JSON ingress: Program.stmts[0].body[0].expr.args[1].elements[0].ast.lhs.type: unknown arith type "Ident"

## OUTCOME: rejected: already implemented in the shared core — the A1 arith Ident node (ef84c6f) is contract-valid and the estree reference renders this A1 (re-verified this round: the real zsh-sh-go A1 for t51_arith_loop.zsh ingresses on main and estree matches native); the failing backend/java worktree core predates main (merge-base 429f9f1) and lacks the fix — merge main to pick it up
