# triage: perl backend can't render what the c-sh-go frontend emits

## NEED
The perl renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend c-sh-go, example t21_stdlib_more.c (cross-product pair).

## EVIDENCE
estree matches native; perl differs

## OUTCOME: rejected: residual diff is in the branch-local perl renderer (backend/perl src/perl_backend.rs, 4.4k lines ahead of main, worker-owned): setVar-after-setArray sigil-slot mismatch for the `char *dst = buf` alias (renders $dst = $buf[0] while $buf was written scalar). Shared core ingresses/renders the A1; the perl worker owns perl_backend.rs (actively committing).
