# triage: js backend can't render what the go-sh frontend emits

## NEED
The js renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend go-sh, example t40_nested_loop.go (cross-product pair).

## EVIDENCE
estree matches native; js differs

## OUTCOME: rejected: js renderer is a stub draft (sh2.arith/sh2.exec TODO markers — "let + sh2.* stubs in this draft" header); "estree matches native" confirms the A1 is contract-valid, so the shared core is exonerated; the backend/js worker owns the renderer (same rationale as the prior rounds' js rejects).
