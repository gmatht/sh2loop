# triage: c backend can't render what the c-sh-go frontend emits

## NEED
The c renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend c-sh-go, example t20_switch.c (cross-product pair).

## EVIDENCE
render failed: ShIR JSON ingress: Program.var_types[0].type: not a string
## OUTCOME: implemented — the cited ingress gap (var_types object-form IrType) is fixed in the shared core (ir_type_from object arms, pinned by the typed_int_type_roundtrip/float_type_roundtrip locks); the pair re-verified PASS via harness/triage.sh this round
