# triage: c backend can't render what the bat-sh-go frontend emits

## NEED
The c renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend bat-sh-go, example t42_nestedcall.bat (cross-product pair).

## EVIDENCE
estree matches native; c differs

## OUTCOME: rejected: out of scope — the c renderer is owned by the backend/c worktree worker (single-owner discipline); the shared-core estree reference already renders and matches native, so no shared-core change exists to make.
