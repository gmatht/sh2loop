# triage: python backend can't render what the zsh-sh-go frontend emits

## NEED
The python renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend zsh-sh-go, example t48_accum.zsh (cross-product pair).

## EVIDENCE
render failed: ShIR JSON ingress: Program.stmts[1].body[0].body[0].expr.ast.rhs.type: unknown arith type "Ident"

## OUTCOME: rejected: already implemented in the shared core — the A1 arith Ident node (ef84c6f) is contract-valid and the estree reference renders this A1 (re-verified this round: the real zsh-sh-go A1 for t48_accum.zsh ingresses on main and estree matches native); the failing backend/python worktree core predates main (merge-base 6bdac8d) and lacks the fix — merge main to pick it up
