# triage: python backend can't render what the go-sh frontend emits

## NEED
The python renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend go-sh, example t80_pointer_type.go (cross-product pair).

## EVIDENCE
estree matches native; python differs

## OUTCOME: implemented — verified 2026-08-16: harness/triage.sh pair PASS (native match). Fix landed this round: python renderer test_value — `$name` is always a var read (unknown → "", store-written → sh2_getVar), mirroring the getVar arm (backend/python 4b87e2a).
