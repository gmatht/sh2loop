# triage: sh backend can't render what the c-sh-go frontend emits

## NEED
The sh renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend c-sh-go, example t49_prime.c (cross-product pair).

## EVIDENCE
render failed: render: command expression not renderable: Arith(IncDec { var: "d", delta: 1, prefix: false })

## OUTCOME: implemented — 2026-08-15: statement-position Arith (IncDec) arm in cmd_to_sh (main src/sh_backend.rs:1925 + backends/sh e2e3c7b); triage re-verified: c-sh-go × sh t49_prime.c = PASS (native match).
