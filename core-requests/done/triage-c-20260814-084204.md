# triage: c backend can't render what the cpp-sh-go frontend emits

## NEED
The c renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend cpp-sh-go, example t05_arith_loop.cc (cross-product pair).

## EVIDENCE
render failed: ShIR JSON ingress: Program.stmts[1].type: unknown stmt type "ForInit"

## OUTCOME: rejected: already implemented in the shared core — the ForInit stmt node (0b1e575 ingress + c40a3aa estree lowering) is contract-valid and the estree reference renders this A1 (re-verified this round: the real cpp-sh-go A1 for t05_arith_loop.cc ingresses on main and estree matches native); the failing backend/c worktree core predates main (merge-base 654b2b0) and lacks the fix — merge main to pick it up
