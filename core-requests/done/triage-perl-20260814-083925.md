# triage: perl backend can't render what the c-sh-go frontend emits

## NEED
The perl renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend c-sh-go, example t67_heap_dyn.c (cross-product pair).

## EVIDENCE
render failed: ShIR JSON ingress: Program.var_types[0].type: not a string

## OUTCOME: rejected: already implemented in the shared core — the typed-C-int var_types objects (07671d8) fix is in the backend/perl worktree (merge-base 2bbbffd); the pair now renders and the perl verdict is SKIP-REFUSE (t67_heap_dyn.c reaches only the documented memAlloc SKIP-REFUSE, a renderer concern) — no core action remains
