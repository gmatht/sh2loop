# triage: js backend can't render what the cpp-sh-go frontend emits

## NEED
The js renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend cpp-sh-go, example t26_preproc_elifdef.cc (cross-product pair).

## EVIDENCE
estree matches native; js differs
## OUTCOME: rejected: backend-specific renderer fix belongs in the backends/<lang> worktree (its own worker); estree reference already matches native — outside the ESTree fix surface
