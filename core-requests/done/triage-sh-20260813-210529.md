# triage: sh backend can't render what the c-sh-go frontend emits

## NEED
The sh renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend c-sh-go, example t09_pointer_alias.c (cross-product pair).

## EVIDENCE
render failed: ShIR JSON ingress: Program.var_types[0].type: not a string

## OUTCOME: rejected: backend-renderer scope: the A1 var_types carry the object IrType shape (typed C ints, core commit 07671d8) and the estree reference renders them (verified: --shir-in-estree exit 0 on the c-sh-go emission); the sh backend's A1 deserializer must accept the object form (backend worker owns backends/sh)
