# triage: rust backend can't render what the zsh-sh-go frontend emits

## NEED
The rust renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend zsh-sh-go, example t58_seq_range.zsh (cross-product pair).

## EVIDENCE
estree matches native; rust differs

## OUTCOME: implemented — t58_seq_range.zsh PASS — For Range init ';' before the comment (rust worktree bce9d03)
