# triage: python backend can't render what the zsh-sh-go frontend emits

## NEED
The python renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend zsh-sh-go, example t34_cmdsub.zsh (cross-product pair).

## EVIDENCE
estree matches native; python differs

## OUTCOME: rejected: FAIL-BACKEND — the estree reference already renders this A1 and matches native; the python renderer lives in the backends/python worktree owned by its backend worker (out of core scope).
