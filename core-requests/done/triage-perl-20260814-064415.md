# triage: perl backend can't render what the posix-sh-go frontend emits

## NEED
The perl renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend posix-sh-go, example t62_word_split.sh (cross-product pair).

## EVIDENCE
estree matches native; perl differs

## OUTCOME: implemented
The A1 `split` marker now field-splits in LIST contexts (echo join+grep,
printf per-field format cycling, for-iter `split /\s+/`, array stores),
and bash printf backslash escapes (`\n`) in the format decode for Perl's
printf (which does not interpret them). t62_word_split.sh matches native
(regression test split_word_expands_in_list_contexts; ./fail 360 vs 325).
