# triage: perl backend can't render what the fish-sh-go frontend emits

## NEED
The perl renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend fish-sh-go, example t81_regex_match.fish (cross-product pair).

## EVIDENCE
estree matches native; perl differs

## OUTCOME: implemented — landed the regexMatch arm in ir.rs ir_expr_to_perl (+ perl_backend.rs Render::call for the wasm entry); verified perl output matches native (matched/no-match/ci-ok).
