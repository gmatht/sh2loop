# triage: c backend can't render what the zsh-sh-go frontend emits

## NEED
The c renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend zsh-sh-go, example t85_cstyle_for.zsh (cross-product pair).

## EVIDENCE
estree matches native; c differs

## OUTCOME: rejected: out of core scope — the A1 is correct (estree matches native, verified this round); the c renderer (src/c_backend.rs, backend-owned worktree) emits a ForInit loop whose cond never advances (infinite i=0 spin); the shared core already ships the strip_cfor pass the renderer could wire.
