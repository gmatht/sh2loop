# triage: js backend can't render what the c-sh-go frontend emits

## NEED
The js renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend c-sh-go, example t06_for.c (cross-product pair).

## EVIDENCE
render failed: ShIR JSON ingress: Program.var_types[0].type: expected {{kind: Float, width: N}}

## OUTCOME: rejected: pair now SKIP-REFUSE — the js renderer refuses the A1 on its documented v1-subset REFUSE > GUESS policy (renderer is backend-owner scope, not shared core); the shared-core ingress root cause is fixed (the A1 ingresses and reaches the renderer).
