# triage: perl backend can't render what the zsh-sh-go frontend emits

## NEED
The perl renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend zsh-sh-go, example t85_cstyle_for.zsh (cross-product pair).

## EVIDENCE
render failed: ShIR JSON ingress: Program.stmts[0].type: unknown stmt type "ForInit"

## OUTCOME: rejected: backend-renderer scope: the A1 ForInit stmt is contract-valid (estree reference renders it — the ForInit-era lowering, commit 68aa4d6/c40a3aa); the perl backend's A1 ingress must accept the node (backend worker owns backends/perl)
