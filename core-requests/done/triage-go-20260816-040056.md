# triage: go backend can't render what the go-sh frontend emits

## NEED
The go renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend go-sh, example t90_join_full_array.go (cross-product pair).

## EVIDENCE
estree matches native; go differs
## OUTCOME: rejected: go renderer lacks setArray/join arms; go worktree also compile-blocked on main's core (go_backend.rs needs asm/named_blocks/ArrayComp/Lambda/Splice/Arith arms) — the go worker must merge main itself (out of core scope)
