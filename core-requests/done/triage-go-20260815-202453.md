# triage: go backend can't render what the perl-sh-go frontend emits

## NEED
The go renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend perl-sh-go, example t55_array_elem.pl (cross-product pair).

## EVIDENCE
estree matches native; go differs

## OUTCOME: rejected: out of scope — the go renderer is owned by the backend/go worktree worker (single-owner discipline); the shared-core estree reference already renders the cited A1 and matches native (re-verified this round), so no shared-core change exists to make.
