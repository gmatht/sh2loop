# triage: c backend can't render what the py-sh-go frontend emits

## NEED
The c renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend py-sh-go, example t36_read_stdin.py (cross-product pair).

## EVIDENCE
render failed:  thread 'main' (525313) panicked at src/c_backend.rs:6517:17: byte index 22 is not a char boundary; it is inside '—' (bytes 21..24) of `/* ${arr[@]:off:len} �
## OUTCOME: rejected: backend-specific renderer fix belongs in the backends/<lang> worktree (its own worker); estree reference already matches native — outside the ESTree fix surface
