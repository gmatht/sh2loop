# triage: js backend can't render what the c-sh-go frontend emits

## NEED
The js renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend c-sh-go, example t44_nested_loops.c (cross-product pair).

## EVIDENCE
render failed: ShIR JSON ingress: Program.var_types[0].type: expected {{kind: Float, width: N}}

## OUTCOME: rejected: duplicate re-escalation of a historical FAIL-BACKEND verdict row (worker cycle lacks a latest-row filter — fix landing this round); the pair now SKIP-REFUSE — the js renderer documents its v1-subset REFUSE > GUESS policy (backend-owner scope); the shared-core ingress root cause is fixed
