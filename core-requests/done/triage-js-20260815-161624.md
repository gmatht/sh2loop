# triage: js backend can't render what the py-sh-go frontend emits

## NEED
The js renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend py-sh-go, example t71_range_loop.py (cross-product pair).

## EVIDENCE
estree matches native; js differs

## OUTCOME: rejected: no shared-core defect — triage classifies this FAIL-BACKEND (estree reference already matches native); the js renderer is backend-owned and its worker is active
