# triage: c backend can't render what the zsh-sh-go frontend emits

## NEED
The c renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend zsh-sh-go, example t89_arith_call.zsh (cross-product pair).

## EVIDENCE
estree matches native; c differs

## OUTCOME: rejected: backend/c worktree renderer gap (zsh mathfunc arith shelled out raw — bash rejects sqrt()/hypot()/fmod()); the c worker owns the renderer — estree reference (shared core) already matches native; no shared-core change exists.
