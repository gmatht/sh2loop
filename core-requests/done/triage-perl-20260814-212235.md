# triage: perl backend can't render what the bat-sh-go frontend emits

## NEED
The perl renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend bat-sh-go, example t51_shift.bat (cross-product pair).

## EVIDENCE
render failed:  thread 'main' (3794603) panicked at /rustc/e408947bfd200af42db322daf0fadfe7e26d3bd1/library/alloc/src/vec/mod.rs:2873:36: range end index 4 out of range for sl
## OUTCOME: rejected: core panic IS fixed (restructure.rs backward-goto splice, main 9c56d52, synced to backend/perl — pair now renders); residual output diff is a perl_backend.rs renderer bug (out of core scope)
