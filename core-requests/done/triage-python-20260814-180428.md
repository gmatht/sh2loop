# triage: python backend can't render what the c-sh-go frontend emits

## NEED
The python renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend c-sh-go, example t14_atoi.c (cross-product pair).

## EVIDENCE
render failed: ShIR JSON ingress: Program.var_types[0].type: not a string

## OUTCOME: rejected: duplicate re-escalation of a historical FAIL-BACKEND verdict row (the triage worker cycle re-reads ALL of verdicts.tsv instead of only new rows — run_triage_worker.sh latest-row filter fix landing this round); the pair now PASS/PASS-RENDER — the shared core already implements object typed-int/Float var_types (07671d8/c45800e, pinned by typed_int_type_roundtrip/float_type_roundtrip)
