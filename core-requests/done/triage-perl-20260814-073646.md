# triage: perl backend can't render what the zsh-sh-go frontend emits

## NEED
The perl renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend zsh-sh-go, example t73_zsh_arith_cond.zsh (cross-product pair).

## EVIDENCE
estree matches native; perl differs

## OUTCOME: rejected: already satisfied — the current triage verdict for this pair is PASS (native match)
