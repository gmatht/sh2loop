# triage: sh backend can't render what the fish-sh-go frontend emits

## NEED
The sh renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend fish-sh-go, example t80_arith_unary.fish (cross-product pair).

## EVIDENCE
estree matches native; sh differs

## OUTCOME: implemented — verified the sh renderer output matches native fish for t80_arith_unary (-3, 7) — the arith Un statement renders and executes correctly.
