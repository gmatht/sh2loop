# triage: sh backend can't render what the py-sh-go frontend emits

## NEED
The sh renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend py-sh-go, example t73_dict.py (cross-product pair).

## EVIDENCE
render failed: render: word not renderable: Index { var: "d", key: Str("a", DoubleQuoted) }

## OUTCOME: implemented — 2026-08-15: the A1 Index expr node arms (word + interp positions, literal/dynamic/@ keys, the POSIX per-element lowering) landed in core 84143ea + backends/sh 39214dc; the dynamic Var/getVar-key subscript arm (`a[i]`, t83) landed this round in backends/sh 524d556 + main b875c44. Triage re-verified: py-sh-go × sh t21_array/t56_array_append/t73_dict/t83_array_index_read all PASS (native match).
