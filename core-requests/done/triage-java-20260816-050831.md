# triage: java backend can't render what the cpp-sh-go frontend emits

## NEED
The java renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend cpp-sh-go, example t31_type_qualifier.cc (cross-product pair).

## EVIDENCE
estree matches native; java differs

## OUTCOME: rejected: backend-renderer scope: "estree matches native" confirms the A1 is contract-valid, so the shared core is exonerated; the java (backends/java) renderer diverges/stubs the lowering — out of the shared-core scope this round; the java (backends/java) backend worker owns the fix (same rationale as the previous round triage-go rejections).
