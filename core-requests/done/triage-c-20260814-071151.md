# triage: c backend can't render what the c-sh-go frontend emits

## NEED
The c renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend c-sh-go, example t79_switch_midbreak.c (cross-product pair).

## EVIDENCE
render failed: ShIR JSON ingress: Program.var_types[0].type: not a string

## OUTCOME: rejected: already implemented in the shared core (07671d8 typed-C-int var_types objects + Cast/Sizeof arith; ef84c6f arith Ident; ForInit stmt; all locked by cargo-test regression tests, re-verified this round: the real frontend A1 for this example ingresses on main, estree 521/521); the failing backend/c worktree core predates main (merge-base 654b2b0 (08-06)) and lacks the core fixes — merge main to pick it up
