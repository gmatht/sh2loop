# triage: sh backend can't render what the fish-sh-go frontend emits

## NEED
The sh renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend fish-sh-go, example t81_regex_match.fish (cross-product pair).

## EVIDENCE
render failed: render: command call not renderable: "regexMatch"

## OUTCOME: implemented — landed the regexMatch arm in src/sh_backend.rs (grep -E[i]q pipeline, mirrors the =~ arm); verified sh render matches native (matched/no-match/ci-ok).
