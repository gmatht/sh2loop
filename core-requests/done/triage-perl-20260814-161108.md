# triage: perl backend can't render what the bat-sh-go frontend emits

## NEED
The perl renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend bat-sh-go, example t49_robocopy2.bat (cross-product pair).

## EVIDENCE
estree matches native; perl differs

## OUTCOME: rejected: phantom — the estree REFERENCE itself fails the pair (runner source allowlist refuses the `rsync` spawn the bat example emits; documented in harness/triage.sh as this exact phantom-request precedent); the pair is unverifiable by design, no renderer/core defect.
