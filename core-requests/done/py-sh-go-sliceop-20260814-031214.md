# py-sh-go: sliceop re-escalated (10th cycle) — contract side re-verified DONE at 03:12; the frontend-side emit arm in frontends/py-sh-go/main.go is still the sole blocker; a[1:6:2] still refuses

## NEED
The py-sh-go frontend must lower Python's **slice step** (grammars-v4
Python3 rule `sliceop`: `':' test?` — the optional THIRD part of a
`subscript_`: `test? ':' test? sliceop?`, i.e. `a[1:6:2]`, `a[1:6:]`,
`a[::2]`). This re-affirms the still-pending requests
`core-requests/py-sh-go-sliceop-20260814-020318.md`, `-021156.md`,
`-022015.md`, `-022734.md`, `-023752.md`, `-024634.md`, `-025524.md`
and `-030404.md` — the estree worker should treat all NINE (now TEN
including this one) as ONE need and implement it once (oldest first,
per the mediation rule), then move all of them to done/.

The **A1 CONTRACT side is DONE and was re-verified end-to-end by this
coverage cycle on 2026-08-14 03:12** (fresh, not just asserted): taking
the live t66 emit and adding the 5th arg
(`param("slice","a","1","2","2")` — exact A1 casing
`"type":"Call"`/`"func":"param"`/`"purity":"PureCpu"`/Str args with
`"style":"DoubleQuoted"`) ingresses via `debashc --shir-in-estree`
(exit 0). The runtime step filter (`stepOf` guard + `.filter((_, i) =>
i % step === 0)`) lives in `harness/sh2-namespace.mjs` (~line 2831);
the ESTree renderer handles `param` `slice` in `sh2perl/src/estree.rs`.
The step field landed via the completed requests
`core-requests/done/py-sh-go-sliceop-20260813-194705.md`,
`core-requests/done/py-sh-go-sliceop-20260813-222910.md` and
`core-requests/done/py-sh-go-sliceop-20260814-001712.md`.

WHAT IS STILL NEEDED is the **frontend-side emit path** in
`frontends/py-sh-go/main.go` (main.go last touched 2026-08-14 00:34;
verified 03:11 with a fresh `make build` — it still has NO sliceop
support), which both completed requests explicitly deferred to py-sh-go
("the frontend fix lands after the contract has the shape") and which
has no owner while the py-sh-go gate is green (the py-sh-go worker is
failure-driven and only invokes pi on a RED gate; the coverage worker's
fix surface is testdata/ + this escalation channel only — the frontend
refuses, so no testdata example can be created):

1. `parsePostfix` `[` arm (main.go ~line 1293; the subscript comment at
   ~1295, the `: end` handling at ~1296-1321, `expectOp("]")` at
   ~1323): after the optional `: end`, accept an optional second `:` +
   step expr (`test? ':' test? (':' test?)?`) instead of failing on
   `expectOp("]")`; handle the empty-start (`[::2]`) and empty-step
   (`[1:6:]`) forms (nil parts stay nil).
2. `SubscriptE` (main.go ~line 83): add a nil-able `SliceStep Expr`
   field (next to `SliceStart`/`SliceEnd` at ~86/87).
3. `subscriptIR` (main.go ~line 2216, 4-arg
   `param("slice", name, start, len)` at ~2242): emit the optional 5th
   arg — `param("slice", name, start, len[, step])` with step from
   SliceStep (nil → omit, byte-identical to today's emits; empty third
   part `a[1:6:]` → emit `"1"`); v1 scope: positive ints only (the
   runtime's `stepOf` guard already enforces this), negative steps
   (`a[::-1]`) stay a follow-up.

The exact spec (parse shape, emit shape, failing case) is the completed
request `core-requests/done/py-sh-go-sliceop-20260813-222910.md`
("MINIMAL-CORE-CHANGE" item 1 + FAILING-CASE).

## WHY
The coverage hook (`rules-gap.sh py-sh-go`) still reports `sliceop` as
a Python3 grammar rule no testdata example exercises (re-verified
2026-08-14 03:11: no `[x:y:z]` step-slice anywhere in
`frontends/py-sh-go/testdata/`; t37_substring / t66_array_slice cover
only the 2-part slice). No example can be created: the frontend still
refuses every sliceop form — re-verified 03:11 against a freshly built
`./py-sh-go --shir`:

    $ printf 'a = ["a","b","c","d","e","f"]\nprint(" ".join(a[1:6:2]))\n' | ./py-sh-go --shir /dev/stdin --raw
    py-sh-go: parse: expected "]"
    exit=2

(`a[::2]` / `a[::-1]` → `unexpected token ":"`, `a[1:6:]` →
`expected "]"` — all exit 2, re-verified 03:11.) This is NOT a
by-design subset refusal: py-sh-go has no FRONTEND.md; the 2-part slice
IS supported and covered (t37, t66); the construct is an A1-extension
escalation (step slices: the shell param-slice idiom has no step;
`a[1:3]` maps (t37/t66), `a[1:3:2]` can't) whose contract/runtime side
is already implemented (re-verified 03:12 — 5-arg ingress exit 0).

Why this duplicate exists (harness note, not a core ask): the pending
requests are ledgered (`frontends/coverage/core-pending-py-sh-go.txt`
maps `sliceop` to the nine pending requests), yet `rules-gap.sh`
re-reports `sliceop` every green cycle: its core-pending filter is
`grep -vFf "$tmp.pats" "$tmp" > "$tmp.2" && mv "$tmp.2" "$tmp"` — when
the pending patterns match ALL remaining gap lines, grep exits 1, the
`&& mv` is skipped, and the unfiltered list is emitted, re-invoking pi
for an already-escalated gap. Fixing `frontends/coverage/rules-gap.sh`
(`&&` → `;`) is outside both this coverage invocation's and the estree
worker's fix surfaces, so it is flagged here for the coverage harness
owner (also flagged in `py-sh-go-sliceop-20260814-020318.md`,
`-025524.md` and `-030404.md`).

## MINIMAL-CORE-CHANGE
None required on the shared core — the A1 contract, ingress, ESTree
renderer and runtime already implement the step (re-verified end-to-end
2026-08-14 03:12). The remaining change is FRONTEND-side, in
`frontends/py-sh-go/main.go` (not the shared core; filed here because
the estree worker's batch is the workspace's only active pi while the
frontend gate is green):

1. `parsePostfix` `[` arm (~line 1293): parse
   `test? ':' test? sliceop?` — after the optional end expr, if the
   next token is `:`, consume it and parse an optional step expr; only
   then `expectOp("]")` (~1323). Empty parts stay nil.
2. `SubscriptE` (~line 83): add `SliceStep Expr` (nil-able).
3. `subscriptIR` (~line 2216): emit the 5th arg
   `param("slice", name, start, len[, step])` — step from SliceStep
   (nil → omit; empty third part → `"1"`); positive ints only, negative
   steps (`a[::-1]`) out of v1 scope (matches the runtime guard).

The exact spec (parse shape, emit shape, failing case) is the completed
request `core-requests/done/py-sh-go-sliceop-20260813-222910.md`
("MINIMAL-CORE-CHANGE" item 1 + FAILING-CASE) and the pending
`core-requests/py-sh-go-sliceop-20260814-020318.md`. The previous
cycle's request (`-030404.md`) was rejected with the routing note that
the frontend-side change is outside the estree worker's shared-core +
harness scope and "belongs to the py-sh-go worker". That routing has no
effect while the py-sh-go gate is green (the py-sh-go worker only
invokes pi on a RED gate), so this coverage cycle re-files: if the
estree worker judges the `frontends/py-sh-go/main.go` change outside
its fix surface, record that routing decision explicitly in the
rejection note so the loop can route the frontend arm to the py-sh-go
worker on its next RED gate, instead of this coverage gap re-filing
each green cycle.

## FAILING-CASE
Minimal Python program (python3 runs it fine):

```python
a = ["a", "b", "c", "d", "e", "f"]
print(" ".join(a[1:6:2]))
```

python3 stdout: `b d f` (indices 1, 3, 5).

Variants that must also work once the frontend arm lands (the contract
already handles all of them — the 5-arg ingress is verified):
`print(" ".join(a[::2]))` → `a c e`; `print(" ".join(a[1:6:]))` →
identical to `a[1:6]` (`b c d e f`). The intended testdata example
(the coverage hook will create it once the frontend emits) is the
FAILING-CASE program above, named t<NN>_array_slice_step.py following
the t37/t66 numbering.

Current behavior (re-verified 2026-08-14 03:11 against a fresh
`make build` of `./py-sh-go`): `a[1:6:2]` / `a[1:6:]` →
`py-sh-go: parse: expected "]"` (exit 2); `a[::2]` / `a[::-1]` →
`py-sh-go: parse: unexpected token ":"` (exit 2). So no testdata
example can cover `sliceop`, and the construct remains inexpressible
end-to-end even though every A1 contract / runtime piece for the 5-arg
step slice is in place.

## OUTCOME: rejected: frontend-side scope (explicitly recorded per the request's routing ask): the A1 contract, ingress, ESTree renderer and runtime already implement the 5-arg step slice end-to-end (re-verified: the t66-style 5-arg param("slice",...,"2") ingresses via debashc --shir-in-estree exit 0; estree reference matches native); the sole remaining arm is frontends/py-sh-go/main.go (parsePostfix `[` + SubscriptE + subscriptIR) which is OUTSIDE the estree worker's shared-core + harness fix surface. ROUTE the frontend emit arm to the py-sh-go worker on its next RED gate (the frontend is failure-driven and green today), or to the coverage harness owner; until then this coverage gap will re-file each green cycle — the loop should treat it as routed, not stalled. Also flagged (as the request notes): frontends/coverage/rules-gap.sh `&&` → `;` pending-filter fix belongs to the coverage harness owner.
