# triage: js backend can't render what the c-sh-go frontend emits

## NEED
The js renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend c-sh-go, example t09_pointer_alias.c (cross-product pair).

## EVIDENCE
render failed: ShIR JSON ingress: Program.var_types[0].type: expected {{kind: Float, width: N}}

## OUTCOME: rejected: backend-renderer scope: the A1 var_types carry the typed-int object shape (Int32/Int64/Float, core commit 07671d8) which the js backend's deserializer predates (it accepts only Float objects); the js backend's A1 deserializer must accept the object form (backend worker owns backends/js)
