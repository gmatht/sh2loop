# triage: python backend can't render what the perl-sh-go frontend emits

## NEED
The python renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend perl-sh-go, example t26_str_cmp.pl (cross-product pair).

## EVIDENCE
estree matches native; python differs

## OUTCOME: rejected: out of scope — the python renderer is owned by the backend/python worktree worker (single-owner discipline); the shared-core estree reference already renders the cited A1 and matches native (re-verified this round), so no shared-core change exists to make.
