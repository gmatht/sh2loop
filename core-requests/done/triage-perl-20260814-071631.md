# triage: perl backend can't render what the bat-sh-go frontend emits

## NEED
The perl renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend bat-sh-go, example t49_robocopy2.bat (cross-product pair).

## EVIDENCE
estree matches native; perl differs

## OUTCOME: rejected: pair unverifiable — the current sweep's estree reference also fails for this pair (SKIP-ESTREE-REF), so the request's premise (estree renders it and matches native) does not hold; re-file when the reference passes
