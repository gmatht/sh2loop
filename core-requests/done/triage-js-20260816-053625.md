# triage: js backend can't render what the zsh-sh-go frontend emits

## NEED
The js renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend zsh-sh-go, example t85_cstyle_for.zsh (cross-product pair).

## EVIDENCE
estree matches native; js differs

## OUTCOME: rejected: out of core scope — the A1 is correct (estree matches native, verified this round); the js renderer (src/js_backend.rs, backend-owned worktree) lacks ForInit/let-cond support (lowers to TODO sh2.exec stubs, output empty); the shared core already ships the strip_cfor pass the renderer could wire (as estree/perl/sh cli do).
