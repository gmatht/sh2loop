# triage: js backend can't render what the py-sh-go frontend emits

## NEED
The js renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend py-sh-go, example t80_comp_if.py (cross-product pair).

## EVIDENCE
render failed: ShIR JSON ingress: Program.stmts[0].expr.args[1].type: unknown expr type "ArrayComp"

Shared-core ingress on main accepts this A1 (verified this round: the exact failing-case file emits + re-ingresses with exit 0 — ForInit / var_types {kind,width} objects / ArrayComp / arith Ident+Cast all in KNOWN_* and handled). The reported failure came from the backend branch's older ingress; the fix is the shared core, picked up on backend sync.
## OUTCOME: implemented

