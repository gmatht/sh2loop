# triage: js backend can't render what the zsh-sh-go frontend emits

## NEED
The js renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend zsh-sh-go, example t51_arith_loop.zsh (cross-product pair).

## EVIDENCE
render failed: ShIR JSON ingress: Program.stmts[0].body[0].expr.args[1].elements[0].ast.lhs.type: unknown arith type "Ident"

## OUTCOME: implemented — verified zsh-sh-go/t51_arith_loop.zsh renders end-to-end (frontend emit -> strict ingress -> render, latest triage sweep) with the current core + backend binaries; the shared-core ingress/contract root cause (typed-C var_types {kind,width} objects, arith Ident/Cast/Sizeof, ForInit, new-node arms) is fixed on main and synced into the backend branches.
