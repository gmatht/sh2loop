# triage: sh backend can't render what the zsh-sh-go frontend emits

## NEED
The sh renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend zsh-sh-go, example t29_increment.zsh (cross-product pair).

## EVIDENCE
estree matches native; sh differs

## OUTCOME: rejected: already fixed in the shared core (d54916b IncDec Assign arm + 72fca51 regression lock — verified MATCH with the main binary this round); the backend/sh worktree predates the fix — merge main (setup_backends.sh --sync) to pick it up
