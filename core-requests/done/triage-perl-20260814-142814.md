# triage: perl backend can't render what the c-sh-go frontend emits

## NEED
The perl renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend c-sh-go, example t21_stdlib_more.c (cross-product pair).

## EVIDENCE
estree matches native; perl differs

## OUTCOME: implemented
perl renderer: scalar write to an array-known var now assigns element 0 ($x[0] = v) — sprintf→setVar on a setArray'd char buffer was written to $x and read back from $x[0] (undef). backends/perl 2e36b11; backend gate 622/638 equiv unchanged. Verified PASS via harness/triage.sh.
