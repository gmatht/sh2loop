# triage: js backend can't render what the go-sh frontend emits

## NEED
The js renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend go-sh, example t36_read_stdin.go (cross-product pair).

## EVIDENCE
estree matches native; js differs

## OUTCOME: rejected: backend-renderer scope — the js renderer lives in the backends/js worktree (backend/js branch, owned by that backend worker); the estree reference already renders this A1 and matches native, so no shared-core change is needed.
