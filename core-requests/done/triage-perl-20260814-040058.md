# triage: perl backend can't render what the py-sh-go frontend emits

## NEED
The perl renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend py-sh-go, example t44_background.py (cross-product pair).

## EVIDENCE
estree matches native; perl differs

## OUTCOME: rejected: already fixed — the current perl worktree renders t44_background.py byte-identically to the estree reference (triage re-verified PASS this round); the verdict row was stale (pre-02:55 rebuild sweep)
