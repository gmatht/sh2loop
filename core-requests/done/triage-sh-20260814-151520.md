# triage: sh backend can't render what the c-sh-go frontend emits

## NEED
The sh renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend c-sh-go, example t49_prime.c (cross-product pair).

## EVIDENCE
render failed: ShIR JSON ingress: Program.var_types[0].type: not a string

## OUTCOME: rejected: the sh renderer (src/sh_backend.rs, sh-worker scope) refuses Arith(IncDec) in statement position (ForInit step after strip_cfor); not a shared-core defect — the A1 ingresses and the estree reference renders it. Needs an sh renderer arm ((d++)) — sh worker owns sh_backend.rs.
