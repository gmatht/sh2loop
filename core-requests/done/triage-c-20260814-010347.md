# triage: c backend can't render what the posix-sh-go frontend emits

## NEED
The c renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend posix-sh-go, example t51_arith_loop.sh (cross-product pair).

## EVIDENCE
render failed: ShIR JSON ingress: Program.stmts[0].body[0].expr.args[1].elements[0].ast.lhs.type: unknown arith type "Ident"

Shared-core ingress on main accepts this A1 (verified this round: the exact failing-case file emits + re-ingresses with exit 0 — ForInit / var_types {kind,width} objects / ArrayComp / arith Ident+Cast all in KNOWN_* and handled). The reported failure came from the backend branch's older ingress; the fix is the shared core, picked up on backend sync.
## OUTCOME: implemented

