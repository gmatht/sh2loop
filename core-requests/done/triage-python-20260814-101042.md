# triage: python backend can't render what the c-sh-go frontend emits

## NEED
The python renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend c-sh-go, example t57_cmp_value.c (cross-product pair).

## EVIDENCE
render failed: ShIR JSON ingress: Program.var_types[0].type: not a string

## OUTCOME: implemented: shared-core ingress already in main; backend/python synced+rebuilt this round; pair now renders (PASS-RENDER) on the sweep
