# triage: sh backend can't render what the go-sh frontend emits

## NEED
The sh renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend go-sh, example t54_map.go (cross-product pair).

## EVIDENCE
render failed: render: command call not renderable: "assocSet"

## OUTCOME: implemented — 2026-08-15: assocSet/assocGet arms in the sh renderer (command/word/interp positions; the POSIX per-element lowering `m_c=C` / `${m_go}`, literal + dynamic keys) landed in backends/sh 524d556 + main b875c44. Triage re-verified: go-sh × sh t54_map.go = PASS (native match).
