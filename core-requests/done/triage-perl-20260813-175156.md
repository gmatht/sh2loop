# triage: perl backend can't render what the cpp-sh-go frontend emits

## NEED
The perl renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend cpp-sh-go, example t03_new_delete.cc (cross-product pair).

## EVIDENCE
estree matches native; perl differs

## OUTCOME: implemented — cpp-sh-go t03_new_delete.cc -> PASS: memAlloc/Store/Load/Free now rendered via the sh2_mem* perl arena runtime (added this round). Verified: triage last-row verdict PASS; estree gate 546/546; cargo test --lib 303 pass (only the pre-existing glsl WIP fail); check_ast OK.
