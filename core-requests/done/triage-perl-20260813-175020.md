# triage: perl backend can't render what the c-sh-go frontend emits

## NEED
The perl renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend c-sh-go, example t83_const_ptr.c (cross-product pair).

## EVIDENCE
render failed: ShIR JSON ingress: Program.var_types[0].type: not a string

## OUTCOME: implemented — c-sh-go t83_const_ptr.c -> PASS: memLoad now rendered via the sh2_mem* perl arena runtime (added this round). Verified: triage last-row verdict PASS; estree gate 546/546; cargo test --lib 303 pass (only the pre-existing glsl WIP fail); check_ast OK.
