# triage: sh backend can't render what the c-sh-go frontend emits

## NEED
The sh renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend c-sh-go, example t74_switch_fallthrough.c (cross-product pair).

## EVIDENCE
render failed: ShIR JSON ingress: Program.var_types[0].type: not a string

## OUTCOME: rejected: already implemented in the shared core — typed-C-int var_types objects (07671d8) are contract-valid and the estree reference renders this A1 (re-verified this round: the real c-sh-go A1 for t74_switch_fallthrough.c ingresses on main and estree matches native); the failing backend/sh worktree core predates main (merge-base 429f9f1) and lacks the fix — merge main to pick it up
