# triage: perl backend can't render what the bat-sh-go frontend emits

## NEED
The perl renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend bat-sh-go, example t12_forf.bat (cross-product pair).

## EVIDENCE
estree matches native; perl differs
## OUTCOME: rejected: backend-renderer scope — the A1 is contract-valid (estree reference renders + matches native, verified this round; the read-with-IFS Object-arg shape now locked by shir_json_in::read_with_ifs_object_arg_roundtrip); the perl renderer's read-IFS divergence lives on backends/perl (perl backend worker owns it)
