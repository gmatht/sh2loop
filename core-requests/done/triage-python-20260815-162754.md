# triage: python backend can't render what the posix-sh-go frontend emits

## NEED
The python renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend posix-sh-go, example t46_printf.sh (cross-product pair).

## EVIDENCE
estree matches native; python differs

## OUTCOME: rejected: no shared-core defect — triage classifies this FAIL-BACKEND (estree reference already matches native); the python renderer is backend-owned and its worker is active
