# triage: perl backend can't render what the cpp-sh-go frontend emits

## NEED
The perl renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend cpp-sh-go, example t02_bool.cc (cross-product pair).

## EVIDENCE
render failed: ShIR JSON ingress: Program.var_types[0].type: not a string

## OUTCOME: rejected: already resolved — backends/perl synced main (bdd3fb5/6f85b12, 164 commits incl. 07671d8/ef84c6f); this pair now clears in triage (PASS/SKIP-REFUSE; estree re-verified MATCHES native)
