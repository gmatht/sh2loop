# triage: sh backend can't render what the c-sh-go frontend emits

## NEED
The sh renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend c-sh-go, example t71_multi_out.c (cross-product pair).

## EVIDENCE
render failed: render: command call not renderable: "setVar"

## OUTCOME: rejected: backend-renderer scope: the A1 setVar call renders in the estree reference; the sh renderer's command-call coverage must add setVar (backend worker owns backends/sh)
