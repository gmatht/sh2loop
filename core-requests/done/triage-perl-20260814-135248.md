# triage: perl backend can't render what the posix-sh-go frontend emits

## NEED
The perl renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend posix-sh-go, example t51_arith_loop.sh (cross-product pair).

## EVIDENCE
render failed: ShIR JSON ingress: Program.stmts[0].body[0].expr.args[1].elements[0].ast.lhs.type: unknown arith type "Ident"

## OUTCOME: implemented — verified posix-sh-go t51_arith_loop.sh renders end-to-end (frontend emit -> strict ingress -> render -> execute) with the current core and matches native (triage.sh PASS this round); the ingress/contract root causes (arith Ident reads ef84c6f, typed var_types object ingress, ForInit strip, perl renderer arms) are fixed in the shared core.
