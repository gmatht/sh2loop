# triage: js backend can't render what the cpp-sh-go frontend emits

## NEED
The js renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend cpp-sh-go, example t31_type_qualifier.cc (cross-product pair).

## EVIDENCE
estree matches native; js differs

## OUTCOME: rejected: backend-renderer scope: "estree matches native" confirms the A1 is contract-valid, so the shared core is exonerated; the js (backends/js) renderer diverges/stubs the lowering — out of the shared-core scope this round; the js (backends/js) backend worker owns the fix (same rationale as the previous round triage-go rejections).
