# triage: python backend can't render what the cpp-sh-go frontend emits

## NEED
The python renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend cpp-sh-go, example t02_bool.cc (cross-product pair).

## EVIDENCE
render failed: ShIR JSON ingress: Program.var_types[0].type: not a string

## OUTCOME: rejected: verified: the current backends/python binary renders cpp-sh-go t02_bool.cc OK (ingress was synced into the backend worktree since this request was filed); estree reference already matched native. No shared-core change needed.

## OUTCOME: rejected: verified: the current backends/python binary renders cpp-sh-go t02_bool.cc OK (ingress was synced into the backend worktree since this request was filed); estree reference already matched native. No shared-core change needed.
