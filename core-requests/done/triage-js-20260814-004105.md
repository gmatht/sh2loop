# triage: js backend can't render what the c-sh-go frontend emits

## NEED
The js renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend c-sh-go, example t40_sizes.c (cross-product pair).

## EVIDENCE
render failed: ShIR JSON ingress: Program.stmts[1].expr.args[1].elements[2].ast.type: unknown arith type "Cast"

## OUTCOME: implemented — verified c-sh-go/t40_sizes.c renders end-to-end (frontend emit -> strict ingress -> render) with the current core; the ingress/contract root cause (typed-C var_types/Cast/Sizeof, Ident arith, ForInit, new-node arms) is fixed.
