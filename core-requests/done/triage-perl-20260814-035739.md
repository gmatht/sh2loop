# triage: perl backend can't render what the bat-sh-go frontend emits

## NEED
The perl renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend bat-sh-go, example t49_robocopy2.bat (cross-product pair).

## EVIDENCE
estree matches native; perl differs

## OUTCOME: rejected: premise false — the estree reference CRASHES on rsync (runner security allowlist, no --source in cross-product triage), so "estree matches native" is unsubstantiated; triage.sh now records SKIP-ESTREE-REF when the reference fails (harness fix landed); perl is the only executable rendering and its output is semantically consistent with the bat
