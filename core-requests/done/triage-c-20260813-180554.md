# triage: c backend can't render what the cpp-sh-go frontend emits

## NEED
The c renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend cpp-sh-go, example t05_arith_loop.cc (cross-product pair).

## EVIDENCE
render failed: ShIR JSON ingress: Program.stmts[1].type: unknown stmt type "ForInit"
## OUTCOME: implemented — the cited ingress gap (stmt "ForInit") is fixed in the shared core (KNOWN_STMTS ForInit + IrStmt::ForInit ingress, pinned by the for_init_roundtrip test); the cpp-sh-go t05_arith_loop.cc pair re-verified PASS via harness/triage.sh this round
