# triage: sh backend can't render what the py-sh-go frontend emits

## NEED
The sh renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend py-sh-go, example t55_array_elem.py (cross-product pair).

## EVIDENCE
render failed: render: interp expr not renderable: Index { var: "a", key: Str("0", DoubleQuoted) }

## OUTCOME: implemented — 2026-08-15: the sh renderer Index arms (word + interp positions) landed in core 84143ea + backends/sh 39214dc; re-verified this round via triage: py-sh-go × sh t55_array_elem.py = PASS (native match).
