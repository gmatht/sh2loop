# triage: perl backend can't render what the bat-sh-go frontend emits

## NEED
The perl renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend bat-sh-go, example t12_forf.bat (cross-product pair).

## EVIDENCE
estree matches native; perl differs

## OUTCOME: rejected: backend-owned perl renderer gap — the estree oracle renders this A1 correctly (re-verified), but main's perl renderer dies at runtime on the bat forf redirect stmts ('not yet supported by the Perl backend (redirect)'); perl_backend.rs work belongs to the perl backend worker
