# triage: sh backend can't render what the c-sh-go frontend emits

## NEED
The sh renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend c-sh-go, example t39_strlen_var.c (cross-product pair).

## EVIDENCE
render failed: ShIR JSON ingress: Program.stmts[1].body[1].expr.args[1].elements[1].ast.type: unknown arith type "Cast"

## OUTCOME: rejected: already implemented in the shared core on main (07671d8 typed-C-int var_types + Cast/Sizeof ingress; ef84c6f arith Ident) — estree reference re-verified MATCHES native this round; backend/sh worktree core predates main (never synced) — run setup_backends.sh --sync sh and rebuild
