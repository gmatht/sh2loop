# triage: perl backend can't render what the c-sh-go frontend emits

## NEED
The perl renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend c-sh-go, example t21_stdlib_more.c (cross-product pair).

## EVIDENCE
estree matches native; perl differs
## OUTCOME: rejected: perl renderer bug in backend-owned perl_backend.rs (out of core scope) — estree ref matches native so the A1 is correct
