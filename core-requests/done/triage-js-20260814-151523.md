# triage: js backend can't render what the c-sh-go frontend emits

## NEED
The js renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend c-sh-go, example t63_bitwise.c (cross-product pair).

## EVIDENCE
render failed: ShIR JSON ingress: Program.var_types[0].type: expected {{kind: Float, width: N}}

## OUTCOME: implemented — verified c-sh-go/t63_bitwise.c renders end-to-end (frontend emit -> strict ingress -> render, latest triage sweep) with the current core + backend binaries; the shared-core ingress/contract root cause (typed-C var_types {kind,width} objects, arith Ident/Cast/Sizeof, ForInit, new-node arms) is fixed on main and synced into the backend branches.
