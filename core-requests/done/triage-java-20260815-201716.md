# triage: java backend can't render what the py-sh-go frontend emits

## NEED
The java renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend py-sh-go, example t81_triple_single_str.py (cross-product pair).

## EVIDENCE
estree matches native; java differs

## OUTCOME: rejected: out of scope — the java renderer is owned by the backend/java worktree worker (single-owner discipline); the shared-core estree reference already renders the cited A1 and matches native (re-verified this round), so no shared-core change exists to make.
