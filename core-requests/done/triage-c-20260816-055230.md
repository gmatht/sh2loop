# triage: c backend can't render what the go-sh frontend emits

## NEED
The c renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend go-sh, example t58_seq_range.go (cross-product pair).

## EVIDENCE
estree matches native; c differs

## OUTCOME: rejected: "estree matches native" confirms the A1 is contract-valid; the c renderer diverges (double-assign `i = i = 2`, post-inc `i = i++`) and the c worktree has ~444 uncommitted worker lines in flight — the backend/c worker owns the renderer.
