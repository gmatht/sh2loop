# triage: rust backend can't render what the zsh-sh-go frontend emits

## NEED
The rust renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend zsh-sh-go, example t15_arith_mul.zsh (cross-product pair).

## EVIDENCE
estree matches native; rust differs

## OUTCOME: rejected: FAIL-BACKEND — the estree reference already renders this A1 and matches native; the rust renderer lives in the backends/rust worktree owned by its backend worker (out of core scope).
