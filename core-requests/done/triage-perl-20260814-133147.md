# triage: perl backend can't render what the c-sh-go frontend emits

## NEED
The perl renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend c-sh-go, example t20_switch.c (cross-product pair).

## EVIDENCE
render failed: ShIR JSON ingress: Program.var_types[0].type: not a string

## OUTCOME: rejected: verified: the current backends/perl binary renders c-sh-go t20_switch.c OK (ingress was synced into the backend worktree since this request was filed); estree reference already matched native. No shared-core change needed.

## OUTCOME: rejected: verified: the current backends/perl binary renders c-sh-go t20_switch.c OK (ingress was synced into the backend worktree since this request was filed); estree reference already matched native. No shared-core change needed.
