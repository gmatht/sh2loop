# triage: python backend can't render what the c-sh-go frontend emits

## NEED
The python renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend c-sh-go, example t20_switch.c (cross-product pair).

## EVIDENCE
render failed: ShIR JSON ingress: Program.var_types[0].type: not a string

## OUTCOME: rejected: already implemented in the shared core — object typed-int/Float var_types (07671d8 typed C ints, c45800e Float) are contract-valid and the estree reference renders this A1; re-verified this round end-to-end (real frontend A1 -> ingress -> render): the pair now PASS/PASS-RENDER with current binaries; the request was filed against a stale sweep binary
