# triage: perl backend can't render what the cpp-sh-go frontend emits

## NEED
The perl renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend cpp-sh-go, example t05_arith_loop.cc (cross-product pair).

## EVIDENCE
render failed: ShIR JSON ingress: Program.stmts[1].type: unknown stmt type "ForInit"

## OUTCOME: rejected: duplicate re-escalation of a historical FAIL-BACKEND verdict row (worker cycle lacks a latest-row filter — fix landing this round); the pair now PASS/PASS-RENDER — ForInit ingress landed in 0b1e575 (pinned by for_init_roundtrip)
