# triage: perl backend can't render what the posix-sh-go frontend emits

## NEED
The perl renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend posix-sh-go, example t51_arith_loop.sh (cross-product pair).

## EVIDENCE
render failed: ShIR JSON ingress: Program.stmts[0].body[0].expr.args[1].elements[0].ast.lhs.type: unknown arith type "Ident"

## OUTCOME: implemented: shared-core ingress already in main; backend/perl synced+rebuilt this round; perl renderer gaps fixed (test group-paren parse, contains interpolated pattern, bat forf IFS-delim read); pair now PASSes (t49 = SKIP-ESTREE-REF phantom, runner rsync allowlist)
