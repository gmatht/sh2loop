# triage: sh backend can't render what the powershell-sh-go frontend emits

## NEED
The sh renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend powershell-sh-go, example t03_cast.ps1 (cross-product pair).

## EVIDENCE
render failed: render: interp expr not renderable: Interpolate([Lit("cast value")])

## OUTCOME: rejected: out of scope — the sh renderer is owned by the backend/sh worktree worker (single-owner discipline); the shared-core estree reference already renders the cited A1 and matches native (re-verified this round), so no shared-core change exists to make.
