# triage: sh backend can't render what the c-sh-go frontend emits

## NEED
The sh renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend c-sh-go, example t94_incdec.c (cross-product pair).

## EVIDENCE
render failed: render: command expression not renderable: Arith(IncDec { var: "x", delta: 1, prefix: true })

## OUTCOME: rejected: still fails in the sh backend's OWN renderer (render: command expression not renderable: Arith(IncDec { var: "x", delta: 1, prefix: true })); that component lives in backends/sh (backend/sh branch), not the shared core — the sh backend worker owns the fix.

## OUTCOME: rejected: still fails in the sh backend's OWN renderer (render: command expression not renderable: Arith(IncDec { var: "x", delta: 1, prefix: true })); that component lives in backends/sh (backend/sh branch), not the shared core — the sh backend worker owns the fix.
