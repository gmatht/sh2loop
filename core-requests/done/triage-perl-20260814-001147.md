# triage: perl backend can't render what the perl-sh-go frontend emits

## NEED
The perl renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend perl-sh-go, example t44_background.pl (cross-product pair).

## EVIDENCE
estree matches native; perl differs

## OUTCOME: implemented — verified perl-sh-go/t44_background.pl renders end-to-end (frontend emit -> strict ingress -> render) with the current core; the ingress/contract root cause (typed-C var_types/Cast/Sizeof, Ident arith, ForInit, new-node arms) is fixed.
