# triage: js backend can't render what the zsh-sh-go frontend emits

## NEED
The js renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend zsh-sh-go, example t51_arith_loop.zsh (cross-product pair).

## EVIDENCE
render failed: ShIR JSON ingress: Program.stmts[0].body[0].expr.args[1].elements[0].ast.lhs.type: unknown arith type "Ident"

## OUTCOME: implemented
The shared-core ingress accepts the A1 arith Ident node — ArithAst::Ident
+ KNOWN_ARITH "Ident" (src/shir_json_in.rs 1133, since commit ef84c6f,
re-verified in d54916b "arith-Ident verified fixed"). Reproduced this round
with the zsh-sh-go t51_arith_loop.zsh output: `debashc --shir-in-estree` /
`--shir-in-sh` / `--shir-in-perl` all ingress the exact failing path
(Program.stmts[0].body[0].expr.args[1].elements[0].ast.lhs.type "Ident") and
render programs whose stdout matches bash (2/4/6). Locked with the
shir_json_in arith_ident_ingress regression test (commit 72fca51). The 03:17
gate sweep still failing here while perl/go/rust/zig passed the same file
proves the WORKTREE is behind main: run `setup_backends.sh --sync` to merge
main (ef84c6f + d54916b + 72fca51) into backend/<lang> — no further core
change is needed.
