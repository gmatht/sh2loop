# triage: perl backend can't render what the cpp-sh-go frontend emits

## NEED
The perl renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend cpp-sh-go, example t05_arith_loop.cc (cross-product pair).

## EVIDENCE
render failed: ShIR JSON ingress: Program.stmts[1].type: unknown stmt type "ForInit"

## OUTCOME: rejected: already implemented in the shared core — the ForInit stmt (0b1e575 + for_init_roundtrip regression test) ingresses and renders; re-verified this round: cpp-sh-go t05_arith_loop.cc renders PASS/PASS-RENDER with current binaries; request filed against a stale sweep binary
