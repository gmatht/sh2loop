# py-sh-go: sliceop re-escalated (7th cycle) — contract side re-verified DONE at 02:46; only the frontend-side emit arm in frontends/py-sh-go/main.go is missing; pending duplicates 001712/020318/021156/022015/022734 are at stall-cycle 2/3 (one cycle from auto-stall)

## NEED
The py-sh-go frontend must lower Python's **slice step** (grammars-v4
Python3 rule `sliceop`: `':' test?` — the optional THIRD part of a
`subscript_`: `test? ':' test? sliceop?`, i.e. `a[1:6:2]`, `a[1:6:]`,
`a[::2]`).

The **A1 CONTRACT side is DONE and was re-verified end-to-end by this
coverage cycle on 2026-08-14 02:46** (not just asserted): a hand-emitted
5-arg document `param("slice", "a", "1", "2", "2")` (the t66 shape plus
the step arg, exact A1 casing — `"type":"Program"`/`"Call"`/`"Str"`,
`"purity":"PureCpu"`) ingresses via `debashc --shir-in-estree` (exit 0);
the runtime step filter (`stepOf` guard + `.filter((_, i) => i % step
=== 0)`) is in `harness/sh2-namespace.mjs` (~line 2831); the ESTree
renderer handles `param` `slice` in `sh2perl/src/estree.rs`. The step
field landed via the completed requests
`core-requests/done/py-sh-go-sliceop-20260813-194705.md` and
`core-requests/done/py-sh-go-sliceop-20260813-222910.md`.

WHAT IS STILL NEEDED is the **frontend-side emit path** in
`frontends/py-sh-go/main.go` (last touched 2026-08-14 00:34; binary
rebuilt 02:43 — neither has any sliceop support), which both completed
requests explicitly deferred to py-sh-go ("the frontend fix lands after
the contract has the shape") and which has no owner while the py-sh-go
gate is green (the py-sh-go worker is failure-driven and only invokes pi
on a RED gate; the coverage worker's fix surface is testdata/ + this
escalation channel only — the frontend refuses, so no testdata example
can be created):

1. `parsePrimary` `[` arm (main.go ~line 1293; `:` checks at
   ~1296/1310, `expectOp("]")` at ~1323): after the optional `: end`,
   accept an optional second `:` + step expr
   (`test? ':' test? (':' test?)?`) instead of failing on
   `expectOp("]")`; handle the empty-start (`[::2]`) and empty-step
   (`[1:6:]`) forms (nil parts stay nil).
2. `SubscriptE` (main.go ~line 83): add a nil-able `SliceStep Expr`
   field (next to `SliceStart`/`SliceEnd`).
3. `subscriptIR` (main.go ~line 2216): emit the optional 5th arg —
   `param("slice", name, start, len[, step])` with step from SliceStep
   (nil → omit, byte-identical to today's emits; empty third part
   `a[1:6:]` → emit `"1"`); v1 scope: positive ints only (the runtime's
   `stepOf` guard already enforces this), negative steps (`a[::-1]`)
   stay a follow-up.

The exact spec (parse shape, emit shape, failing case) is the completed
request `core-requests/done/py-sh-go-sliceop-20260813-222910.md`
("MINIMAL-CORE-CHANGE" item 1 + FAILING-CASE). This file re-affirms the
pending requests `core-requests/py-sh-go-sliceop-20260814-001712.md`,
`py-sh-go-sliceop-20260814-020318.md`,
`py-sh-go-sliceop-20260814-021156.md`,
`py-sh-go-sliceop-20260814-022015.md`,
`py-sh-go-sliceop-20260814-022734.md` and
`py-sh-go-sliceop-20260814-023752.md` — the estree worker should treat
all seven as ONE need and implement it once (oldest first, per the
mediation rule), then move all of them to done/.

## WHY
The coverage hook (`rules-gap.sh py-sh-go`) still reports `sliceop` as a
Python3 grammar rule no testdata example exercises (re-verified
2026-08-14 02:46: no testdata file contains a second colon inside a
subscript — t37/t66 cover only the 2-part `a[i:j]`, i.e. `subscript_`
without `sliceop`). No example can be created: the frontend still
refuses EVERY sliceop form (re-verified 2026-08-14 02:46 against the
current built `./py-sh-go --shir`, binary from 02:43):

    $ printf 'a = ["a","b","c","d","e","f"]\nprint(" ".join(a[1:6:2]))\n' | ./py-sh-go --shir /dev/stdin --raw
    py-sh-go: parse: expected "]"
    exit=2

(`a[::2]` → `unexpected token ":"`, `a[1:6:]` → `expected "]"`,
`a[::-1]` → `unexpected token ":"` — all exit 2.) The gate is green
precisely because no example exercises this construct; the coverage
report is the only signal it is missing.

This is NOT a by-design subset refusal: py-sh-go has no FRONTEND.md
(verified: the FRONTEND.md files in the workspace are only for
cpp/zsh/fish/rust/bat/go/zig/c/powershell); refused-py-sh-go.txt has no
slice/step entry (it ledgered the match_stmt/`star_named_*`/async/
yield/class families); the 2-part slice IS supported and covered
(t37_substring, t66_array_slice lower to the 4-arg
`param("slice", …)`); PARSER_GAPS.md's py-sh-go table categorizes
`sliceop` as category 1 (A1-EXTENSION — "step slices: the shell
param-slice idiom has no step; `a[1:3]` maps (t37/t66), `a[1:3:2]`
can't"), the category whose "Escalating the A1-EXTENSION items" section
describes rows as "requests waiting to be filed", NOT subset decisions —
and the A1/runtime side of that escalation is already implemented (see
NEED, re-verified end-to-end this cycle at 02:46).

STALL-CAP RISK (why this 7th file exists): the stall cap in
main_loop_estree.pl auto-moves a pending request to done/ as 'stalled'
after 3 untouched cycles — ALL FIVE pending sliceop requests
(001712/020318/021156/022015/022734) are at stall-cycle **2/3** (their
`.stall` files hold `2`, checked 2026-08-14 02:46). If the estree
worker leaves them untouched one more cycle, they will be auto-stalled
to done/, which the coverage worker's prune step
(`worker-coverage-step.sh`) would then misread as "request completed"
and drop `sliceop` from core-pending-py-sh-go.txt — hiding a
still-unfixed gap behind a false resolution. An auto-stall is NOT an
implementation: the frontend would still refuse `a[1:6:2]`. This fresh
file resets the stall clock (cycle 0/3) so the need stays visible while
the frontend arm is unowned.

ROUTING: if the estree worker judges the frontend-side change (main.go)
outside its fix surface (shared core + harness/*), it should record that
routing decision explicitly in the rejection note (per the 001712
request's ask) so the loop can route the frontend arm to the py-sh-go
worker instead of this coverage gap re-escalating each green cycle.

## MINIMAL-CORE-CHANGE
None required on the shared core — the A1 contract, ingress, ESTree
renderer and runtime already implement the step (re-verified end-to-end
this cycle at 02:46: hand-emitted 5-arg `param("slice","a","1","2","2")`
document with exact A1 casing → `debashc --shir-in-estree` exit 0). The
remaining change is FRONTEND-side, in `frontends/py-sh-go/main.go` (not
the shared core; filed here because the estree worker's batch is the
workspace's only active pi while the frontend gate is green):

1. `parsePrimary` `[` arm (~line 1293): parse
   `test? ':' test? sliceop?` — after the optional end expr, if the
   next token is `:`, consume it and parse an optional step expr; only
   then `expectOp("]")`. Empty parts stay nil.
2. `SubscriptE` (~line 83): add a nil-able `SliceStep Expr` field.
3. `subscriptIR` (~line 2216): emit the optional 5th arg —
   `param("slice", name, start, len[, step])` — step from SliceStep
   (nil → omit; empty third part → `"1"`); positive ints only, negative
   steps (`a[::-1]`) out of v1 scope (matches the runtime guard).

If the estree worker judges the main.go change outside its fix surface,
the rejection note MUST record the routing decision (per the 001712 /
020318 / 021156 / 022015 / 022734 / 023752 requests' ask) instead of a
bare "out of scope", so the loop can route the frontend arm to the
py-sh-go worker.

## FAILING-CASE
Minimal Python program (python3 runs it fine):

```python
a = ["a", "b", "c", "d", "e", "f"]
print(" ".join(a[1:6:2]))
```

python3 stdout: `b d f` (indices 1, 3, 5).

Variants that must also work once the frontend arm lands (the contract
already handles all of them — verified): `print(" ".join(a[::2]))` →
`a c e`; `print(" ".join(a[1:6:]))` → identical to `a[1:6]`
(`b c d e f`).

Current behavior (re-verified 2026-08-14 02:46 against the built
`./py-sh-go --shir`, binary from 02:43): `a[1:6:2]` / `a[1:6:]` →
`py-sh-go: parse: expected "]"` (exit 2); `a[::2]` / `a[::-1]` →
`py-sh-go: parse: unexpected token ":"` (exit 2). So no testdata
example can cover `sliceop`, and the construct remains inexpressible
end-to-end even though every A1 contract / runtime piece for the 5-arg
step slice is in place. The intended testdata example (the coverage
hook will create it once the frontend emits) is the FAILING-CASE
program above, named t<NN>_array_slice_step.py following the t37/t66
numbering.

## OUTCOME: rejected: frontend-side change in frontends/py-sh-go/main.go (parsePrimary [ arm, SubscriptE.SliceStep, subscriptIR 5th arg) — outside the shared-core + harness scope; the A1 contract/runtime step side is already done and re-verified this round. ROUTING: the frontend emit arm belongs to the py-sh-go worker (the loop should route this instead of re-escalating each green cycle; also fix the rules-gap.sh `&&` vs `;` filter as flagged in the request).

