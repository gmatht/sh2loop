# triage: perl backend can't render what the perl-sh-go frontend emits

## NEED
The perl renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend perl-sh-go, example t02_control.pl (cross-product pair).

## EVIDENCE
estree matches native; perl differs

## OUTCOME: rejected: backend-owned renderer staleness — main's perl renderer re-verified MATCHES native (perl renderer fixes 9340682/970d3e9 are on main, after the worktree's 08-14 02:04 sync); sync backend/perl and rebuild
