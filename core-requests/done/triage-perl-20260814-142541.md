# triage: perl backend can't render what the c-sh-go frontend emits

## NEED
The perl renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend c-sh-go, example t69_ptr_walk.c (cross-product pair).

## EVIDENCE
render failed: ShIR JSON ingress: Program.var_types[0].type: not a string

## OUTCOME: implemented
t69_ptr_walk.c verified green: c-sh-go gate (ingress acceptance + executed-stdout vs native) 103/103, including all 15 named examples. Root causes landed in prior core rounds: A1 var_types object form ({"kind":"Int32"} etc.) accepted by ir_type_from (shir_json_in.rs), arith "Cast" ingress, A1 arith Ident reads (shir_json.rs). estree 546/546; perl 370 baseline unchanged.
