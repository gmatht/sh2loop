# triage: js backend can't render what the zsh-sh-go frontend emits

## NEED
The js renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend zsh-sh-go, example t01_echo.zsh (cross-product pair).

## EVIDENCE
estree matches native; js differs

## OUTCOME: rejected: FAIL-BACKEND — the estree reference already renders this A1 and matches native; the js renderer lives in the backends/js worktree owned by its backend worker (out of core scope).
