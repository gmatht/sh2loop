# triage: perl backend can't render what the bat-sh-go frontend emits

## NEED
The perl renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend bat-sh-go, example t49_robocopy2.bat (cross-product pair).

## EVIDENCE
estree matches native; perl differs

## OUTCOME: rejected: phantom escalation — the estree reference itself fails on this pair (the runner refuses rsync, not in the source allowlist; harness/triage.sh SKIP-ESTREE-REF is the honest unverifiable verdict); nothing for any renderer to fix
