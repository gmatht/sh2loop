# triage: go backend can't render what the cpp-sh-go frontend emits

## NEED
The go renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend cpp-sh-go, example t29_goto.cc (cross-product pair).

## EVIDENCE
estree matches native; go differs
## OUTCOME: rejected: core ingress var_types object-form fix IS on main (t29 now ingresses); go worktree is 223 commits behind and its renderer compile-blocks on main's core — sync/renderer work belongs to the go worker (out of core scope)
