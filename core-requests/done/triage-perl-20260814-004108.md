# triage: perl backend can't render what the c-sh-go frontend emits

## NEED
The perl renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend c-sh-go, example t53_negative.c (cross-product pair).

## EVIDENCE
render failed: ShIR JSON ingress: Program.var_types[0].type: not a string

## OUTCOME: implemented — verified c-sh-go/t53_negative.c renders end-to-end (frontend emit -> strict ingress -> render) with the current core; the ingress/contract root cause (typed-C var_types/Cast/Sizeof, Ident arith, ForInit, new-node arms) is fixed.
