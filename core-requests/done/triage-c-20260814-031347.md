# triage: c backend can't render what the posix-sh-go frontend emits

## NEED
The c renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend posix-sh-go, example t51_arith_loop.sh (cross-product pair).

## EVIDENCE
render failed: ShIR JSON ingress: Program.stmts[0].body[0].expr.args[1].elements[0].ast.lhs.type: unknown arith type "Ident"

## OUTCOME: rejected: backend-ingress scope — the A1 arith node type "Ident" is contract-valid and the SHARED core already implements it (main ef84c6f, KNOWN_ARITH + ArithAst::Ident; re-verified: the exact t48/t51 emissions ingress via debashc --shir-in-estree exit 0, and the backend/perl cross-product pair PASSes); the c backend branch's own A1 ingress copy (backends/c/src/shir_json_in.rs KNOWN_ARITH + ArithAst in backends/c/src/ir.rs) is behind main and must accept Ident — the backend worker owns backends/c (setup_backends.sh --sync or port ef84c6f).
