# triage: js backend can't render what the c-sh-go frontend emits

## NEED
The js renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend c-sh-go, example t60_compound_ops.c (cross-product pair).

## EVIDENCE
render failed: ShIR JSON ingress: Program.var_types[0].type: expected {{kind: Float, width: N}}
## OUTCOME: rejected: already implemented in the shared core — main ingresses+renders this A1 (verified this round: estree reference OK on all 24 distinct A1s of the batch); the failing js worktree binary predates the typed-var_types/ForInit/Cast ingress (07671d8/0b1e575) — js backend worker: setup_backends.sh --sync + rebuild
