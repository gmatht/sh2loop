# triage: rust backend can't render what the py-sh-go frontend emits

## NEED
The rust renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend py-sh-go, example t21_array.py (cross-product pair).

## EVIDENCE
estree matches native; rust differs

## OUTCOME: rejected: no shared-core defect — triage classifies this FAIL-BACKEND (estree reference already matches native); the rust renderer is backend-owned and its worker is active
