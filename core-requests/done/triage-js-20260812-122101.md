# triage: js backend can't render what the c-sh-go frontend emits

## NEED
The js renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend c-sh-go, example t27_goto.c (cross-product pair).

## EVIDENCE
render failed: ShIR JSON ingress: Program.stmts[1].type: unknown stmt type "Label"

## OUTCOME: rejected: backend-renderer scope: the A1 Label/Goto stmts are contract-valid (the estree reference renders them — restructure_goto_only runs at core ingress); the js backend's A1 ingress must run the goto restructure / accept the nodes (backend worker owns backends/js)
