# triage: js backend can't render what the fish-sh-go frontend emits

## NEED
The js renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend fish-sh-go, example t81_regex_match.fish (cross-product pair).

## EVIDENCE
render failed:  thread 'main' (1610602) panicked at src/shir.rs:26264:18: internal error: entered unreachable code: Perl-only IR expression reached the ESTree renderer: Regex 

## OUTCOME: implemented — the Regex estree arm (f05a1c2) fixed the panic; verified js render succeeds and the estree reference matches native.
