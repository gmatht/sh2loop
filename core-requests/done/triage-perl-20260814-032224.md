# triage: perl backend can't render what the zsh-sh-go frontend emits

## NEED
The perl renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend zsh-sh-go, example t25_num_cmp.zsh (cross-product pair).

## EVIDENCE
estree matches native; perl differs

## OUTCOME: implemented
The shared-core perl renderer handles this A1: `debashc --shir-in-perl` on
the zsh-sh-go t25_num_cmp A1 (If cond `exec let "1 < 2"`) renders a script
whose stdout ("yes") matches bash (the `exec let` condition arm dates to
b70f7a3). The latest triage sweep already shows zsh-sh-go/t25_num_cmp.zsh →
perl PASS native match; the filed evidence came from an earlier sweep before
the perl worktree synced main. No core change needed; `setup_backends.sh
--sync` in the perl worktree is sufficient.
