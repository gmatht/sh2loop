# triage: perl backend can't render what the zsh-sh-go frontend emits

## NEED
The perl renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend zsh-sh-go, example t25_num_cmp.zsh (cross-product pair).

## EVIDENCE
estree matches native; perl differs

## OUTCOME: rejected: renderer-semantic difference in the perl backend (estree matches native) — not a shared-core issue; route to the perl backend worker (src/perl_backend.rs is in-repo but owned by that worker).

