# triage: perl backend can't render what the posix-sh-go frontend emits

## NEED
The perl renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend posix-sh-go, example t62_word_split.sh (cross-product pair).

## EVIDENCE
estree matches native; perl differs

## OUTCOME: implemented — verified PASS via `harness/triage.sh posix-sh-go t.sh perl` (fresh backend/perl binary): the backend/perl branch's sync of the core arith-Ident support (main ef84c6f) + its renderer work resolved the pair; estree reference also ingresses the emission (--shir-in-estree exit 0). No shared-core change was needed this round.
