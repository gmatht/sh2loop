# triage: c backend can't render what the py-sh-go frontend emits

## NEED
The c renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend py-sh-go, example t78_try_except.py (cross-product pair).

## EVIDENCE
render failed: ShIR JSON ingress: Program.stmts[0].type: unknown stmt type "Try"
## OUTCOME: rejected: backend-owned — the A1 is contract-valid (the estree reference renders it and matches native); the c renderer differs (FAIL-BACKEND verdict this round), a c_backend.rs defect in the backends/c worktree, out of shared-core scope
