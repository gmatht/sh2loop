# triage: perl backend can't render what the cpp-sh-go frontend emits

## NEED
The perl renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend cpp-sh-go, example t04_comment.cc (cross-product pair).

## EVIDENCE
render failed: ShIR JSON ingress: Program.var_types[0].type: not a string
## OUTCOME: rejected: stale — the current triage verdicts PASS (native match) for this pair (re-verified this round; perl binary rebuilt 2026-08-14 02:55); filed from an earlier red row
