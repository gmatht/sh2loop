# triage: perl backend can't render what the py-sh-go frontend emits

## NEED
The perl renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend py-sh-go, example t73_dict.py (cross-product pair).

## EVIDENCE
estree matches native; perl differs

## OUTCOME: implemented — verified 2026-08-16: harness/triage.sh pair PASS (native match). Fix landed this round: perl renderer setArray assoc-ness also from a prior `declare -A name` (self.hashes), mirroring the estree runtime's `isAssoc || assocNames.has(nm)` (backend/perl 718f3fc; the core emits the same flag-less shape for standalone `map=([k]=v)`). Perl backend gate 622/638 equiv unchanged.
