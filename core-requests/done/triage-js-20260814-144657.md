# triage: js backend can't render what the posix-sh-go frontend emits

## NEED
The js renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend posix-sh-go, example t51_arith_loop.sh (cross-product pair).

## EVIDENCE
render failed: ShIR JSON ingress: Program.stmts[0].body[0].expr.args[1].elements[0].ast.lhs.type: unknown arith type "Ident"
## OUTCOME: rejected: verified: the current backends/js binary renders posix-sh-go t51_arith_loop.sh PASS vs native (fixed by the backend/js worker's merges / prior ingress fixes since filing); no shared-core change needed.
