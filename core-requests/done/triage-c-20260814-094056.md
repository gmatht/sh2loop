# triage: c backend can't render what the posix-sh-go frontend emits

## NEED
The c renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend posix-sh-go, example t51_arith_loop.sh (cross-product pair).

## EVIDENCE
render failed: ShIR JSON ingress: Program.stmts[0].body[0].expr.args[1].elements[0].ast.lhs.type: unknown arith type "Ident"

## OUTCOME: rejected: already implemented in the shared core on main (07671d8 typed-C-int var_types + Cast/Sizeof ingress; ef84c6f arith Ident) — estree reference re-verified MATCHES native this round; backend/c worktree core predates main (last merge 08-06) — run setup_backends.sh --sync c and rebuild
