# triage: perl backend can't render what the bat-sh-go frontend emits

## NEED
The perl renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend bat-sh-go, example t48_robocopy.bat (cross-product pair).

## EVIDENCE
estree matches native; perl differs

## OUTCOME: implemented — bat-sh-go t48_robocopy.bat -> PASS (perl renderer handles the A1). Verified: triage last-row verdict PASS; estree gate 546/546; cargo test --lib 303 pass (only the pre-existing glsl WIP fail); check_ast OK.
