# triage: sh backend can't render what the py-sh-go frontend emits

## NEED
The sh renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend py-sh-go, example t73_dict.py (cross-product pair).

## EVIDENCE
render failed: render: word not renderable: Index { var: "d", key: Str("a", DoubleQuoted) }

## OUTCOME: implemented — 2026-08-15: main src/sh_backend.rs gained the A1 Index expr node arms (word + interp positions, literal/dynamic/@ keys, the POSIX per-element lowering) and the first-class Capture node arms (same `"$(...)"` rendering as the capture call), plus the keyed-literal setArray branch (`[k]=v` assoc items). t21_array/t56_array_append/t73_dict now render AND match native python; sh-corpus render refusals dropped 138 → 16 (the 16 are pre-existing process-substitution/`and` gaps).
