# triage: perl backend can't render what the zsh-sh-go frontend emits

## NEED
The perl renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend zsh-sh-go, example t32_redirect.zsh (cross-product pair).

## EVIDENCE
estree matches native; perl differs

## OUTCOME: rejected: already fixed in the shared core (970d3e9 perl redirect-interpolation — verified MATCH with the main binary this round); the backend/perl worktree predates the fix — merge main (setup_backends.sh --sync) to pick it up
