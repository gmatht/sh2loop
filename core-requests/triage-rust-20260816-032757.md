# triage: rust backend can't render what the cpp-sh-go frontend emits

## NEED
The rust renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend cpp-sh-go, example t29_goto.cc (cross-product pair).

## EVIDENCE
estree matches native; rust differs

## OUTCOME: implemented — t29_goto.cc PASS — goto restructure via the --shir-in-rust pipeline (bce9d03)
