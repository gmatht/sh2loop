# triage: js backend can't render what the posix-sh-go frontend emits

## NEED
The js renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend posix-sh-go, example t48_accum.sh (cross-product pair).

## EVIDENCE
render failed: ShIR JSON ingress: Program.stmts[1].body[0].expr.ast.rhs.type: unknown arith type "Ident"

## OUTCOME: rejected: already implemented in the shared core — the arith Ident node (ef84c6f; locked by the lifted-loop-var ingress regression test) is contract-valid and the estree reference renders it (re-verified this round: the real frontend A1 for this example ingresses on main and matches native); the failing backend/js worktree core predates main (merge-base c2d5cf6) and lacks the core fixes — merge main to pick it up
