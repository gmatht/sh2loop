# triage: js backend can't render what the cpp-sh-go frontend emits

## NEED
The js renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend cpp-sh-go, example t29_goto.cc (cross-product pair).

## EVIDENCE
estree matches native; js differs

## OUTCOME: implemented — t29_goto.cc PASS — pipeline + exec true/false + arith steps + break/continue (js worktree 5e7e7ac)
