# triage: perl backend can't render what the bat-sh-go frontend emits

## NEED
The perl renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend bat-sh-go, example t24_forf2.bat (cross-product pair).

## EVIDENCE
estree matches native; perl differs

## OUTCOME: rejected: not a shared-core gap — the A1 carries the delims as an IFS env on the read exec (estree + the sh backend render it correctly; sh PASSes t24_forf2); the perl worktree renderer drops the env Object on exec calls — the fix belongs to the --shir-in-perl worktree renderer
