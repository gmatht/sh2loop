# triage: rust backend can't render what the bat-sh-go frontend emits

## NEED
The rust renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend bat-sh-go, example t04_if.bat (cross-product pair).

## EVIDENCE
estree matches native; rust differs

## OUTCOME: rejected: out of scope — the rust renderer is owned by the backend/rust worktree worker (single-owner discipline); the shared-core estree reference already renders and matches native, so no shared-core change exists to make.
