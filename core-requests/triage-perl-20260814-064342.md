# triage: perl backend can't render what the posix-sh-go frontend emits

## NEED
The perl renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend posix-sh-go, example t51_arith_loop.sh (cross-product pair).

## EVIDENCE
render failed: ShIR JSON ingress: Program.stmts[0].body[0].expr.args[1].elements[0].ast.lhs.type: unknown arith type "Ident"

## OUTCOME: rejected: already satisfied by the shared core (verified this round — core debashc --shir-in-perl renders the A1 and matches native); the triage verdict came from the stale backend/perl worktree binary, which lags main (d49de8f contains→index / IncDec-stmt arms, ef84c6f arith Ident) — merge main into backend/perl and rebuild; out of core scope.
