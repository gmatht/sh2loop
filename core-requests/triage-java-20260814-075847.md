# triage: java backend can't render what the cpp-sh-go frontend emits

## NEED
The java renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend cpp-sh-go, example t05_arith_loop.cc (cross-product pair).

## EVIDENCE
render failed: ShIR JSON ingress: Program.stmts[1].type: unknown stmt type "ForInit"

## OUTCOME: rejected: already implemented in the shared core — the ForInit stmt node (locked by the for_init_roundtrip regression test) is contract-valid and the estree reference renders it (re-verified this round: the real frontend A1 for this example ingresses on main and matches native); the failing backend/java worktree core predates main (merge-base 429f9f1) and lacks the core fixes — merge main to pick it up
