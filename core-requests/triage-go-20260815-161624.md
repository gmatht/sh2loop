# triage: go backend can't render what the py-sh-go frontend emits

## NEED
The go renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend py-sh-go, example t71_range_loop.py (cross-product pair).

## EVIDENCE
estree matches native; go differs

## OUTCOME: rejected: no shared-core defect — triage classifies this FAIL-BACKEND (estree reference already matches native); the go renderer is backend-owned and its worker is active
