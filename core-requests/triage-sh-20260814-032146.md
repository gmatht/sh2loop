# triage: sh backend can't render what the zsh-sh-go frontend emits

## NEED
The sh renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend zsh-sh-go, example t29_increment.zsh (cross-product pair).

## EVIDENCE
estree matches native; sh differs

## OUTCOME: implemented
The sh renderer's arith-statement IncDec arm (Assign with a single target
whose expr is Arith IncDec on the SAME var -> `: $((...))`, never a bare
`$((...))` line or an x=$((x++)) clobber) landed in commit d54916b — its
commit message cites this exact example (triage-sh t29_increment). Reproduced
this round with the real frontend output: `debashc --shir-in-sh` on the
zsh-sh-go t29 A1 renders a script whose stdout ("2") matches bash. Locked
with the sh_backend incdec_assign_emits_colon_arith regression test (commit
72fca51). The 03:17 gate sweep failing while perl passed the same file proves
the sh WORKTREE is behind main: run `setup_backends.sh --sync` — no further
core change is needed.
