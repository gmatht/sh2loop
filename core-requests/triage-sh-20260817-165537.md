# triage: sh backend can't render what the fish-sh-go frontend emits

## NEED
The sh renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend fish-sh-go, example t53_param_default.fish (cross-product pair).

## EVIDENCE
estree matches native; sh differs
