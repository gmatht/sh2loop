# triage: rust backend can't render what the go-sh frontend emits

## NEED
The rust renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend go-sh, example t79_go_stmt.go (cross-product pair).

## EVIDENCE
estree matches native; rust differs

## DUPLICATE: triage-rust-20260816-234940
## REASON: Identical pair — same frontend (go-sh), same example
## (t79_go_stmt.go), same backend (rust), same evidence ("estree matches
## native; rust differs"). This is the worker's re-escalation of the pair
## 234940 already tracks (the sweep re-filed it after the earlier row was
## superseded by a PASS). Merge into 234940, whose NOT-A-TRANSFORM verdict
## (rust renderer t79 codegen, fixed in cfd5c4b) covers both.
