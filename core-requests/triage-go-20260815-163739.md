# triage: go backend can't render what the zsh-sh-go frontend emits

## NEED
The go renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend zsh-sh-go, example t45_env_set.zsh (cross-product pair).

## EVIDENCE
estree matches native; go differs

## OUTCOME: rejected: FAIL-BACKEND — the estree reference already renders this A1 and matches native; the go renderer lives in the backends/go worktree owned by its backend worker (out of core scope).
