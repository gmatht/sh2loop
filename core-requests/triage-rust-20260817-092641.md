# triage: rust backend can't render what the bat-sh-go frontend emits

## NEED
The rust renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend bat-sh-go, example t36_redirect_var.bat (cross-product pair).

## EVIDENCE
estree matches native; rust differs

## SUPERSEDED: dead_store_elim (redirect-target reads)
## REASON: Same cluster as the sh request: the broken estree reference (DSE
## dropped the redirect-target store, folding `%f%` to '') made the
## correctly-rendering rust output "differ". Commit 1e123dd (12:40) fixed the
## DSE read-set walk AND landed the rust_backend Redirect arm (native dup/dup2,
## getVar targets read the hoisted native var) + the generic exec arm; verified
## today: the rust render of this A1 executes to "hi\nmore" (matches the
## corrected oracle), and the 16:58 sweep records rust PASS.
