# triage: java backend can't render what the cpp-sh-go frontend emits

## NEED
The java renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend cpp-sh-go, example t04_comment.cc (cross-product pair).

## EVIDENCE
render failed: ShIR JSON ingress: Program.var_types[0].type: not a string

## OUTCOME: rejected: already implemented in the shared core — typed-C-int var_types objects (07671d8) is contract-valid and the estree reference renders it (re-verified this round: the real frontend A1 for this example ingresses on main and matches native); the failing backend/java worktree core predates main (merge-base 429f9f1) and lacks the core fixes — merge main to pick it up
