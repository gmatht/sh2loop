# triage: perl backend can't render what the py-sh-go frontend emits

## NEED
The perl renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend py-sh-go, example t44_background.py (cross-product pair).

## EVIDENCE
estree matches native; perl differs

## OUTCOME: implemented
IrStmt::Background + the `wait` builtin landed in the shared-core perl
renderer (src/ir.rs, commit pending): `( echo bg ) &` forks a child
(autoflush before fork so buffered stdout is never duplicated) and
`wait` reaps via `while (wait() != -1) {}`. posix-sh-go t44_background.sh
and py-sh-go t44_background.py now render and match native (regression
test background_stmt_forks_and_wait_reaps; ./fail 360 vs 325 baseline).
