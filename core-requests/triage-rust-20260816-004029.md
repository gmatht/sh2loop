# triage: rust backend can't render what the bat-sh-go frontend emits

## NEED
The rust renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend bat-sh-go, example t48_robocopy.bat (cross-product pair).

## EVIDENCE
estree matches native; rust differs

## OUTCOME: rejected: backend/rust worktree renderer gap (param/arith/exec/builtin lowering); the rust worker owns the renderer (in-flight merge, uncommitted work) — estree reference (shared core) already matches native; no shared-core change exists.
