# triage: rust backend can't render what the perl-sh-go frontend emits

## NEED
The rust renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend perl-sh-go, example t66_array_slice.pl (cross-product pair).

## EVIDENCE
estree matches native; rust differs

## OUTCOME: rejected: backend-renderer scope — the rust renderer lives in the backends/rust worktree (backend/rust branch, owned by that backend worker); the estree reference already renders this A1 and matches native, so no shared-core change is needed.
