# triage: js backend can't render what the posix-sh-go frontend emits

## NEED
The js renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend posix-sh-go, example t48_accum.sh (cross-product pair).

## EVIDENCE
render failed: ShIR JSON ingress: Program.stmts[1].body[0].expr.ast.rhs.type: unknown arith type "Ident"

## OUTCOME: rejected: backend-worktree scope — the A1 arith Ident node is contract-valid and the shared core ingresses it since ef84c6f (regression-locked by arith_ident_ingress, 72fca51; estree+perl pass); this backend worktree's forked shir_json_in.rs (KNOWN_ARITH without "Ident") predates that sync — merge main / port the Ident arm (backend worker owns the worktree)
