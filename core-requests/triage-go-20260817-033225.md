# triage: go backend can't render what the bat-sh-go frontend emits

## NEED
The go renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend bat-sh-go, example t23_labels.bat (cross-product pair).

## EVIDENCE
estree matches native; go differs
