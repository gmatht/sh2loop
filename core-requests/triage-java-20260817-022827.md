# triage: java backend can't render what the rust-frontend frontend emits

## NEED
The java renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend rust-frontend, example t24_print.rs (cross-product pair).

## EVIDENCE
estree matches native; java differs
