# triage: perl backend can't render what the bat-sh-go frontend emits

## NEED
The perl renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend bat-sh-go, example t24_forf2.bat (cross-product pair).

## EVIDENCE
estree matches native; perl differs

## OUTCOME: rejected: backend-owned renderer — the estree reference proves the A1 correct; the forf read-with-IFS A1 shape is contract-locked (bat forf cluster), but the perl renderer's read-loop ignores the trailing IFS Object and falls back to whitespace splitting ("item one,two" vs estree "item one") — perl_backend.rs lives in the backend/perl worktree
