# triage: sh backend can't render what the cpp-sh-go frontend emits

## NEED
The sh renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend cpp-sh-go, example t14_break.cc (cross-product pair).

## EVIDENCE
render failed: render: command expression not renderable: Arith(IncDec { var: "i", delta: 1, prefix: false })

## OUTCOME: implemented — 2026-08-15: statement-position Arith (IncDec) arm added to cmd_to_sh in the sh backend (backends/sh worktree commit e2e3c7b; the main core already had the arm at src/sh_backend.rs:1925). t14_break.cc now renders through --shir-in-sh and the executed output matches native C++ (1 / 2 / done i=3).
