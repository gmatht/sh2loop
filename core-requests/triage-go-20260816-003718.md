# triage: go backend can't render what the cpp-sh-go frontend emits

## NEED
The go renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend cpp-sh-go, example t27_sized_type.cc (cross-product pair).

## EVIDENCE
estree matches native; go differs

## OUTCOME: rejected: backend/go worktree renderer gap; the go worktree core is 216 commits behind main (ingress cannot parse object-typed var_types for t27; zsh mathfunc text shelled out raw for t89) — needs `setup_backends.sh --sync go` + renderer work by the go worker; estree reference already matches native.
