# triage: rust backend can't render what the bat-sh-go frontend emits

## NEED
The rust renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend bat-sh-go, example t52_pipeline.bat (cross-product pair).

## EVIDENCE
estree matches native; rust differs

## OUTCOME: rejected: backend-renderer scope: the rust renderer lives in the backends/rust worktree (backend/rust branch, owned by that backend worker); the 09:25 triage verdicts confirm "estree matches native; rust differs", so the A1 is correct and no shared-core change is needed.
