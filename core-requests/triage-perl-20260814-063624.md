# triage: perl backend can't render what the perl-sh-go frontend emits

## NEED
The perl renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend perl-sh-go, example t68_case_glob.pl (cross-product pair).

## EVIDENCE
estree matches native; perl differs

## OUTCOME: implemented
The spaced-test tokenizer now keeps `=~` as ONE op token and the perl
test renderer emits a regex match (was `=` + `~` → `$s eq '~'` garbage).
t68_case_glob.pl renders star/alt2 and matches native (regression test
regex_test_op_renders_match).
