# triage: java backend can't render what the c-sh-go frontend emits

## NEED
The java renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend c-sh-go, example t79_switch_midbreak.c (cross-product pair).

## EVIDENCE
render failed: ShIR JSON ingress: Program.var_types[0].type: not a string
## OUTCOME: rejected: the var_types object-IrType ingress gap was fixed in the shared core (ir_type_from object arms, pinned by for_init_roundtrip); triage now records FAIL-BACKEND (estree matches native, java differs) — java renderer, owned by the backend/java worktree worker.
