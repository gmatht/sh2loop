# triage: go backend can't render what the zig-sh-go frontend emits

## NEED
The go renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend zig-sh-go, example t13_const_cast.zig (cross-product pair).

## EVIDENCE
estree matches native; go differs

## OUTCOME: implemented — pair verified PASS via triage this round (zig-sh-go t13_const_cast.zig x go)
