# triage: c backend can't render what the c-sh-go frontend emits

## NEED
The c renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend c-sh-go, example t65_ternary.c (cross-product pair).

## EVIDENCE
render failed: ShIR JSON ingress: Program.var_types[0].type: not a string
## OUTCOME: rejected: backend-renderer scope: FAIL-BACKEND — the cited shared-core ingress gap is fixed (07671d8/0b1e575; the A1 ingresses and renders), the A1 is contract-valid (estree matches native per triage), but the c backend's renderer output differs (worker owns backends/c worktree)
