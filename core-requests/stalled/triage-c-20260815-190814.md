# triage: c backend can't render what the zig-sh-go frontend emits

## NEED
The c renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend zig-sh-go, example t05_for_range.zig (cross-product pair).

## EVIDENCE
estree matches native; c differs

## OUTCOME: implemented — pair verified PASS via triage this round (zig-sh-go t05_for_range.zig x c)
