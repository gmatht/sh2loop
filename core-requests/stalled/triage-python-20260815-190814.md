# triage: python backend can't render what the zig-sh-go frontend emits

## NEED
The python renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend zig-sh-go, example t03_if_else.zig (cross-product pair).

## EVIDENCE
estree matches native; python differs

## OUTCOME: implemented
