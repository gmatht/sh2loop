# triage: c backend can't render what the powershell-sh-go frontend emits

## NEED
The c renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend powershell-sh-go, example t06_do_statement.ps1 (cross-product pair).

## EVIDENCE
estree matches native; c differs

## OUTCOME: rejected: backend-renderer scope: FAIL-BACKEND — the A1 is contract-valid (estree reference matches native); the c backend's renderer differs (worker owns backends/c worktree)
