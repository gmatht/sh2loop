# triage: sh backend can't render what the py-sh-go frontend emits

## NEED
The sh renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend py-sh-go, example t21_array.py (cross-product pair).

## EVIDENCE
render failed: render: word not renderable: Index { var: "a", key: Str("1", DoubleQuoted) }
