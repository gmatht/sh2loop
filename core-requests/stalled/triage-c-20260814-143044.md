# triage: c backend can't render what the cpp-sh-go frontend emits

## NEED
The c renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend cpp-sh-go, example t04_comment.cc (cross-product pair).

## EVIDENCE
render failed: ShIR JSON ingress: Program.var_types[0].type: not a string

## OUTCOME: implemented — the cited ingress gap (var_types object-form IrType) is fixed in the shared core (ir_type_from object arms, commit 07671d8 era; pinned by the typed var_types round-trip tests); the cpp-sh-go t04_comment.cc pair verified PASS via triage this round
