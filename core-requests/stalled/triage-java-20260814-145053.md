# triage: java backend can't render what the zsh-sh-go frontend emits

## NEED
The java renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend zsh-sh-go, example t48_accum.zsh (cross-product pair).

## EVIDENCE
render failed: ShIR JSON ingress: Program.stmts[1].body[0].body[0].expr.ast.rhs.type: unknown arith type "Ident"
## OUTCOME: rejected: the Ident arith ingress gap was fixed in the shared core (KNOWN_ARITH + ArithAst::Ident, pinned by the arith-Ident regression test); triage now records SKIP-REFUSE "expr not in the v1 Java subset: Arith(Assign...)" — a java_backend.rs renderer refusal, owned by the backend/java worktree worker.
