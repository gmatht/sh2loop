# triage: go backend can't render what the powershell-sh-go frontend emits

## NEED
The go renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend powershell-sh-go, example t13_foreach.ps1 (cross-product pair).

## EVIDENCE
estree matches native; go differs

## OUTCOME: rejected: backend-renderer scope: SKIP-REFUSE — the split Call is contract-valid (estree reference matches native) but the go renderer's documented REFUSE (TODO(unsupported): split) beats guessing (worker owns backends/go worktree)
