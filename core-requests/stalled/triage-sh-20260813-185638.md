# triage: sh backend can't render what the perl-sh-go frontend emits

## NEED
The sh renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend perl-sh-go, example t68_case_glob.pl (cross-product pair).

## EVIDENCE
estree matches native; sh differs
