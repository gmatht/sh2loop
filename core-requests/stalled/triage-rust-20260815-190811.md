# triage: rust backend can't render what the zig-sh-go frontend emits

## NEED
The rust renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend zig-sh-go, example t12_comptime_const.zig (cross-product pair).

## EVIDENCE
estree matches native; rust differs

## OUTCOME: rejected: backend/rust worktree renderer gap (printf lowering); the rust worker owns it and is actively developing it (in-flight merge, uncommitted printf arms at 02:13) — the shared-core estree reference already matches native; no shared-core change exists.
