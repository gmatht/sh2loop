# triage: c backend can't render what the c-sh-go frontend emits

## NEED
The c renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend c-sh-go, example t54_clamp_abs.c (cross-product pair).

## EVIDENCE
render failed: ShIR JSON ingress: Program.var_types[0].type: not a string
## OUTCOME: implemented — the cited ingress gap (var_types object-form IrType) is fixed in the shared core (ir_type_from object arms, 07671d8; pinned by the 0b1e575 typed var_types round-trip lock); the c-sh-go t54_clamp_abs.c pair re-verified PASS via harness/triage.sh this round
