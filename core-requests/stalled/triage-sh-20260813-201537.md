# triage: sh backend can't render what the zsh-sh-go frontend emits

## NEED
The sh renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend zsh-sh-go, example t84_arith_index.zsh (cross-product pair).

## EVIDENCE
estree matches native; sh differs

## OUTCOME: implemented — sh renderer now handles the A1 arith Index node with NEGATIVE keys (zsh a[-1] = last element): arith_to_sh const-folds keys and routes negatives through _arr_get, which maps k<0 onto len+k. Fixed this round in backends/sh src/sh_backend.rs; pair PASS (31/40 vs native zsh).
