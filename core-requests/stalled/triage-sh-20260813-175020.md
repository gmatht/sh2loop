# triage: sh backend can't render what the c-sh-go frontend emits

## NEED
The sh renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend c-sh-go, example t83_const_ptr.c (cross-product pair).

## EVIDENCE
render failed: ShIR JSON ingress: Program.var_types[0].type: not a string

## OUTCOME: rejected: C pointer-deref protocol (memLoad/addrOf/memStore — 11 c-sh-go examples) is outside the shell-flavored A1 subset; the estree JS runtime has a heap emulation the sh runtime (real bash) lacks — sh renderer refuses loudly (Refuse > guess).
