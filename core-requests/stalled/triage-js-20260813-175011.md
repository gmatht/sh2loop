# triage: js backend can't render what the c-sh-go frontend emits

## NEED
The js renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend c-sh-go, example t04_if_else.c (cross-product pair).

## EVIDENCE
render failed: ShIR JSON ingress: Program.var_types[0].type: expected {{kind: Float, width: N}}
## OUTCOME: implemented — 2026-08-16 triage re-verified: c-sh-go x js t04_if_else.c = PASS (native match). The ingress rejection (var_types object IrType) was fixed in the shared core (ir_type_from object arms; pinned by for_init_roundtrip + dowhile regression tests); nothing further remains for the core.
