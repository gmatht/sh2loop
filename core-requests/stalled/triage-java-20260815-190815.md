# triage: java backend can't render what the zig-sh-go frontend emits

## NEED
The java renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend zig-sh-go, example t01_print.zig (cross-product pair).

## EVIDENCE
estree matches native; java differs
## OUTCOME: implemented — 2026-08-16 triage re-verified: zig-sh-go x java t01_print.zig = PASS (native match). The A1 ingresses cleanly through the shared core and the java backend now renders it correctly.
