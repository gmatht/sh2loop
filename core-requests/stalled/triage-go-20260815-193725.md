# triage: go backend can't render what the sh2perl frontend emits

## NEED
The go renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend sh2perl, example _wiretest.sh (cross-product pair).

## EVIDENCE
estree matches native; go differs
