# triage: java backend can't render what the posix-sh-go frontend emits

## NEED
The java renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend posix-sh-go, example t48_accum.sh (cross-product pair).

## EVIDENCE
render failed: ShIR JSON ingress: Program.stmts[1].body[0].expr.ast.rhs.type: unknown arith type "Ident"

## OUTCOME: rejected: superseded: the cited ShIR JSON ingress errors (arith Ident / object var_types / Try-Goto-ForInit stmts) are fixed in the shared core (shir_json_in.rs: KNOWN_ARITH Ident, ir_type_from object arms, stmt arms — lib roundtrip-pinned) and the java renderer arms for these exact pairs are at main (1ec0871, submodule HEAD); the 09:25 triage verdicts confirm ingress passes — the residual is the backend/java worktree's stale v1-subset renderer (backend/java branch, worker-owned sync; that worker is currently down)
