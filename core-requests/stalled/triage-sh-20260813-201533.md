# triage: sh backend can't render what the zsh-sh-go frontend emits

## NEED
The sh renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend zsh-sh-go, example t78_arith_assign.zsh (cross-product pair).

## EVIDENCE
estree matches native; sh differs

## OUTCOME: implemented — sh renderer now renders frontend `exec let` args: strips the GLOB_MAGIC prefix (reference semantics) and treats compound assignments (*=, +=, ...) as assignment (LHS stays bare, side effect no longer lost). Fixed this round in backends/sh src/sh_backend.rs; pair PASS (11 vs native zsh).
