# triage: sh backend can't render what the c-sh-go frontend emits

## NEED
The sh renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend c-sh-go, example t04_if_else.c (cross-product pair).

## EVIDENCE
render failed: ShIR JSON ingress: Program.var_types[0].type: not a string

## OUTCOME: implemented — pair now PASS (native match) via the sh renderer evolution on backend/sh (arith/loop/until/ternary/increment/continue/index arms); verified by harness/triage.sh on 2026-08-16
