# triage: sh backend can't render what the posix-sh-go frontend emits

## NEED
The sh renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend posix-sh-go, example t75_arith_compound.sh (cross-product pair).

## EVIDENCE
estree matches native; sh differs
