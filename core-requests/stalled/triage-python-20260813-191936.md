# triage: python backend can't render what the posix-sh-go frontend emits

## NEED
The python renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend posix-sh-go, example t48_accum.sh (cross-product pair).

## EVIDENCE
render failed: ShIR JSON ingress: Program.stmts[1].body[0].expr.ast.rhs.type: unknown arith type "Ident"

## OUTCOME: implemented — t48_accum.sh PASS (b57adcd/13860af)
