# triage: python backend can't render what the bat-sh-go frontend emits

## NEED
The python renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend bat-sh-go, example t29_mixed2.bat (cross-product pair).

## EVIDENCE
render failed: ShIR JSON ingress: Program.stmts[9].type: unknown stmt type "Goto"
