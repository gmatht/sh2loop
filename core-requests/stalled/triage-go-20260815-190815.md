# triage: go backend can't render what the zig-sh-go frontend emits

## NEED
The go renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend zig-sh-go, example t01_print.zig (cross-product pair).

## EVIDENCE
estree matches native; go differs

## OUTCOME: rejected: backend-renderer scope: FAIL-BACKEND — the A1 is contract-valid (estree reference matches native); the go backend's renderer differs (worker owns backends/go worktree)
