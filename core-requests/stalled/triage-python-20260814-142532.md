# triage: python backend can't render what the c-sh-go frontend emits

## NEED
The python renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend c-sh-go, example t47_digit_sum.c (cross-product pair).

## EVIDENCE
render failed: ShIR JSON ingress: Program.var_types[0].type: not a string

## OUTCOME: implemented: shared-core python renderer (shir_to_python keeps the frontend typed var_types; ForInit/Break/Continue/DoWhile stmt arms; real sh2.* runtime for the C memory arena, store, arrays, ternary/param/arith) landed in sh2perl main 1acf1f7 + backend/python e25b3a5; verified PASS via harness/triage.sh; estree 546/546, perl 370, check_ast 22/22
