# triage: c backend can't render what the c-sh-go frontend emits

## NEED
The c renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend c-sh-go, example t39_strlen_var.c (cross-product pair).

## EVIDENCE
render failed: ShIR JSON ingress: Program.stmts[1].body[1].expr.args[1].elements[1].ast.type: unknown arith type "Cast"
## OUTCOME: implemented — the cited ingress gap (arith "Cast") is fixed in the shared core (KNOWN_ARITH Cast + ArithAst::Cast arm, 07671d8); the c-sh-go t39_strlen_var.c pair re-verified PASS via harness/triage.sh this round
