# triage: sh backend can't render what the perl-sh-go frontend emits

## NEED
The sh renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend perl-sh-go, example t02_control.pl (cross-product pair).

## EVIDENCE
render failed: render: command expression not renderable: Arith(IncDec { var: "n", delta: 1, prefix: false })

## OUTCOME: implemented
