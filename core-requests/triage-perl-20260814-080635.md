# triage: perl backend can't render what the perl-sh-go frontend emits

## NEED
The perl renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend perl-sh-go, example t27_not_expr.pl (cross-product pair).

## EVIDENCE
estree matches native; perl differs

## OUTCOME: rejected: backend-owned renderer — the estree reference proves the A1 correct; the test-call negation arm renders `!("( ... )")` (quoted-string truthiness, always false) so the not-branch is dropped — perl_backend.rs lives in the backend/perl worktree
