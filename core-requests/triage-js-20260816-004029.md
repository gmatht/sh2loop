# triage: js backend can't render what the bat-sh-go frontend emits

## NEED
The js renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend bat-sh-go, example t48_robocopy.bat (cross-product pair).

## EVIDENCE
estree matches native; js differs

## OUTCOME: rejected: backend/js worktree renderer gap (TODO stubs); the js worker owns the renderer — estree reference (shared core) already matches native; no shared-core change exists.
