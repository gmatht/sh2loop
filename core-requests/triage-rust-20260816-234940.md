# triage: rust backend can't render what the go-sh frontend emits

## NEED
The rust renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend go-sh, example t79_go_stmt.go (cross-product pair).

## EVIDENCE
estree matches native; rust differs

## NOT-A-TRANSFORM: rust backend renderer (t79 codegen, cfd5c4b)
## REASON: The pair fails on RENDERER emission, not on a shared A1/IR shape:
## the rust renderer's codegen for the 50M-loop's discarded-value arith step
## (`i++` through a String round-trip per iteration) took 59s at -O0, tripping
## the triage harness's 30s timeout → empty stdout → "rust differs". The A1 is
## fine (estree matches native; the same A1 renders and runs correctly today).
## The rust worker fixed it in cfd5c4b (12:50): discarded-value arith
## statements render straight to i64 (18s at -O0). Pure rust emission — stays
## with the rust worker; no shared transform to bundle.
