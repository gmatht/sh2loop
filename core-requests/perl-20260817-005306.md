# perl: TRAPPED (build fails repeatedly)

## NEED
The perl worker cannot build; it likely needs a shared-core change (shIR node / deserializer / contract / parser) it cannot make in-scope. Inspect the worker log and the pi-fix attempts.

## WHY
Repeated build failures; see frontends-or-backends/backend and harness/.

## MINIMAL-CORE-CHANGE
To be determined by the estree worker from the failing build.

## FAILING-CASE
See the worker log: /home/llm/sh2loop/loop-frontend-or-backend-perl.log.
