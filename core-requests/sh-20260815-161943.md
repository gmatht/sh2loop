# sh: TRAPPED (build fails repeatedly)

## NEED
The sh worker cannot build; it likely needs a shared-core change (shIR node / deserializer / contract / parser) it cannot make in-scope. Inspect the worker log and the pi-fix attempts.

## WHY
Repeated build failures; see frontends-or-backends/backend and harness/.

## MINIMAL-CORE-CHANGE
To be determined by the estree worker from the failing build.

## FAILING-CASE
See the worker log: /home/llm/sh2loop/loop-frontend-or-backend-sh.log.

## OUTCOME: implemented — 2026-08-15: trap root cause fixed in core commit 68fb13c (cli/src/lib.rs): the top-level --shir/--shir-raw file arms now decode invalid-UTF-8 bytes with SharedUtils::bytes_to_marked_lossy (U+E000 PUA) instead of String::from_utf8_lossy (U+FFFD). The sh gate's only remaining red (utf8-non-utf8-content.sh) renders byte-identical end-to-end (--shir | --shir-in-sh → sh out.sh == bash stdout). estree corpus 545/546 (baseline), perl 314/232 (baseline).
