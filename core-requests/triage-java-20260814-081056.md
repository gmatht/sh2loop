# triage: java backend can't render what the posix-sh-go frontend emits

## NEED
The java renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend posix-sh-go, example t51_arith_loop.sh (cross-product pair).

## EVIDENCE
render failed: ShIR JSON ingress: Program.stmts[0].body[0].expr.args[1].elements[0].ast.lhs.type: unknown arith type "Ident"

## OUTCOME: rejected: already implemented in the shared core — the arith Ident node (ef84c6f; locked by the lifted-loop-var ingress regression test) is contract-valid and the estree reference renders it (re-verified this round: the real frontend A1 for this example ingresses on main and matches native); the failing backend/java worktree core predates main (merge-base 429f9f1) and lacks the core fixes — merge main to pick it up
