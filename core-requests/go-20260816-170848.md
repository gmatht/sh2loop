# go: probe args.go stalled at MISMATCH (TRANSLATE_ONE_APPLICATION loop)

## NEED
The construct below cannot be translated within the allowed
surface (frontends/go/ + harness/ + target). It likely needs
a shared-core or contract change.

## FAILING-CASE
Probe: /tmp/wk/probes/args.go
// args: os.Args[1:] — the program's argv with argv0 stripped (the
// shell's `"$@"`). FRONTEND-GAP (landed, go-sh.go): the A1/runtime
// positional slice `${@:off:len}` IS array-valued (the core lowers
// param("slice", "@", off, len) to the runtime's native positional
// list — bash offsets are 1-based, ${@:1} = all params, so
// os.Args[1:] ↔ param("slice", "@", "1", "")), and the runtime's
// setArray SPLICES the slice array into the array store — the frontend
// now lowers `args := os.Args[1:]` to that setArray shape, so
// len(args)/args[i] read off the store. `range args` over the result
// stays REFUSED (the A1 For iter is a static element list — a
// runtime-loaded array has no iter shape; see
// core-requests/go-sh-dogfood-20260815-contract-boundaries.md §11).
// fail-go runs the oracle with no args, so len(os.Args[1:]) is
// deterministically 0 (and the translated run matches).
args := os.Args[1:]
fmt.Println(len(args))

Expected (source-native):

Actual (target):
0

## MINIMAL-CORE-CHANGE
To be determined by the core owner from the failing case.
