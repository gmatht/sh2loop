# triage: js backend can't render what the bat-sh-go frontend emits

## NEED
The js renderer must handle this A1 (the estree reference renders it and
matches native).

## FAILING-CASE
frontend bat-sh-go, example t14_call.bat (cross-product pair).

## EVIDENCE
estree matches native; js differs

## OUTCOME: rejected: out of scope — the js renderer is owned by the backend/js worktree worker (single-owner discipline); the shared-core estree reference already renders and matches native, so no shared-core change exists to make.
